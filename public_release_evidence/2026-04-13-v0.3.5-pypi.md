# 2026-04-13 PyPI Publish + Post-Publish Smoke (`v0.3.5`)

- Status: success
- Version / tag: `0.3.5` / `v0.3.5`
- Target: post-publish public-package smoke against `cureview==0.3.5` on PyPI plus GitHub Release asset verification
- Commands run:
  - `gh run watch 24323219760 --exit-status`
  - `gh run view 24323219760 --json status,conclusion,url,displayTitle`
  - `gh release view v0.3.5 --json tagName,name,isDraft,isPrerelease,url,assets`
  - `uvx --refresh --from cureview==0.3.5 cure --help`
  - `HOME=<temp-root>/home XDG_CONFIG_HOME=<temp-root>/home/config XDG_STATE_HOME=<temp-root>/home/state XDG_CACHE_HOME=<temp-root>/home/cache OPENAI_API_KEY=dummy-openai-key uvx --from cureview==0.3.5 cure setup --agent codex`
  - `UV_TOOL_DIR=<temp-root>/uv-tools UV_TOOL_BIN_DIR=<temp-root>/uv-tool-bin uv tool install --force cureview==0.3.5`
  - `HOME=<temp-root>/home XDG_CONFIG_HOME=<temp-root>/home/config XDG_STATE_HOME=<temp-root>/home/state XDG_CACHE_HOME=<temp-root>/home/cache OPENAI_API_KEY=dummy-openai-key <temp-root>/uv-tool-bin/cure setup --agent codex`
  - `HOME=<temp-root>/home XDG_CONFIG_HOME=<temp-root>/home/config XDG_STATE_HOME=<temp-root>/home/state XDG_CACHE_HOME=<temp-root>/home/cache OPENAI_API_KEY=dummy-openai-key <temp-root>/uv-tool-bin/cure doctor --pr-url https://github.com/chunkhound/chunkhound/pull/220 --json`
- Changelog entry shipped:
  - `### Fixed`
  - `- cure pr with the Codex provider no longer aborts the review-intelligence gate with "missing successful code_research" on non-trivial repos. The staged cure-chunkhound helper now emits its first tools/call waiting heartbeat immediately (rather than after 10s) and repeats every 5s, so codex-cli sees a visibly live output stream during the 2–5 min LLM synthesis step of research and no longer surfaces the call to the model as a hang. The prompt templates used by cure pr also now explicitly tell the model that research legitimately takes 2–5 minutes per call on large repos, that the heartbeat lines are normal progress rather than a hang, and that it must issue one research invocation at a time and wait for its final JSON object before issuing another.`
- Verified public command surface:
  - `Publish Package` workflow run `24323219760` completed with conclusion `success` (https://github.com/grzegorznowak/CURe/actions/runs/24323219760)
  - GitHub Release `v0.3.5` is visible (non-draft, non-prerelease) at https://github.com/grzegorznowak/CURe/releases/tag/v0.3.5 with the four expected assets: `cureview-v0.3.5-linux-x86_64.tar.gz`, `cureview-v0.3.5-macos-arm64.tar.gz`, `cureview-v0.3.5-macos-x86_64.tar.gz`, and `SHA256SUMS`
  - `uvx --from cureview==0.3.5 cure --help` fetched the published wheel and exited 0
  - `uvx --from cureview==0.3.5 cure setup --agent codex` succeeded in a clean temp-root layout and persisted `codex-cli`
  - `uv tool install cureview==0.3.5` into an isolated `UV_TOOL_DIR`/`UV_TOOL_BIN_DIR` installed the `cure` executable
  - `cure doctor --pr-url https://github.com/chunkhound/chunkhound/pull/220 --json` via the uv-tool-installed binary completed with summary `fail=0`, `ok=13`, `warn=5` after running `cure setup --agent codex` in the same temp-root
  - anonymous public GitHub PR metadata fallback worked without `gh auth login`
- What worked:
  - `Publish Package` workflow built + published the wheel and sdist to PyPI and uploaded all three standalone archives plus `SHA256SUMS` to the GitHub Release
  - `uvx --from cureview==0.3.5 cure --help` confirmed PyPI now serves the new version
  - `uv tool install cureview==0.3.5` flow produced a working `cure` binary identical in surface to the local wheel smoke
  - `cure doctor` reported the same `fail=0`, `ok=13`, `warn=5` shape as the local artifact smoke once `cure setup --agent codex` had been run in the temp-root
- Blockers / risks:
  - `doctor` reported warnings only, not failures
  - warning sources were expected for this smoke layout: missing Jira config, repo-local ChunkHound hint incompatible because the candidate DB is outside the repo root, and the executor-network advisory
  - before `cure setup --agent codex` was run in the uv-tool smoke temp-root, `doctor` reported `fail=3` as expected — setup is the documented bootstrap gate, not a regression
- Rollback / hotfix decision:
  - no rollback needed
  - treat `v0.3.5` on PyPI and the matching GitHub Release as the closed release
- Exact next operator action:
  - none — release handoff complete; resume normal work on `main`
