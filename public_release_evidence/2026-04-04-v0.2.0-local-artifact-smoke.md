# 2026-04-04 Local Artifact Smoke (`v0.2.0`)

- Status: success
- Version / tag: `0.2.0` / `v0.2.0`
- Target: local artifact smoke from the release tree before tag push
- Commands run:
  - `python -m unittest /workspaces/cure_workspace/projects/CURe/tests/test_release_workflow_unittest.py`
  - `python -m unittest discover -s /workspaces/cure_workspace/projects/CURe/tests -p 'test_*.py'`
  - `uv build --out-dir /workspaces/cure_workspace/projects/CURe/dist-public-proveout --clear`
  - `python -m venv /tmp/cure-v020-smoke-PY7KWC/venv`
  - `/tmp/cure-v020-smoke-PY7KWC/venv/bin/python -m pip install /workspaces/cure_workspace/projects/CURe/dist-public-proveout/cureview-0.2.0-py3-none-any.whl`
  - `/tmp/cure-v020-smoke-PY7KWC/venv/bin/cure --help`
  - verified absent: `/tmp/cure-v020-smoke-PY7KWC/venv/bin/reviewflow`
  - verified import failure: `/tmp/cure-v020-smoke-PY7KWC/venv/bin/python -c 'import reviewflow'`
  - `HOME=/tmp/cure-v020-smoke-PY7KWC/home XDG_CONFIG_HOME=/tmp/cure-v020-smoke-PY7KWC/config XDG_STATE_HOME=/tmp/cure-v020-smoke-PY7KWC/state XDG_CACHE_HOME=/tmp/cure-v020-smoke-PY7KWC/cache OPENAI_API_KEY=dummy-openai-key /tmp/cure-v020-smoke-PY7KWC/venv/bin/cure setup --agent codex`
  - `HOME=/tmp/cure-v020-smoke-PY7KWC/home XDG_CONFIG_HOME=/tmp/cure-v020-smoke-PY7KWC/config XDG_STATE_HOME=/tmp/cure-v020-smoke-PY7KWC/state XDG_CACHE_HOME=/tmp/cure-v020-smoke-PY7KWC/cache OPENAI_API_KEY=dummy-openai-key /tmp/cure-v020-smoke-PY7KWC/venv/bin/cure install --agent codex`
  - `HOME=/tmp/cure-v020-smoke-PY7KWC/home XDG_CONFIG_HOME=/tmp/cure-v020-smoke-PY7KWC/config XDG_STATE_HOME=/tmp/cure-v020-smoke-PY7KWC/state XDG_CACHE_HOME=/tmp/cure-v020-smoke-PY7KWC/cache OPENAI_API_KEY=dummy-openai-key /tmp/cure-v020-smoke-PY7KWC/venv/bin/cure doctor --pr-url https://github.com/chunkhound/chunkhound/pull/220 --json`
- Verified public command surface:
  - built wheel installed the `cure` executable successfully
  - no deprecated `reviewflow` executable was installed
  - `import reviewflow` failed as expected with `ModuleNotFoundError: No module named 'reviewflow'`
  - canonical `cure setup --agent codex` succeeded in a clean temp-root layout
  - `cure install --agent codex` succeeded and reused existing `chunkhound` on `PATH`
  - `cure doctor --pr-url https://github.com/chunkhound/chunkhound/pull/220 --json` completed with summary `fail=0`, `ok=13`, `warn=5`
  - `agent_selection` in the smoke doctor payload resolved cleanly to saved `codex-cli` with status `ready`
- What worked:
  - release workflow/unit coverage passed after bumping the package version to `0.2.0`
  - the exact GitHub workflow gate `python -m unittest discover -s tests -p 'test_*.py'` passed locally on the release tree
  - `uv build --clear` produced `cureview-0.2.0.tar.gz` and `cureview-0.2.0-py3-none-any.whl`
  - installed-wheel smoke preserved the public `cure` command surface exactly as intended
  - package-first bootstrap remained deterministic when setup/install were pinned with `--agent codex`
  - public GitHub fallback worked without `gh auth`, so `doctor` still completed on a public PR target
- Blockers / risks:
  - `doctor` reported warnings only, not failures
  - warning sources were expected for this smoke layout: missing Jira config, on-demand cache/sandbox creation, executor-network advisory, and repo-local ChunkHound hint detection skipped because the smoke cwd was not a git worktree
  - PyPI/TestPyPI publish and GitHub Release asset upload still depend on the tag-triggered GitHub workflow
- Rollback / hotfix decision:
  - no rollback needed before tag push
  - proceed with the `v0.2.0` minor-release tag from `main`
- Exact next operator action:
  - push the release commit and tag `v0.2.0`, then watch `Publish Package` for the PyPI publish and standalone asset upload results
