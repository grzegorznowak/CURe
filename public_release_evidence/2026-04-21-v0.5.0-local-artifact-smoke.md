# v0.5.0 Local Artifact Smoke

- **Status**: PASS
- **Version / tag**: 0.5.0 / v0.5.0
- **Target**: local artifact smoke (pre-publish gate)

## Commands run

```bash
python -m unittest discover -s tests -p 'test_release_workflow_unittest.py'
# 12 tests OK

python -m unittest discover -s tests -p 'test_*.py'
# 612 tests OK (19.7s)

ruff check tests/test_release_workflow_unittest.py
# All checks passed

mypy
# Success: no issues found in 8 source files

uv build --out-dir dist-public-proveout --clear
# cureview-0.5.0.tar.gz + cureview-0.5.0-py3-none-any.whl

uvx twine check dist-public-proveout/*
# PASSED (both sdist and wheel)

# Isolated wheel smoke in <temp-root>/venv:
pip install dist-public-proveout/cureview-0.5.0-py3-none-any.whl
cure --help                          # OK
reviewflow executable                # PASS: not present
python -c 'import reviewflow'        # PASS: ModuleNotFoundError
cure setup --agent claude            # OK
cure doctor --pr-url https://github.com/chunkhound/chunkhound/pull/220 --json
# 15 ok, 4 warn, 0 fail
```

## Changelog entry shipped

### Added

- Verbose findings mode (`--wtf on`): opt-in enriched review output that adds Severity/Impact, Likelihood, Why, Assumptions/Preconditions, Downgrade Factors, Code Trail, and Reproduction Story to each final finding. Default review output is unchanged when the flag is omitted or off.

### Changed

- Release process now explicitly requires the GitHub Release body to mirror the curated `CHANGELOG.md` entry, keeping the Releases page in sync with the repo changelog.

## Verified public command surface

- `cure` (via `cure --help`)

## What worked

- All 612 unit tests pass
- Static analysis clean (ruff, mypy)
- Wheel and sdist build and pass twine check
- Installed wheel provides `cure` entry point, no `reviewflow` leakage
- `cure setup`, `cure doctor` both functional in clean environment

## Blockers / risks

None.

## Rollback / hotfix decision

N/A — no blocker.

## Exact next operator action

Commit, tag v0.5.0, and push to origin/main.
