# v0.4.0 PyPI Publish + Post-Publish Smoke

- **Status**: PASS
- **Version / tag**: 0.4.0 / v0.4.0
- **Target**: PyPI publish + post-publish public-package smoke

## Publish Package workflow

- **URL**: https://github.com/grzegorznowak/CURe/actions/runs/24658459281
- **Result**: success (all 6 jobs)
  - Build and verify release artifacts: success
  - Publish to PyPI: success
  - Build standalone asset (linux-x86_64): success
  - Build standalone asset (macos-arm64): success
  - Build standalone asset (macos-x86_64): success
  - Publish GitHub Release assets: success

## GitHub Release

- Tag: v0.4.0
- Assets: `cureview-v0.4.0-linux-x86_64.tar.gz`, `cureview-v0.4.0-macos-arm64.tar.gz`, `cureview-v0.4.0-macos-x86_64.tar.gz`, `SHA256SUMS`

## Commands run

```bash
uvx --from cureview cure --help
# OK — full subcommand list displayed

uvx --from cureview cure setup --agent claude
# OK — config + chunkhound-base.json written

uv tool install cureview
# cureview==0.4.0 installed, executable: cure

cure doctor --pr-url https://github.com/chunkhound/chunkhound/pull/220 --json
# summary: 15 ok, 4 warn, 0 fail
```

## Changelog entry shipped

### Added

- Citation contract for review sources: follow-up and synthesis reviews now emit structured citations that trace each finding back to the originating diff chunk or prior-step artifact, via the new `cure_citations` module.

### Changed

- Multipass review phases renamed for clarity; step prompts tightened to enforce grounding against the actual diff rather than paraphrased summaries.
- Synthesis retries now preserve intermediate artifacts and cap UI retry attempts, with clearer operator feedback on retry state and effort.

### Fixed

- Synthesis finalization no longer drops findings when mixing cited and uncited sources in the same review pass.
- Resume flow correctly re-enters synthesis when the prior run was synth-only, instead of skipping to the next step.
- Omission rewrite footer and grounding rejection messaging aligned across all review modes (local, zip, follow-up).

## Verified public command surface

- `cure` (via `uvx --from cureview cure --help` and `uv tool install cureview`)

## What worked

- PyPI publish via Trusted Publishing (OIDC)
- All 3 standalone assets + SHA256SUMS uploaded to GitHub Release
- `uvx --from cureview cure` installs and runs correctly
- `uv tool install cureview` upgrades from 0.3.9 to 0.4.0
- `cure setup` and `cure doctor` functional from public package

## Blockers / risks

None.

## Rollback / hotfix decision

N/A — no blocker.

## Exact next operator action

Commit evidence file to main. Release v0.4.0 is complete.
