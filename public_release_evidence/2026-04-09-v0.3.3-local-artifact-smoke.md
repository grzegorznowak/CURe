# 2026-04-09 Local Artifact Smoke (`v0.3.3`)

- Status: success
- Version / tag: `0.3.3` / `v0.3.3`
- Target: local artifact smoke from the release tree before tag push
- Commands run:
  - `python -m unittest /workspaces/cure_workspace/projects/CURe/tests/test_release_workflow_unittest.py`
  - `python -m unittest discover -s /workspaces/cure_workspace/projects/CURe/tests -p 'test_*.py'`
  - `ruff check /workspaces/cure_workspace/projects/CURe/tests/test_release_workflow_unittest.py /workspaces/cure_workspace/projects/CURe/tests/_reviewflow_unittest_runtime_ui_impl.py`
  - `mypy`
  - `uv build --out-dir /workspaces/cure_workspace/projects/CURe/dist-public-proveout --clear`
  - `uvx twine check /workspaces/cure_workspace/projects/CURe/dist-public-proveout/*`
  - `python -m venv /tmp/cure-v033-smoke-WnacV8/venv`
  - `/tmp/cure-v033-smoke-WnacV8/venv/bin/python -m pip install /workspaces/cure_workspace/projects/CURe/dist-public-proveout/cureview-0.3.3-py3-none-any.whl`
  - `/tmp/cure-v033-smoke-WnacV8/venv/bin/cure --help`
  - verified absent: `/tmp/cure-v033-smoke-WnacV8/venv/bin/reviewflow`
  - verified import failure: `/tmp/cure-v033-smoke-WnacV8/venv/bin/python -c 'import reviewflow'`
  - `HOME=/tmp/cure-v033-smoke-WnacV8/home XDG_CONFIG_HOME=/tmp/cure-v033-smoke-WnacV8/config XDG_STATE_HOME=/tmp/cure-v033-smoke-WnacV8/state XDG_CACHE_HOME=/tmp/cure-v033-smoke-WnacV8/cache OPENAI_API_KEY=dummy-openai-key /tmp/cure-v033-smoke-WnacV8/venv/bin/cure setup --agent codex`
  - `HOME=/tmp/cure-v033-smoke-WnacV8/home XDG_CONFIG_HOME=/tmp/cure-v033-smoke-WnacV8/config XDG_STATE_HOME=/tmp/cure-v033-smoke-WnacV8/state XDG_CACHE_HOME=/tmp/cure-v033-smoke-WnacV8/cache OPENAI_API_KEY=dummy-openai-key /tmp/cure-v033-smoke-WnacV8/venv/bin/cure install --agent codex`
  - `HOME=/tmp/cure-v033-smoke-WnacV8/home XDG_CONFIG_HOME=/tmp/cure-v033-smoke-WnacV8/config XDG_STATE_HOME=/tmp/cure-v033-smoke-WnacV8/state XDG_CACHE_HOME=/tmp/cure-v033-smoke-WnacV8/cache OPENAI_API_KEY=dummy-openai-key /tmp/cure-v033-smoke-WnacV8/venv/bin/cure doctor --pr-url https://github.com/chunkhound/chunkhound/pull/220 --json`
- Verified public command surface:
  - built wheel installed the `cure` executable successfully
  - no deprecated `reviewflow` executable was installed
  - `import reviewflow` failed as expected with `ModuleNotFoundError: No module named 'reviewflow'`
  - canonical `cure setup --agent codex` succeeded in a clean temp-root layout
  - `cure install --agent codex` succeeded and reused existing `chunkhound` on `PATH`
  - `cure doctor --pr-url https://github.com/chunkhound/chunkhound/pull/220 --json` completed with summary `fail=0`, `ok=13`, `warn=5`
  - `agent-selection` resolved cleanly to saved `codex-cli` with status `ready`
- What worked:
  - release workflow/unit coverage passed after bumping the package version to `0.3.3`
  - the full `python -m unittest discover -s tests -p 'test_*.py'` gate passed after aligning the stale doctor-help test with the intentionally hidden deprecated `--llm-plan-effort` flag
  - `ruff check` for the touched release workflow and runtime UI tests passed cleanly
  - configured `mypy` passed on the in-repo baseline set
  - `uv build --clear` produced `cureview-0.3.3.tar.gz` and `cureview-0.3.3-py3-none-any.whl`
  - `uvx twine check` passed for both the wheel and source distribution
  - installed-wheel smoke preserved the public `cure` command surface exactly as intended
  - package-first bootstrap remained deterministic when setup/install were pinned with `--agent codex`
  - public GitHub fallback worked without `gh auth`, so `doctor` still completed on a public PR target
- Blockers / risks:
  - `doctor` reported warnings only, not failures
  - warning sources were expected for this smoke layout: missing Jira config, repo-local ChunkHound hint incompatible because the candidate DB is outside the repo root, and the executor-network advisory
  - PyPI publish and GitHub Release standalone asset upload still depend on the tag-triggered GitHub workflow
- Rollback / hotfix decision:
  - no rollback needed before tag push
  - proceed with the `v0.3.3` bugfix-release tag from `main`
- Exact next operator action:
  - commit the release prep, push `main`, tag `v0.3.3`, and watch `Publish Package` for the PyPI publish and standalone asset upload results
