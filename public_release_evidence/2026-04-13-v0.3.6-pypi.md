# 2026-04-13 PyPI Publish + Post-Publish Smoke (`v0.3.6`)

- Status: success
- Version / tag: `0.3.6` / `v0.3.6`
- Target: post-publish public-package smoke against `cureview==0.3.6` on PyPI plus GitHub Release asset verification
- Commands run:
  - `gh run watch 24324634165 --exit-status`
  - `gh run view 24324634165 --json status,conclusion,url`
  - `gh release view v0.3.6 --json tagName,isDraft,isPrerelease,url,assets`
  - `uvx --refresh --from cureview==0.3.6 cure --help`
  - `HOME=<temp-root>/home XDG_CONFIG_HOME=<temp-root>/home/config XDG_STATE_HOME=<temp-root>/home/state XDG_CACHE_HOME=<temp-root>/home/cache OPENAI_API_KEY=dummy-openai-key uvx --from cureview==0.3.6 cure setup --agent codex`
  - `UV_TOOL_DIR=<temp-root>/uv-tools UV_TOOL_BIN_DIR=<temp-root>/uv-tool-bin HOME=<temp-root>/home uv tool install --force cureview==0.3.6`
  - `HOME=<temp-root>/home XDG_CONFIG_HOME=<temp-root>/home/config XDG_STATE_HOME=<temp-root>/home/state XDG_CACHE_HOME=<temp-root>/home/cache OPENAI_API_KEY=dummy-openai-key <temp-root>/uv-tool-bin/cure setup --agent codex`
  - `HOME=<temp-root>/home XDG_CONFIG_HOME=<temp-root>/home/config XDG_STATE_HOME=<temp-root>/home/state XDG_CACHE_HOME=<temp-root>/home/cache OPENAI_API_KEY=dummy-openai-key <temp-root>/uv-tool-bin/cure doctor --pr-url https://github.com/chunkhound/chunkhound/pull/220 --json`
- Changelog entry shipped:
  - `### Fixed`
  - `- cure pr no longer executes the reviewed repo's test suite on its own initiative. All review prompt templates now forbid running pytest, npm test, go test, cargo test, build scripts, linters, or formatters, and instruct the model to review test coverage and quality statically and to rely on gh pr checks $PR_URL (plus gh run view) for pass/fail status. This keeps cure pr focused on review intelligence instead of duplicating CI work and burning the codex_plan turn budget on flaky local runs.`
- Verified public command surface:
  - `Publish Package` workflow run `24324634165` completed with conclusion `success` (https://github.com/grzegorznowak/CURe/actions/runs/24324634165)
  - GitHub Release `v0.3.6` is visible (non-draft, non-prerelease) at https://github.com/grzegorznowak/CURe/releases/tag/v0.3.6 with the four expected assets: `cureview-v0.3.6-linux-x86_64.tar.gz`, `cureview-v0.3.6-macos-arm64.tar.gz`, `cureview-v0.3.6-macos-x86_64.tar.gz`, and `SHA256SUMS`
  - `uvx --from cureview==0.3.6 cure --help` fetched the published wheel and exited 0 (first attempt hit a transient PyPI index cache lag that reported "No version of cureview==0.3.6"; immediate retry succeeded with no code change)
  - `uvx --from cureview==0.3.6 cure setup --agent codex` succeeded in a clean temp-root layout and persisted `codex-cli`
  - `uv tool install cureview==0.3.6` into an isolated `UV_TOOL_DIR`/`UV_TOOL_BIN_DIR` installed the `cure` executable
  - `cure doctor --pr-url https://github.com/chunkhound/chunkhound/pull/220 --json` via the uv-tool-installed binary completed with summary `fail=0`, `ok=13`, `warn=5` after running `cure setup --agent codex` in the same temp-root
  - anonymous public GitHub PR metadata fallback worked without `gh auth login`
- What worked:
  - `Publish Package` workflow built + published the wheel and sdist to PyPI and uploaded all three standalone archives plus `SHA256SUMS` to the GitHub Release
  - post-publish `uv tool install cureview==0.3.6` flow produced a working `cure` binary identical in surface to the local wheel smoke
  - `cure doctor` reported the same `fail=0`, `ok=13`, `warn=5` shape as the local artifact smoke once `cure setup --agent codex` had been run in the temp-root
- Blockers / risks:
  - `doctor` reported warnings only, not failures
  - warning sources were expected for this smoke layout: missing Jira config, repo-local ChunkHound hint incompatible because the candidate DB is outside the repo root, and the executor-network advisory
  - first `uvx --refresh --from cureview==0.3.6 cure --help` invocation hit a transient PyPI index cache lag; an immediate retry with the same command succeeded, and both `uvx cure setup` and `uv tool install` worked on the first try, so no publish-blocker condition was observed
- Rollback / hotfix decision:
  - no rollback needed
  - treat `v0.3.6` on PyPI and the matching GitHub Release as the closed release
- Exact next operator action:
  - none — release handoff complete; resume normal work on `main`
