# 2026-03-19 PyPI Trusted Publishing Failure (`v0.1.2`)

- Status: blocked
- Version / tag: `0.1.2` / `v0.1.2`
- Target: first real PyPI publish through `.github/workflows/publish-package.yml`
- GitHub run:
  - Publish Package: `https://github.com/grzegorznowak/CURe/actions/runs/23284804281`
  - CI: `https://github.com/grzegorznowak/CURe/actions/runs/23284804262`
- What worked:
  - `main` was pushed with the public package workflow and docs/runtime/test changes
  - the `Publish Package` workflow validated tag/package metadata
  - workflow unit tests passed on GitHub Actions for `v0.1.2`
  - distribution build, `twine check`, and installed-wheel smoke all passed on GitHub Actions
  - the publish job reached the PyPI Trusted Publishing exchange step
- What failed:
  - the `Publish to PyPI` job failed during `pypa/gh-action-pypi-publish@release/v1`
  - PyPI returned:
    - `Trusted publishing exchange failure`
    - `invalid-publisher: valid token, but no corresponding publisher (Publisher with matching claims was not found)`
- Trusted publishing claims reported by PyPI:
  - `repository`: `grzegorznowak/CURe`
  - `workflow_ref`: `grzegorznowak/CURe/.github/workflows/publish-package.yml@refs/tags/v0.1.2`
  - `environment`: `pypi`
  - `ref`: `refs/tags/v0.1.2`
- Notes:
  - `v0.1.0` and `v0.1.1` were earlier failed release tags; `v0.1.2` is the first tag where the GitHub workflow itself made it cleanly to the PyPI publish step
  - TestPyPI was skipped because `v0.1.2` is a final release tag, so the workflow correctly targeted PyPI
- Blockers / risks:
  - the PyPI project does not currently have a Trusted Publisher record that matches this repository/workflow/environment claim set
  - until that publisher is registered on PyPI, any new final tag will fail at the same OIDC exchange step
  - public install smoke (`uvx --from cureview ...`, `uv tool install cureview`) cannot run until a publish succeeds
- Rollback / hotfix decision:
  - no rollback was performed because no files were published to PyPI
  - do not reuse or retag `v0.1.2`; once PyPI Trusted Publishing is configured, cut the next release from a new version/tag
- Exact next operator action:
  - in PyPI, create or update the Trusted Publisher for `grzegorznowak/CURe` so it accepts `.github/workflows/publish-package.yml` with environment `pypi`, then cut the next version/tag and rerun the publish prove-out
