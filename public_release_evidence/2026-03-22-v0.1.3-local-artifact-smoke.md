# 2026-03-22 Local Artifact Smoke (`v0.1.3`)

- Status: success
- Version / tag: `0.1.3` / `v0.1.3`
- Target: local artifact smoke from the release tree before tag push
- Commands run:
  - `python -m unittest discover -s /workspaces/cure_workspace/projects/CURe/tests -p 'test_*.py'`
  - `uv build --out-dir /workspaces/cure_workspace/projects/CURe/dist-public-proveout`
  - `python -m venv /tmp/cure-v013-smoke-oEAfLQ/venv`
  - `/tmp/cure-v013-smoke-oEAfLQ/venv/bin/python -m pip install /workspaces/cure_workspace/projects/CURe/dist-public-proveout/cureview-0.1.3-py3-none-any.whl`
  - `/tmp/cure-v013-smoke-oEAfLQ/venv/bin/cure --help`
  - verified absent: `/tmp/cure-v013-smoke-oEAfLQ/venv/bin/reviewflow`
  - verified import failure: `/tmp/cure-v013-smoke-oEAfLQ/venv/bin/python -c 'import reviewflow'`
  - `HOME=/tmp/cure-v013-smoke-oEAfLQ/home XDG_CONFIG_HOME=/tmp/cure-v013-smoke-oEAfLQ/config XDG_STATE_HOME=/tmp/cure-v013-smoke-oEAfLQ/state XDG_CACHE_HOME=/tmp/cure-v013-smoke-oEAfLQ/cache OPENAI_API_KEY=dummy-openai-key /tmp/cure-v013-smoke-oEAfLQ/venv/bin/cure init`
  - `HOME=/tmp/cure-v013-smoke-oEAfLQ/home XDG_CONFIG_HOME=/tmp/cure-v013-smoke-oEAfLQ/config XDG_STATE_HOME=/tmp/cure-v013-smoke-oEAfLQ/state XDG_CACHE_HOME=/tmp/cure-v013-smoke-oEAfLQ/cache OPENAI_API_KEY=dummy-openai-key /tmp/cure-v013-smoke-oEAfLQ/venv/bin/cure install`
  - `HOME=/tmp/cure-v013-smoke-oEAfLQ/home XDG_CONFIG_HOME=/tmp/cure-v013-smoke-oEAfLQ/config XDG_STATE_HOME=/tmp/cure-v013-smoke-oEAfLQ/state XDG_CACHE_HOME=/tmp/cure-v013-smoke-oEAfLQ/cache OPENAI_API_KEY=dummy-openai-key /tmp/cure-v013-smoke-oEAfLQ/venv/bin/cure doctor --pr-url https://github.com/chunkhound/chunkhound/pull/220 --json`
- Verified public command surface:
  - built wheel installed the `cure` executable successfully
  - no deprecated `reviewflow` executable was installed
  - `import reviewflow` failed as expected from the installed artifact
  - `cure init` succeeded in a clean temp-root layout
  - `cure install` succeeded and reused existing `chunkhound` on `PATH`
  - `cure doctor --pr-url https://github.com/chunkhound/chunkhound/pull/220 --json` completed with `fail=0`
- What worked:
  - full unit suite passed locally before the release tag cut
  - `uv build` produced `cureview-0.1.3.tar.gz` and `cureview-0.1.3-py3-none-any.whl`
  - installed-wheel smoke preserved the public `cure` command surface exactly as intended
  - public GitHub fallback worked without `gh auth`, so `doctor` still completed on a public PR target
- Blockers / risks:
  - `doctor` reported warnings only, not failures
  - warning sources were expected for this smoke layout: missing Jira config, on-demand cache/sandbox creation, executor-network advisory, and an incompatible repo-local ChunkHound hint from the release repo checkout
  - PyPI/TestPyPI publish and GitHub Release asset upload were not exercised locally; they depend on the tag-triggered GitHub workflow
- Rollback / hotfix decision:
  - no rollback needed before tag push
  - proceed with the `v0.1.3` tag and let `.github/workflows/publish-package.yml` handle publication
- Exact next operator action:
  - push commit + tag `v0.1.3`, then watch `Publish Package` for the final PyPI publish and standalone asset upload results
