# 2026-03-19 Local Artifact Smoke

- Status: partial
- Version / tag: `0.1.0` / local artifact prove-out from the workspace tree
- Target: local artifact smoke before the first tag-driven publish
- Commands run:
  - `uv build --out-dir /tmp/cure-public-proveout-tm2DEn/dist --clear`
  - `python -m venv /tmp/cure-public-proveout-tm2DEn/venv`
  - `/tmp/cure-public-proveout-tm2DEn/venv/bin/python -m pip install --upgrade pip /tmp/cure-public-proveout-tm2DEn/dist/cureview-0.1.0-py3-none-any.whl`
  - `XDG_CONFIG_HOME=/tmp/cure-public-proveout-tm2DEn/xdg/config XDG_STATE_HOME=/tmp/cure-public-proveout-tm2DEn/xdg/state XDG_CACHE_HOME=/tmp/cure-public-proveout-tm2DEn/xdg/cache OPENAI_API_KEY=dummy-openai-key /tmp/cure-public-proveout-tm2DEn/venv/bin/cure init`
  - `PATH=/tmp/cure-public-proveout-tm2DEn/venv/bin:$PATH ... /tmp/cure-public-proveout-tm2DEn/venv/bin/cure install`
  - `PATH=/tmp/cure-public-proveout-tm2DEn/venv/bin:$PATH ... /tmp/cure-public-proveout-tm2DEn/venv/bin/cure commands --json`
  - `PATH=/tmp/cure-public-proveout-tm2DEn/venv/bin:$PATH ... /tmp/cure-public-proveout-tm2DEn/venv/bin/cure doctor --pr-url https://github.com/chunkhound/chunkhound/pull/220 --json`
- Verified public command surface:
  - installed executable: `cure`
  - verified absent: `/tmp/cure-public-proveout-tm2DEn/venv/bin/reviewflow`
- What worked:
  - `uv build` produced `cureview-0.1.0.tar.gz` and `cureview-0.1.0-py3-none-any.whl`
  - the wheel installed cleanly into a fresh virtual environment
  - `cure --help` worked from the installed wheel
  - `cure init` wrote `/tmp/cure-public-proveout-tm2DEn/xdg/config/cure/cure.toml`
  - `cure init` wrote `/tmp/cure-public-proveout-tm2DEn/xdg/config/cure/chunkhound-base.json` with OpenAI embedding defaults when `OPENAI_API_KEY` was present
  - `cure install` completed and made `chunkhound` available at `/tmp/cure-public-proveout-tm2DEn/venv/bin/chunkhound`
  - `cure doctor --pr-url https://github.com/chunkhound/chunkhound/pull/220 --json` exited `0` with summary `ok=11 warn=3 fail=0`
  - public GitHub PR access worked without `gh auth login`; the doctor payload reported anonymous public fallback confirmed for `github.com`
- Blockers / risks:
  - this session did not push a `v0.1.0*` tag or execute `publish-package.yml`, so there is still no TestPyPI or PyPI publication evidence
  - the published-package smoke (`uvx --from cureview ...`, `uv tool install cureview`) is still pending the first successful index publish
  - the doctor run still reports expected warnings for on-demand sandbox/cache directory creation and missing local Jira config
- Rollback / hotfix decision:
  - no rollback action taken because the prove-out stopped before any public index publication
  - if the first tag-driven publish fails, treat this file as the pre-publish baseline and record the failure in a new dated evidence log instead of editing this one in place
- Exact next operator action:
  - push the first matching release tag through `publish-package.yml`, then record the resulting TestPyPI or PyPI package URL plus `uvx --from cureview cure ...` smoke results in a new `public_release_evidence/<date>-v<version>-<target>.md` file
