# 2026-04-13 Local Artifact Smoke (`v0.3.5`)

- Status: success
- Version / tag: `0.3.5` / `v0.3.5`
- Target: local artifact smoke from the release tree before tag push
- Commands run:
  - `python -m unittest discover -s tests -p 'test_release_workflow_unittest.py'`
  - `python -m unittest discover -s tests -p 'test_*.py'`
  - `ruff check tests/test_release_workflow_unittest.py`
  - `ruff check cure_llm.py tests/_reviewflow_unittest_runtime_ui_impl.py`
  - `mypy`
  - `uv build --out-dir dist-public-proveout --clear`
  - `uvx twine check dist-public-proveout/cureview-0.3.5.tar.gz dist-public-proveout/cureview-0.3.5-py3-none-any.whl`
  - `python -m venv <temp-root>/venv`
  - `<temp-root>/venv/bin/pip install dist-public-proveout/cureview-0.3.5-py3-none-any.whl`
  - `<temp-root>/venv/bin/cure --help`
  - verified absent: `<temp-root>/venv/bin/reviewflow`
  - verified import failure: `<temp-root>/venv/bin/python -c 'import reviewflow'`
  - `HOME=<temp-root>/home XDG_CONFIG_HOME=<temp-root>/home/config XDG_STATE_HOME=<temp-root>/home/state XDG_CACHE_HOME=<temp-root>/home/cache OPENAI_API_KEY=dummy-openai-key <temp-root>/venv/bin/cure setup --agent codex`
  - `HOME=<temp-root>/home XDG_CONFIG_HOME=<temp-root>/home/config XDG_STATE_HOME=<temp-root>/home/state XDG_CACHE_HOME=<temp-root>/home/cache OPENAI_API_KEY=dummy-openai-key <temp-root>/venv/bin/cure doctor --pr-url https://github.com/chunkhound/chunkhound/pull/220 --json`
- Changelog entry shipped:
  - `### Fixed`
  - `- cure pr with the Codex provider no longer aborts the review-intelligence gate with "missing successful code_research" on non-trivial repos. The staged cure-chunkhound helper now emits its first tools/call waiting heartbeat immediately (rather than after 10s) and repeats every 5s, so codex-cli sees a visibly live output stream during the 2–5 min LLM synthesis step of research and no longer surfaces the call to the model as a hang. The prompt templates used by cure pr also now explicitly tell the model that research legitimately takes 2–5 minutes per call on large repos, that the heartbeat lines are normal progress rather than a hang, and that it must issue one research invocation at a time and wait for its final JSON object before issuing another.`
- Verified public command surface:
  - built wheel installed the `cure` executable successfully
  - no deprecated `reviewflow` executable was installed
  - `import reviewflow` failed as expected with `ModuleNotFoundError: No module named 'reviewflow'`
  - `cure setup --agent codex` succeeded in a clean temp-root layout and persisted `codex-cli`
  - `cure doctor --pr-url https://github.com/chunkhound/chunkhound/pull/220 --json` completed with summary `fail=0`, `ok=13`, `warn=5`
  - anonymous public GitHub PR metadata fallback worked without `gh auth login`
- What worked:
  - release workflow contract tests passed (12/12)
  - full unit suite passed on the release tree (493/493)
  - `ruff check` passed on the release workflow test and on the touched files (`cure_llm.py`, `tests/_reviewflow_unittest_runtime_ui_impl.py`)
  - configured `mypy` passed on the in-repo baseline set (8 source files)
  - `uv build --clear` produced `cureview-0.3.5.tar.gz` and `cureview-0.3.5-py3-none-any.whl`
  - `uvx twine check` passed for both the wheel and source distribution
  - installed-wheel smoke preserved the public `cure` command surface exactly as intended
  - package-first bootstrap remained deterministic with `cure setup --agent codex`
- Blockers / risks:
  - `doctor` reported warnings only, not failures
  - warning sources were expected for this smoke layout: missing Jira config, repo-local ChunkHound hint incompatible because the candidate DB is outside the repo root, and the executor-network advisory
  - PyPI publish and GitHub Release standalone asset upload still depend on the tag-triggered GitHub workflow
- Rollback / hotfix decision:
  - no rollback needed before tag push
  - proceed with the `v0.3.5` patch-release tag from `main`
- Exact next operator action:
  - commit the release prep, push `main`, tag `v0.3.5`, and watch `Publish Package` for the PyPI publish and standalone asset upload results
