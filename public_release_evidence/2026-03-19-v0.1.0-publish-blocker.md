# 2026-03-19 Tag-Driven Publish Blocker

- Status: blocked
- Version / tag: intended first public `v0.1.0` publish prove-out
- Target: GitHub tag push plus `publish-package.yml` execution
- Commands run:
  - `gh auth status`
  - `gh repo view --json nameWithOwner,defaultBranchRef,isPrivate,url`
  - `gh workflow list`
  - `git -C /workspaces/cure_workspace/projects/CURe ls-remote --heads origin`
- What happened:
  - `gh auth status` failed with: `You are not logged into any GitHub hosts. Run gh auth login to authenticate.`
  - `gh repo view ...` failed with: `To get started with GitHub CLI, please run:  gh auth login`
  - `gh workflow list` failed with the same unauthenticated GitHub CLI error
  - `git ls-remote --heads origin` failed with:
    - `no such identity: /workspaces/academy+/projects/cure/.git/info/grzegorznowak_github_agent.pub: No such file or directory`
    - `git@github.com: Permission denied (publickey).`
    - `fatal: Could not read from remote repository.`
- Verified blocker:
  - this workspace cannot currently authenticate to GitHub via `gh`
  - this workspace cannot currently authenticate to `origin` over SSH because the configured identity path is missing
  - without one of those access paths, Story 05 cannot push the first matching release tag or observe the resulting `publish-package.yml` run
- Blockers / risks:
  - there is still no TestPyPI or PyPI publication evidence for `cureview`
  - the published-package smoke (`uvx --from cureview ...`, `uv tool install cureview`) cannot start until the tag-driven publish path is reachable
- Rollback / hotfix decision:
  - no rollback decision was taken because the publish did not start
  - once GitHub access is restored, rerun the release prove-out from the current package version/tag decision rather than editing this blocker log in place
- Exact next operator action:
  - restore one working GitHub publish path for this repo: either `gh auth login` / `GH_TOKEN`, or a valid SSH key/identity for `git@github.com:grzegorznowak/CURe.git`; then push the release commit and matching tag so `publish-package.yml` can run
