# 2026-04-16 PyPI Publish + Post-Publish Smoke (`v0.3.8`)

- Status: success
- Version / tag: `0.3.8` / `v0.3.8`
- Target: post-publish public-package smoke against `cureview==0.3.8` on PyPI plus GitHub Release asset verification
- Commands run:
  - `gh run watch 24496317497 --exit-status`
  - `gh release view v0.3.8 --json tagName,isDraft,isPrerelease,assets`
  - `uvx --refresh --from cureview==0.3.8 cure --help`
  - `UV_TOOL_DIR=<temp-root>/uv-tools UV_TOOL_BIN_DIR=<temp-root>/uv-tool-bin HOME=<temp-root>/home uv tool install --force cureview==0.3.8`
  - `HOME=<temp-root>/home XDG_CONFIG_HOME=<temp-root>/home/config XDG_STATE_HOME=<temp-root>/home/state XDG_CACHE_HOME=<temp-root>/home/cache OPENAI_API_KEY=dummy-openai-key <temp-root>/uv-tool-bin/cure setup --agent codex`
  - `HOME=<temp-root>/home XDG_CONFIG_HOME=<temp-root>/home/config XDG_STATE_HOME=<temp-root>/home/state XDG_CACHE_HOME=<temp-root>/home/cache OPENAI_API_KEY=dummy-openai-key <temp-root>/uv-tool-bin/cure doctor --pr-url https://github.com/chunkhound/chunkhound/pull/220 --json`
- Changelog entry shipped:
  - `### Fixed`
  - `- The hot-start ChunkHound base-cache prompt now pauses the active dashboard before waiting for operator input and resumes it afterward, so the TUI no longer appears stuck on "Base cache (agent is working)" while CURe is actually waiting for a hidden /dev/tty prompt.`
- Verified public command surface:
  - `Publish Package` workflow run `24496317497` completed with conclusion `success` (https://github.com/grzegorznowak/CURe/actions/runs/24496317497)
  - GitHub Release `v0.3.8` is visible (non-draft, non-prerelease) at https://github.com/grzegorznowak/CURe/releases/tag/v0.3.8 with the four expected assets: `cureview-v0.3.8-linux-x86_64.tar.gz`, `cureview-v0.3.8-macos-arm64.tar.gz`, `cureview-v0.3.8-macos-x86_64.tar.gz`, and `SHA256SUMS`
  - `uvx --from cureview==0.3.8 cure --help` hit a transient PyPI index cache lag on first attempt; immediate retry succeeded
  - `uv tool install cureview==0.3.8` into an isolated `UV_TOOL_DIR`/`UV_TOOL_BIN_DIR` installed the `cure` executable on first try
  - `cure setup --agent codex` succeeded in a clean temp-root layout and persisted `codex-cli`
  - `cure doctor --pr-url https://github.com/chunkhound/chunkhound/pull/220 --json` completed with summary `fail=0`, `ok=13`, `warn=5`
  - anonymous public GitHub PR metadata fallback worked without `gh auth login`
- What worked:
  - `Publish Package` workflow built + published the wheel and sdist to PyPI and uploaded all three standalone archives plus `SHA256SUMS` to the GitHub Release
  - `uv tool install cureview==0.3.8` flow produced a working `cure` binary identical in surface to the local wheel smoke
  - `cure doctor` reported `fail=0`, `ok=13`, `warn=5` matching the local artifact smoke
- Blockers / risks:
  - `doctor` reported warnings only, not failures
  - warning sources were expected for this smoke layout: missing Jira config, repo-local ChunkHound hint incompatible because the candidate DB is outside the repo root, and the executor-network advisory
  - transient PyPI index cache lag on first `uvx --refresh` invocation (resolved on immediate retry, same as observed in v0.3.6)
- Rollback / hotfix decision:
  - no rollback needed
  - treat `v0.3.8` on PyPI and the matching GitHub Release as the closed release
- Exact next operator action:
  - none — release handoff complete; resume normal work on `main`
