# Progress: story-04-review-runtime-integration-guardrails-memory-trace

> Runtime/progress artifact migrated from legacy `agent_coordination` during the 2026-06-12 OpenSpec-format rewrite.
> Canonical story contract: `story.md`. Legacy source remains under `/workspaces/cure_workspace/agent_coordination/epics/cure-subsequent-pr-review/`.

## Current Claim
- Claimed at: 2026-06-13T04:39:52Z
- Claimed by: pi child resume session
- Model: not exposed by pi harness (no `MODEL` environment variable available)
- Scope: Address latest PR #22 feedback FB-027/FB-028/FB-029: make final output lead with prior-review issue history and stable clusters/internal coverage, degrade malformed discussion-linker LLM output, and stabilize memory replay identity when ordinal group IDs shift.
- Worktrees:
  - CURe: `/home/vscode/add-worktrees/CURe-cure-subsequent-pr-review-story-04-review-runtime-integration-guardrails-memory-trace`
- Primary write surfaces: `cure_subsequent_review/runtime.py`, `cure_subsequent_review/discussion_linker.py`, `cure_subsequent_review/discussion_signals.py`, `cure_subsequent_review/memory_store.py`, `prompts/*`, related tests, OpenSpec progress/handoff.
- Status: 🔵 IN PR

### Legacy Scaffold Notes
> Story scaffolded by `/epic-story-plan` and refined through `/grillme` session. Stories 01-03 delivered intake, auto-mode, and semantic dispositions. This story connects them to runtime prompts, pre-review intelligence, shared PR memory, interactive degraded UX, and deterministic local landmark verification.

## Progress Timeline
- 2026-06-13T10:05:45Z **Live audit ingestion**: inspected latest PR #22 sandbox `/home/vscode/.local/state/cure/sandboxes/grzegorznowak-cure-pr22-20260613-080828-d739` at head `372b4a753099c4b6e077d98551da51039222a16b`. The run completed with final `review.md` and verdict REQUEST CHANGES. Story 04 closure gates from the previous feedback are now live-proven: strict multipass grounding accepts all five `review.step-*.md` artifacts with first-line `### Step Result:` and no invalid artifacts; A17 post-review governor runs warn-only and succeeds; A19/`DA-0006` is `out-of-scope` / `move_out_of_scope`, no longer `carried-forward/re_report`; FB-028 malformed-linker abort is resolved for the original failure shape; FB-029 is resolved only for the narrow different-`finding_ids` replay case. The audit also found follow-up hardening issues, including visible top-level `### Internal DA coverage` overexposing audit rows, broader memory/linker identity gaps, discussion authority escalation, source/path/citation boundaries, concise prior-review parsing, and planner-abort guardrails. These were initially scaffolded into a synthetic Story 05, then remapped on 2026-06-14 into Stories 01, 03, and 04 so OpenSpec ownership matches the pre-existing story surfaces actually under review.
  - Computed-vs-carried note: the five `### Prior Review Issue History` rows are prior issue identities carried from prior corpus/comments/sessions, but their latest statuses were computed in this run: 14 source-verification rows have `llm_finding_verifier` provenance and `DA-0006` uses the FB-026 official-footer policy override; no `memory_cache` source-verification provenance was found for those history statuses.
- 2026-06-13T08:04:40Z **Step**: fixed strict multipass prompt/schema regression surfaced by live PR #22 rerun sandbox `/home/vscode/.local/state/cure/sandboxes/grzegorznowak-cure-pr22-20260613-050345-4d51`.
  - Changed: added a prompt regression proving issue-history-first output guidance applies only to final review/synthesis templates, while schema-bound multipass plan/step/resume-step templates keep their required output headers such as `### Step Result:`. Reworded schema-bound prompt guidance so `$PRIOR_REVIEW_BRIEF` remains available as context without overriding strict grounding formats.
  - Test: PASS — `python -m pytest tests/test_reviewflow_prompts_unittest.py tests/test_subsequent_review.py -q`; `ruff check .`; `git diff --check`; `mypy`.
  - Notes: This is an implementation bug fix, not a story contract change; Story A16 already targets final `review.md`/synthesis output rather than intermediate multipass artifacts.
- 2026-06-13T04:47:11Z **Step**: implemented latest PR #22 feedback fixes FB-027/FB-028/FB-029 in the active Story 04 worktree.
  - Changed: strengthened `$PRIOR_REVIEW_BRIEF` prompt override so live output must start with `### Prior Review Issue History`; report-governor audit now degrades when issue history is not first or when stable issue clusters/statuses from the governor brief are missing; governor tests preserve the body-only PR-comments cluster as `carried-forward/re_report` while official-footer policy remains `out-of-scope`; malformed LLM discussion-linker output now records degraded discussion signals instead of aborting; memory replay now rejects same-ordinal group IDs when cached finding identity differs.
  - Test: PASS — red regressions failed before implementation; targeted suites, public wrapper, ruff, diff-check, and mypy passed.
  - Notes: Story status remains `🔵 IN PR`; local changes are not committed/pushed and need fresh implementation review before PR sync/live rerun.
- 2026-06-13T04:39:52Z **Resume**: picked up latest PR #22 request-changes feedback FB-027/FB-028/FB-029 while preserving Plan lane approval.
  Worktrees: CURe=/home/vscode/add-worktrees/CURe-cure-subsequent-pr-review-story-04-review-runtime-integration-guardrails-memory-trace
  Main-tree targets: none
  Claim: fix final output issue-history ordering/coverage, malformed LLM linker degradation, and memory replay identity stability.
- 2026-06-12T18:20:00Z Verified the A16 local implementation/proof anchors in the active Story 04 worktree, added explicit post-review sanitization prompt-guidance assertions for human issue history/internal DA coverage, and reran: `python -m pytest tests/_subsequent_review_unit_report_governor_unittest.py -q`; `python -m pytest tests/_subsequent_review_unit_runtime_packaging_unittest.py tests/test_reviewflow_prompts_unittest.py -q`; `python -m pytest tests/test_subsequent_review.py -q`; `python -m pytest tests/_subsequent_review_unit_source_truth_unittest.py tests/_subsequent_review_unit_disposition_arbiter_unittest.py tests/_subsequent_review_unit_report_governor_unittest.py -q`; `ruff check .`; `git diff --check`; `mypy`. Local A16 implementation/proof tasks are now checked. Follow-up sync: committed `fb78dca21a4ad5c0a21d6a889189fb38d25e7559` (`cure-subsequent-pr-review/04: add human issue history guardrails`), pushed it to PR #22 head branch `origin/cure-subsequent-pr-review/story-01-intake`, updated the PR body through the GitHub API after `gh pr edit` hit the Projects-classic GraphQL deprecation error, and confirmed `mergeStateStatus=CLEAN` with Detect Secrets success. Live PR #22 rerun/audit and final done transition remain unchecked/pending.
- 2026-06-12T18:10:00Z Resumed implementation in the active Story 04 worktree and locally implemented A16 human-readable final-output guardrails. `build_governor_brief()` now emits a cluster-first `### Prior Review Issue History (required final output)` section with titles/statuses/plain-English reasons and an audit-only `### Internal DA coverage` section; duplicate DA rows with the same issue title collapse into one reader-facing issue cluster. The post-review report-governor prompt/audit now treats raw-DA-list-only output, missing issue history, missing issue clusters, missing internal DA coverage, and contradictory internal DA status as degraded/warn-only evidence. Landmark golden was refreshed for the new headings. Verification: `python -m pytest tests/_subsequent_review_unit_report_governor_unittest.py -q`; `python -m pytest tests/_subsequent_review_unit_runtime_packaging_unittest.py tests/test_reviewflow_prompts_unittest.py -q`; `python -m pytest tests/test_subsequent_review.py -q`; `python -m pytest tests/_subsequent_review_unit_source_truth_unittest.py tests/_subsequent_review_unit_disposition_arbiter_unittest.py tests/_subsequent_review_unit_report_governor_unittest.py -q`; `ruff check .`; `git diff --check`; `mypy`. Product code changed in the active worktree but is not yet committed/pushed; PR #22 live rerun/audit remains pending.
- 2026-06-12T17:20:10Z Plan review approved after direct verification that OpenSpec and legacy active A17/report-governor awareness wording now align on human issue-cluster awareness plus internal `DA-*` coverage. Plan lane moved from 🟡 PLAN DRAFT to 🟢 PLAN APPROVED; status stays 🔵 IN PR. Remaining gates are implementation/output gates: A16 human-readable issue history and PR #22 `DA-0006` rerun/audit. No product source code changed.
- 2026-06-12T17:06:04Z Plan review requested changes: legacy story A17 (`agent_coordination/.../story-04-review-runtime-integration-guardrails-memory-trace.md:116`) conflicts with OpenSpec story A17 (`projects/CURe/openspec/changes/story-04-review-runtime-integration-guardrails-memory-trace/story.md:116`) by checking missing/partial raw `DA-*` awareness instead of human issue-cluster awareness plus internal DA coverage. MASTER/story/OpenSpec plan lanes moved to 🟠 PLAN CHANGES REQUESTED; status stays 🔵 IN PR. No product source code changed.
- 2026-06-12T16:59:21Z Resumed planning after the A16 OpenSpec output-model update. Synchronized legacy coordination story/progress/MASTER with the human-friendly final-output contract, recorded A16 as provisional pending prompt/report-governor implementation and live/output audit, and moved the plan lane back to 🟡 PLAN DRAFT for fresh plan review. No product code changed in this update.
- 2026-06-12T16:09:46Z Modeled the human-friendly final-output requirement into OpenSpec: A16 now requires a prior-review issue summary with issue titles, statuses, and plain-English reasons as the reader-facing surface; raw DA-number sets are internal/provenance-only. A16 proof maturity is provisional pending prompt/report-governor implementation and live/output audit. No product code changed in this update.
- 2026-06-12T14:32:33Z Committed locally approved A19/TAP-20 DA-0006 footer-policy fix as `99cd1ea` (`cure-subsequent-pr-review/04: finalize footer policy disposition`) and pushed it to the existing PR #22 head branch `origin/cure-subsequent-pr-review/story-01-intake`. Story moved from 🔄 IN PROGRESS to 🔵 IN PR for remote check/live-output tracking. Pre-commit focused check run in this session: `python -m pytest tests/_subsequent_review_unit_source_truth_unittest.py tests/_subsequent_review_unit_disposition_arbiter_unittest.py tests/_subsequent_review_unit_report_governor_unittest.py -q` → `18 passed, 15 subtests`; fresh story review had also passed public subsequent wrapper, decision/prior-corpus, ruff, diff-check, mypy, and exact live-input replay. `gh pr view 22` after push reports PR open at head `99cd1ea118ecd8cfc3738646e20095975ba7c8da`; 2026-06-12T14:34:28Z refresh reports `mergeStateStatus=CLEAN` and Detect Secrets success.
- 2026-06-12T14:30:00Z Resumed A19/TAP-20 and locally fixed the DA-0006 footer-policy blocker. Added red-first source-truth and disposition regressions for the live `G-0006`/`SV-0006`/`DA-0006` equivalent: official-footer acceptance without author/login authentication is classified as FB-026 policy-approved (`source_state=resolved_from_source`, provenance `policy_override=official_footer_marker_acceptance`) and the disposition arbiter moves it out of scope instead of `re_report`; generic/body-only CURe-looking text still goes through source verification and remains rejected by prior-corpus tests. Governor map proof now asserts `DA-0006: out-of-scope` and not `carried-forward/re_report`. Verification: red tests failed before implementation; focused source/disposition/governor suite `18 passed, 15 subtests`; public subsequent wrapper `112 passed, 29 subtests`; `ruff check .`; `git diff --check`; `mypy`. PR #22 live rerun/audit still required before moving Story 04 out of 🔄 IN PROGRESS.
- 2026-06-12T14:06:00Z Recorded latest PR #22 live-output audit from sandbox `/home/vscode/.local/state/cure/sandboxes/grzegorznowak-cure-pr22-20260612-132229-cfad`: verdict PARTIAL. A15-A18 pass in live artifacts, but A19/TAP-20 remains incomplete because the old footer-auth false-positive is still carried forward as `G-0006` / `SV-0006` / `DA-0006` (`source_state=still_open`, `action=re_report`, `Prior Review Disposition Map` status `carried-forward/re_report`) despite FB-026 footer-policy provenance being visible. Story moved from 🔵 IN PR back to 🔄 IN PROGRESS; PR #22 remains the canonical surface at head `da1c1ce6a625057c1dd4335a72b14ce67ba6df79`, and the next implementation must adjust semantic/disposition/governor classification so `DA-0006` is no longer carried forward under the official-footer policy.
- 2026-06-12T13:19:14Z Committed locally approved PR #22 follow-up implementation as `da1c1ce` (`cure-subsequent-pr-review/04: finalize review output follow-up`) and pushed it to the existing PR #22 head branch `origin/cure-subsequent-pr-review/story-01-intake`. Story moved from local ✅ DONE to 🔵 IN PR for remote review/check tracking. Verification before commit: focused follow-up suites `63 passed, 23 subtests`; public wrapper `109 passed, 29 subtests`; `ruff check .`; `git diff --check`; `mypy`. PR #22 is open at head `da1c1ce6a625057c1dd4335a72b14ce67ba6df79`; `gh pr view` refresh at 2026-06-12T13:20:50Z reported `mergeStateStatus=CLEAN` and Detect Secrets success.
- 2026-06-12T13:12:47Z Story review approved the PR #22 follow-up implementation. Rechecked A15-A19/TAP-16-TAP-20 with direct source/test inspection and local verification; the prior A18/TAP-19 blocker is resolved for the actual parenthesized multi-line `from cure_sessions import (...)` shape in `cure.py`. Actual probe against `cure.py:12953 stale duplicate _resolve_session_relative_path` returns `not_verifiable`, `inactive_source_reference_active_binding:_resolve_session_relative_path:cure_sessions.py`, and `llm_calls=0`. Verification: targeted A18 regression `1 passed`; focused follow-up suites `63 passed, 23 subtests`; public wrapper `109 passed, 29 subtests`; `ruff check .`; `git diff --check`; `mypy`. Story moved to done / ✅ DONE; next action is PR #22 synchronization/status refresh if desired.
- 2026-06-12T13:04:55Z Resumed after story review REQUEST CHANGES and fixed the A18/TAP-19 blocker. Added a red regression for the real parenthesized multi-line `from cure_sessions import (...)` stale duplicate shape, then taught the inactive binding detector to parse multi-line/parenthesized import lists and degrade inactive duplicate source refs without invoking the LLM. Actual probe against `cure.py:12953 stale duplicate _resolve_session_relative_path` now returns `not_verifiable`, `inactive_source_reference_active_binding:_resolve_session_relative_path:cure_sessions.py`, and `llm_calls=0`. Verification: red regression failed before fix; targeted verifier/source-truth `9 passed, 5 subtests`; focused follow-up suites `63 passed, 23 subtests`; public wrapper `109 passed, 29 subtests`; `ruff check .`; `git diff --check`; `mypy`. Story remains in progress pending review.
- 2026-06-12T12:59:16Z Story review requested changes for the PR #22 follow-up implementation. A18/TAP-19 blocker: the inactive duplicate-source detector only recognizes one-line `from module import name` statements, so it misses the actual multi-line `from cure_sessions import (...)` re-export in `cure.py`; a probe against `cure.py:12953` called the verifier LLM and returned `still_open` instead of degrading the stale duplicate reference. Focused follow-up suites, public wrapper, `ruff check .`, `git diff --check`, and `mypy` still pass, so tests need a real multi-line re-export regression. Story remains in progress.
- 2026-06-12T12:51:55Z Resumed Story 04 implementation for approved PR #22 follow-up A15-A19. Added red-first regressions and implementation for runtime package/context finalization, required Prior Review Disposition Map coverage/degraded report-governor checks, inactive duplicate source-reference degradation, and FB-026 footer-policy propagation through package/governor/audit prompts. Updated landmark golden for the new disposition-map heading. Verification: focused follow-up suites `62 passed, 23 subtests`; public subsequent wrapper `108 passed, 29 subtests`; `ruff check .`; `mypy`; `git diff --check`. Product code changed in historical Story 04 worktree; `docs/examples/subsequent-pr-run-flow.svg` untouched.
- 2026-06-12T12:35:12Z Resumed Story 04 planning after plan-review request changes. Changed A15-A19 Acceptance Proof Matrix proof maturity from `planned` to canonical `provisional` for pre-implementation follow-up proof targets, recorded the resume log, and moved the tracker back to 🟡 PLAN DRAFT / ⚪ TODO. Coordination-doc-only update; product code and `docs/examples/subsequent-pr-run-flow.svg` untouched.
- 2026-06-12T12:25:12Z Resumed Story 04 planning after plan-review request changes. Integrated A15-A19 into scenarios and the canonical Acceptance Proof Matrix, expanded TAP-16-TAP-20 into full canonical Test Architecture Plan rows, updated Implementation Notes with F1-F5/A15-A19 red-first steps, and moved the tracker back to 🟡 PLAN DRAFT / ⚪ TODO. Coordination-doc-only update; product code and `docs/examples/subsequent-pr-run-flow.svg` untouched.
- 2026-06-12T12:11:40Z Reopened Story 04 after PR #22 live-run review-output audit found runtime/final-output acceptance gaps. Plan lane reset to 🟡 PLAN DRAFT and implementation status reset to ⚪ TODO while the follow-up plan is drafted/reviewed. Added A15-A19/TAP-16-20 draft follow-up rows for package/status consistency, required prior-review disposition mapping, warn-only governor degradation, active source-binding verification, and FB-026 runtime policy propagation.
- 2026-06-11T18:04:02Z Corrected PR target per operator direction: pushed Story 04 commit `66efc87` to existing PR #22 head branch `origin/cure-subsequent-pr-review/story-01-intake`; PR #22 is now the canonical review surface. Duplicate PR #23 had already been merged into that branch and is not the active PR.
- 2026-06-11T17:34:50Z Reopened remote review from local `✅ DONE`; moved step to `🔵 IN PR`; implementation committed as `66efc87`. Initial staging created duplicate PR #23, later superseded by existing PR #22.
- 2026-06-11T16:57:39Z Story review approved. Rechecked previous blockers from source and regressions: A1-A14 proof rows are final; strict governor validation fails closed before `governor_brief.md` for missing/malformed citation ledgers or absent referenced rows while warn/off behave as specified; injected linker no-link results do not fall back to text linking; memory replay requires top-level `last_seen_head == current_head`. Verification passed: targeted blocker suites (11 passed), focused Story 04 suites (35 passed, 5 subtests), public/config/prompt wrappers (286 passed, 28 subtests), `ruff check .`, `git diff --check`, and `mypy`. Story moved to done / ✅ DONE.
- 2026-06-11T15:01:11Z Resumed after story review REQUEST CHANGES and fixed the two remaining blockers. A5/A3 strict governor: added red-first runtime-packaging regressions for missing `source_verification.json` and `discussion_signals.json`; `prepare_review_runtime_pre_prompt()` now validates strict-mode disposition citation dependencies before writing/rendering `governor_brief.md`, fails closed on missing/malformed citation ledgers or referenced rows, preserves warn-mode continuation and off-mode skip behavior, and PR-flow continues to surface the `ValueError` as `ReviewflowError`. Proof matrix: finalized A1-A14 rows with concrete source/test/check references and removed all `provisional` entries. Verification: strict regression failed before implementation and passes after; focused Story 04 suites pass (`35 passed, 5 subtests`), public wrapper/config/prompt suites pass (`286 passed, 28 subtests`), `ruff check .`, `git diff --check`, and `mypy` pass. Story remains in progress and is ready for story review/convergence.
- 2026-06-11T14:44:47Z Resumed after story review REQUEST CHANGES and fixed the two blocker regressions. A12/A13: `resolve_discussion_signals()` now honors an injected linker's explicit empty `group_ids` result instead of falling back to text linking, preventing low-confidence `group_id:null` linker results from suppressing source verification; added a no-link regression. A8: `ReviewMemoryStore.synthesize_resolved_source_row()` now requires top-level `last_seen_head == current_head` before consulting per-head entries, preventing stale resolved replay after a newer head is still-open; added a two-head regression. Red-first targeted tests failed before the fixes and pass after. Focused subsequent-review/linker/semantic/source/runtime-memory tests, public wrapper, config/prompt wrappers, `ruff check .`, `git diff --check`, and `mypy` all pass. Story remains in progress and is ready for story review/convergence.
- 2026-06-11T14:27:51Z Resumed for acceptance/readiness pass. Rechecked the Story 04 acceptance/TAP/proof matrix against the current progress notes: all major planned Story 04 seams are represented locally, including A12-A14/TAP-13-15 and optional A14 Variant-C enrichment. Made no product changes. Verification pass: public subsequent-review wrapper plus config/prompt wrappers (`282 passed, 28 subtests passed`), `ruff check .`, and `mypy` all pass. Story remains in progress and is ready for story review/convergence unless reviewers request additional proof.
- 2026-06-11T14:13:03Z Continued optional A14/TAP-14 Variant-C enrichment seam. `verify_source_truth()` now carries `pr_files_changed` plus linked discussion-signal row JSON into each `FindingVerificationRequest`; `run_semantic_pipeline()` / `run_subsequent_review_intake()` propagate the field; `_pr_flow_impl` records changed file paths from PR diff stats and passes them to subsequent-review intake; `LlmFindingVerifier` prompt coverage now asserts changed files and discussion signals are present. Red-first source-truth and PR-flow assertions failed before implementation and pass after. Focused source/verifier/semantic/control/PR-flow suites, landmark/control/PR-flow suite, public wrapper, ruff, and mypy pass. Story remains in progress and appears ready for story review/convergence unless reviewers request additional proof.
- 2026-06-11T13:17:54Z Continued A10/TAP-10 landmark trace seam. Added test-only `cure_subsequent_review.landmark_trace` runner, deterministic local fixture/golden under `tests/fixtures/subsequent_review/landmark_trace/`, and acceptance suite registration in the public subsequent-review wrapper. The runner copies Story 01-03 fixture artifacts, executes runtime modules 9/10/11 through existing helpers, represents module-12 degraded retry evidence, writes `landmark_trace_summary.json`, and asserts module-13 remains outside the runtime semantic registry. Focused landmark/runtime/control/public wrapper suites plus ruff and mypy pass. Story remains in progress; next optional seam is Variant-C verifier payload enrichment (`pr_files_changed` / richer `discussion_signals`) or final story review if no more TAP gaps are required.
- 2026-06-11T13:07:43Z Continued A14/TAP-14 production routing. Kept decision/degraded discussion fetch before sandbox clone, but deferred enabled subsequent-review intake for actual review runs until after repo checkout, ChunkHound top-up, and LLM runtime preflights, then wired `LlmFindingVerifier(repo_dir=<checked-out repo>)` as the production `source_verifier` with direct-file context and optional ChunkHound-backed research LLM when indexing is enabled. Moved runtime pre-prompt packaging/governor execution to after deferred intake so `$PRIOR_REVIEW_BRIEF` is still available before prompt rendering. `--no-review` artifact-generation runs keep the prior pre-clone/default-verifier path. Added PR-flow integration proof that the verifier is routed only after a checked-out repo exists. Focused verifier/source/semantic/linker/control-plane/PR-flow suites, public wrapper, ruff, and mypy pass. Story remains in progress; A10 landmark trace is the next likely seam.
- 2026-06-11T12:23:41Z Continued from the 12:15 red seam. Wired `LlmDiscussionLinker` into production `_pr_flow_impl` when a review LLM is resolved, using the same resolved LLM config and shared `ReviewMemoryStore`/current head for linker cache; added PR-flow integration proof that enabled review runs pass an `LlmDiscussionLinker` to intake. Began A14/TAP-14 with `LlmFindingVerifier` unit seam: direct file ±20-line source context, strict JSON state normalization, `need_more_context` -> chunkhound-research -> second-pass flow, and `not_verifiable` when evidence refs are absent. Production PR-flow source-verifier routing remains pending because current intake executes before sandbox repo checkout.
- 2026-06-11T12:15:38Z Picked up existing in-progress step and resumed work from the latest handoff CTA: production `_pr_flow_impl` / runtime LLM wiring for `LlmDiscussionLinker` before widening to A14/TAP-14 or A10.
- 2026-06-11T12:11:41Z Continued A12/A13/TAP-13/TAP-15 discussion linker and semantic verifier filter seam. Added `LlmDiscussionLinker` with strict JSON classifier contract, per-event prompt containing event + prior finding group bodies, low-confidence `group_id:null` normalization, and `cure_memory.json` linker-result cache replay/update by event/body-hash/head. Reordered semantic pipeline so discussion signals resolve before source truth and added a tunable verifier skip matrix: groups where all linked discussion signals are untrusted skip-classes (`pushback`, `by_design`, `addressed_elsewhere`, `duplicate_superseded`) skip provider calls and remain reportable as still-open without treating discussion as source proof; trusted or verify-class signals still go through source verification. Focused linker/semantic/source/memory/control-plane/PR-flow/public wrapper suites, ruff, and mypy pass. Story remains in progress; production `_pr_flow_impl` LLM linker wiring, production LLM finding verifier, and landmark trace remain pending.
- 2026-06-11T11:57:43Z Continued A5/TAP-05 post-review report-governor sanitization seam. Added warn-only `report_governor_result.json` audit helper with fakeable auditor contract, JSON normalization/degraded failure records, empty-brief/governor-off skip behavior, manifest/meta updates, and `_pr_flow_impl` post-review routing through the same resolved LLM/runtime as the main review. Strict pre-prompt missing-ledger `ValueError` is now surfaced as `ReviewflowError` by PR flow. Focused report-governor/runtime suites, PR-flow/public subsequent wrapper, ruff, and mypy pass. Story remains in progress; LLM discussion linker, semantic verifier filter reorder, production LLM verifier, and landmark trace remain pending.
- 2026-06-11T11:50:09Z Continued TAP-07/TAP-09 degraded discussion fetch controller and FB-010 single-fetch reuse seam. Added module-12 `DiscussionFetchController` with retry/skip/abort decisions and `degraded_runtime.json`, converted `decide_subsequent_review()` to return `(decision, discussion)` and accept prefetched discussion, taught intake to reuse `prefetched_discussion`/`discussion_fetcher`, and wired `_pr_flow_impl` so controller discussion is reused for decision and intake before downstream artifact writes. Focused degraded-runtime/decision/control-plane/PR-flow suites, public subsequent-review wrapper, ruff, and mypy pass. Story remains in progress; post-review sanitization LLM, LLM discussion linker/verifier, semantic filter reorder, and landmark trace remain pending.
- 2026-06-11T11:30:32Z Continued TAP-02/TAP-03/TAP-04 red-first runtime packaging/governor seam. Added module-9 `review_context_package.json` / `subsequent_review_context.md`, module-10 pre-review `governor_brief.md` generation, manifest records, strict/off governor handling for pre-prompt, and `$PRIOR_REVIEW_BRIEF` prompt placeholder injection across the 11 subsequent-aware PR prompt templates (zip excluded). `_pr_flow_impl` now runs the pre-prompt runtime phase after enabled intake and routes the resulting brief into single-pass and multipass initial PR prompts; render fallback leaves the variable empty elsewhere. Focused packaging/memory/PR-flow/public wrapper/prompt tests, ruff, and mypy pass. Story remains in progress; degraded fetch controller, post-review sanitization LLM, LLM linker/verifier, semantic filter reorder, and landmark trace remain pending.
- 2026-06-11T10:49:45Z Continued TAP-09/A8 red-first runtime memory update seam. Added `cure_subsequent_review.runtime` post-review helper to read completed `source_verification.json` / `disposition_ledger.json`, update shared per-PR `cure_memory.json`, and record module status in `run_manifest.json`; `_pr_flow_impl` now constructs/routes a shared memory store/current head into intake and updates memory after a successful review artifact. Focused runtime-memory, memory-store, PR-flow, public subsequent-review wrapper, config wrapper, ruff, and mypy pass. Story remains in progress; packager/governor runtime orchestration, prompt brief injection, degraded fetch controller, LLM linker/verifier, and landmark trace remain pending.
- 2026-06-11T10:31:40Z Continued TAP-12/A5 red-first governor-mode config seam and PR head guardrail. Added `[subsequent_review].governor_mode` parser/export with `strict` default, valid `strict|warn|off` normalization, invalid/non-string `ReviewflowError`, `_pr_flow_impl` early config validation/metadata, and missing PR head SHA fail-fast before sandbox creation. Focused config/PR-flow/memory/control tests, public subsequent-review wrapper, ruff, and mypy pass. Story remains in progress; runtime modules/governor/linker/verifier/degraded orchestration remain pending.
- 2026-06-11T10:19:18Z Started TAP-06/A8 red-first memory-store seam. Added shared per-PR `ReviewMemoryStore`, source-truth memory synthesis gate, semantic/control-plane optional `memory_store` + `current_head` routing, and public subsequent-review wrapper registration for the new memory-store unit suite. Focused tests, public subsequent-review wrapper, ruff, and mypy pass. Story remains in progress; broader Story 04 runtime modules/linker/verifier/governor/degraded paths remain pending.
- 2026-06-11T10:05:22Z Recorded readonly plan approval in the tracker, claimed step, and prepared the Story 04 implementation worktree. Focused red seam not chosen yet.

## Session Handoff
- **Timestamp**: 2026-06-13T04:47:11Z
- **Status**: 🔵 IN PR
- **Completed In This Session**:
  - Addressed FB-027/A16 locally: prompt templates now state that, when the governor brief contains required issue history, final output must begin with `### Prior Review Issue History` before steps/summary/assessments; report-governor audit deterministically degrades if issue history is not first or if stable governor-brief issue clusters/statuses are missing.
  - Added proof that body-only PR comments remain a carried-forward/re_report issue cluster while official-footer policy findings remain out-of-scope.
  - Addressed FB-028 locally: malformed/failed LLM discussion-linker output returns a degraded no-link result, preserving semantic artifact generation instead of raising.
  - Addressed FB-029 locally: memory replay rejects same ordinal `group_id` cache hits when the cached finding identity differs from the current finding IDs.
- **Remaining**:
  - Fresh `/openspec-story-review` on the local product/coordinator diff.
  - If approved, commit/push to PR #22 and rerun/audit live PR #22 output.
  - Done gate remains a live audit proving `DA-0006` is not carried-forward/re_report and final output leads with human issue history.
- **Blockers**: none.
- **Next Steps**: `/openspec-story-review cure-subsequent-pr-review story-04-review-runtime-integration-guardrails-memory-trace` against the active worktree.
- **Worktrees**:
  - CURe: `/home/vscode/add-worktrees/CURe-cure-subsequent-pr-review-story-04-review-runtime-integration-guardrails-memory-trace`
- **Proof Statement**: Red regressions for FB-027/FB-028/FB-029 failed before implementation and now pass. Verification passed: targeted report-governor/discussion-linker/discussion-signals/memory/prompt suite (`67 passed`), public subsequent-review wrapper (`115 passed, 29 subtests`), A19 preservation suite (`19 passed, 15 subtests`), `ruff check .`, `git diff --check`, and `mypy`.

## PR State
- PR URL: https://github.com/grzegorznowak/CURe/pull/22
- Number: 22
- Title: Add subsequent PR intake ledger
- Branch: cure-subsequent-pr-review/story-01-intake
- Opened at: pre-existing Story 01 PR
- PR status: open; latest audited head is `372b4a753099c4b6e077d98551da51039222a16b`; current pushed hardening head before this remap repair is `ee7410aeedad7e5df92caec90a89ecc28ff89de6`. The 2026-06-13T08:08 live-output audit completed and proved the previous Story 04 gates for strict multipass schema, A17 warn-only governor, and A19/`DA-0006`; however, the final CURe review verdict is REQUEST CHANGES for hardening issues now remapped into Stories 01, 03, and 04. 2026-06-12T18:07Z refresh previously reported `mergeStateStatus=CLEAN` and Detect Secrets success.
- Merge commit: —
- Last synced/audited: 2026-06-13T08:44Z; head `372b4a753099c4b6e077d98551da51039222a16b`; pushed to `origin/cure-subsequent-pr-review/story-01-intake`; PR body updated in prior sync. Duplicate PR #23 was already merged into this branch and remains superseded.
- Coordination note: PR #22 remains the canonical review surface. Story 04 is 🟢 PLAN APPROVED / 🔵 IN PR; the prior live done gate for `DA-0006` and strict multipass step schemas is satisfied in sandbox `grzegorznowak-cure-pr22-20260613-080828-d739`, but the overall PR review remains REQUEST CHANGES until a fresh live audit verifies the remapped runtime hardening.

### Reopened PR #22 Live-Run Follow-up Plan

PR #22 live-run audit reopened Story 04 because the enabled-path runtime artifacts existed but the final review/governor output exposed acceptance gaps beyond the A1-A14 baseline.

#### Narrative Follow-up Findings

| Finding | Acceptance | Narrative requirement |
|---|---|---|
| F1 | A15 | Context package and markdown statuses must reflect final manifest/runtime module statuses after packager/governor/memory updates; package/context output must be refreshed after runtime status updates. |
| F2 | A16 | Final subsequent-review prompts/reports need a human-readable prior-review issue summary that groups DA records into issue clusters with titles/statuses/reasons while preserving complete internal `DA-*` disposition coverage. |
| F3 | A17 | Report-governor awareness gaps (`partial`, missing DA row, contradiction) should degrade/warn only and never block publication, even in strict mode. |
| F4 | A18 | Verifier/source-truth must avoid stale duplicate `cure.py` symbol confusion by resolving active import/re-export bindings or degrading inactive-source references. |
| F5 | A19 | Story 02/FB-026 footer policy must propagate into Story 04 runtime context/governor/final-output checks and semantic disposition: official footer is sufficient regardless of author/login; generic/body-only markers remain rejected; policy-approved footer findings such as `G-0006`/`SV-0006`/`DA-0006` must not remain `still_open` + `re_report`/`carried-forward`. |

#### Reopened TAP / Verification Addendum

Follow-up verification rows TAP-16 through TAP-20 are integrated into the canonical `### Test Architecture Plan` below with the same full-column proof contract as TAP-01 through TAP-15.

## Unresolved Debt Friction
- A16 live output now starts with human-readable issue history, but the same output still exposes a prominent top-level `### Internal DA coverage` section. Product decision: keep complete DA coverage as audit/provenance, but remove/demote it from ordinary consumer-facing `review.md`; tracked in Story 04 as FB-030 after remap.
- Broader memory/linker identity, runtime citation enforcement, linker runtime policy, and abort-path hardening from the 2026-06-13 live audit are tracked in Story 04 after remap. Story 01 owns the supporting session-bound path and concise-parser items; Story 03 owns the authority/source-truth invariants.
- Story 04's previous live gate for `DA-0006` is satisfied: latest artifacts classify `DA-0006` as `out-of-scope` / `move_out_of_scope`, not `carried-forward/re_report`.


## Live-audit remap progress

- 2026-06-14T10:46:40Z **OpenSpec remap repair**: synthetic Story 05 is superseded/remapped. Story 04 now owns FB-030, FB-031, FB-034, runtime-FB-035, FB-036, and FB-038; Story 01 owns FB-033/FB-037 and supports stable identity; Story 03 owns FB-032 and the source-truth side of FB-035. Product/test code from PR #22 commits `f96e7ad` and `ee7410a` remains unchanged. Fresh PR #22 live audit is still pending before Story 04 can leave `🔵 IN PR`.
