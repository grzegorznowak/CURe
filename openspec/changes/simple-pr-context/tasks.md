# Tasks: simple-pr-context

> **Current-contract note (FB-021–FB-024, amended by FB-031–FB-033):** Every checked item below is preserved as historical evidence of work completed under its then-current contract. Checked items that require automatic enablement, unbounded model input, local session/history discovery, local fail-hard behavior, or fail-hard enrichment are explicitly superseded and are not current completion evidence. R10–R12 record the completed FB-021–FB-024 implementation, while the unchecked R13 plan is the current remediation authority for FB-031–FB-033. S17–S31 cover A19–A28/A31–A34, A29/A30 are direct closure acceptances, and TAP-14–TAP-23 own the current proof.

## Setup & Prerequisites
- [x] Create directory `cure_pr_context/` with empty `__init__.py`
- [x] Create directory `tests/cure_pr_context/` (without `__init__.py` so it does not shadow the real package during pytest)
- [x] Add `cure_pr_context` to `pyproject.toml` (`[tool.setuptools].packages`) for install/wheel
- [x] Add top-level `cure_github.py` to `pyproject.toml` (`[tool.setuptools].py-modules`) for install/wheel
- [x] Read old `github_history.py` / old branch `cure.py` from branch `cure-subsequent-pr-review/story-01-intake` — extract `collect_pr_discussion`, endpoints, `gh_api_list`, pagination, and normalization
- [x] **Historical pre-FB-019 work (superseded for remote membership):** Read old `prior_corpus.py` and port footer/session/head handling. This remains implementation history only; the superseded FB-019/FB-020 R10–R12 plan would have removed remote authority while preserving local discovery, but FB-022 now removes local discovery from the MVP.

## Core Implementation
- [x] Implement/port `cure_github.py::gh_api_list()` — list-capable helper for `gh api --paginate [--slurp]` with fallback without `--slurp`; keep GitHub CLI/public API adapter helpers out of `cure.py` except import/re-export seams
- [x] Implement `fetcher.py::fetch_pr_discussion()` — 3 endpoints, list-capable `gh_fetch`, normalize to flat dicts
- [x] **Historical pre-FB-019 implementation (superseded):** Added remote-footer extraction to `find_past_reviews(..., head_sha)`. Footer/session/head metadata ceased to be remote-membership authority under FB-019; the historical replacement plan preserved local discovery, which FB-022 now supersedes.
- [x] **Historical pre-FB-019 implementation (superseded):** Added cross-source local/posted identity and Jaccard pruning. This checked item records prior work only and must not be read as current completion; the historical replacement plan retained local-only collapse, which FB-022 now removes from the MVP.
- [x] Implement `orient.py::build_orientation_brief()` — scanner prompt + LLM call → fixed sections + inline instructions
- [x] Bound the scanner prompt's `Resolved areas` semantics to supplied discussion/past-review text that says an area was addressed or resolved; explicitly avoid claiming authoritative GitHub thread-resolution state
- [x] **Historical pre-FB-019 orchestration (superseded in part):** Implemented fetch → footer-aware corpus → retained-side cross-source dedup → orient → pruned/retained artifacts. The historical replacement plan used complete ordered remote discussion plus separate local-only collapse; FB-022 now removes that local component.
- [x] Remove `$PRIOR_CONTEXT` and old in-template sequencing instructions from normal/big singlepass templates; keep `$PRIOR_CONTEXT` only in the multipass synth template; plan/step templates remain context-free
- [x] Implement `cure.py::_reconcile_prior_context(draft_review, orientation_brief, run_llm)` with an inline reconcile prompt for singlepass pass 2
- [x] Inject `build_pr_context()` in `cure.py::_pr_flow_impl` after `compute_pr_stats`, passing effective `head_sha` (`review_head_sha` if it exists, otherwise PR API `head_sha`); for singlepass with non-empty context, run draft review then `_reconcile_prior_context`; for singlepass with empty context, keep the one-call flow; exclude prior context from `_build_multipass_step_entries`
- [x] Pass `PRIOR_CONTEXT` (brief or `""`) in `extra_vars` to the fresh multipass synth render; normal/big singlepass, plan, and step templates have no `$PRIOR_CONTEXT` token
- [x] Update `_resume_flow_impl` to read persisted `work/pr_context_orientation.md` when present (otherwise `""`) and pass `PRIOR_CONTEXT` whenever it renders the shared multipass synth template
- [x] Register `pr_context` meta in `progress.meta` and abort before prompt rendering on GitHub/LLM/session scan errors

### Feedback-remediation implementation (complete)
- [x] **R2 historical mixed remediation:** Completed local-artifact validation (historical A13/A17 evidence) and pre-FB-019 remote session-ID/posted-pruning work. Neither establishes current FB-021–FB-024 completion.
- [x] **R3 / FB-006, FB-007:** Make orientation heading recognition backtick/tilde-fence-aware and keep `build_pr_context()`'s return boundary finalized/normalized before canonical persistence.
- [x] **R4 / FB-009:** Tighten `cure_github._decode_gh_api_list_stdout` so every decoded document/page is array-valued before flattening; reject adjacent objects, mixed pages, scalars, objects, and malformed JSON with `ReviewflowError`.
- [x] **R5 / FB-003, FB-007, FB-008:** In `cure.py`, enforce invocation-attributable fresh non-empty pass-one/pass-two artifacts and atomic final promotion for context-bearing singlepass; persist only finalized orientation content; make fresh/resumed synth consume the exact canonical value or `""` through opaque non-recursive insertion that preserves `keep:$PRIOR_CONTEXT|$OTHER|${OTHER}|$PLAN_JSON_PATH|${PLAN_JSON_PATH}|$AGENT_DESC` byte-for-byte.

## Verification & Proof
- [x] `tests/cure_pr_context/test_fetcher.py` — unit tests with mock `gh_fetch`/`gh_api_list` returning arrays (TAP-01)
- [x] **Historical pre-FB-019 TAP-02 proof (superseded for remote behavior):** Proved footer/head selection and cross-source remote pruning. These assertions are historical only; the superseded plan assigned local A13/A17/A18 to TAP-02 and remote retention to TAP-13/TAP-05.
- [x] `tests/cure_pr_context/test_orient.py` — retain heading normalization tests and add captured-prompt proof for normalized payload plus bounded `Resolved areas` semantics (TAP-03)
- [x] **Historical TAP-04 artifact assertion (superseded in part):** Proved orchestration using then-pruned discussion and retained past reviews. A2 failure propagation remains historical proof; the superseded plan would have replaced pruned discussion with complete remote model input.
- [x] **Historical pre-FB-019 TAP-05 proof (superseded for remote behavior):** Proved footer-aware retained-side pruning and pruned output. The superseded R10/R12 plan targeted complete ordered remote result/orientation/debug proof while keeping separate local reviews; FB-021/FB-022 replace that target.
- [x] `tests/cure_pr_context/test_templates.py` — verify multipass synth has/renders `$PRIOR_CONTEXT`, normal/big singlepass templates do NOT contain it, plan/step templates do NOT contain it, and the reconcile prompt works with draft + brief (TAP-06)
- [x] `tests/test_cure_pr_flow.py` — retain injection/effective-`head_sha` and singlepass branch proof; add a deterministic reconcile-LLM exception case proving `_reconcile_prior_context()` failure propagates through `_pr_flow_impl`, records failed-run observability, and does not accept the draft as a successful final review; add resumed multipass synth regression proof for present/absent `work/pr_context_orientation.md`, required `PRIOR_CONTEXT` substitution, and no raw token; keep plan/step context-free; fallback may extract a synth-render helper, with no generic `--dry-run` (TAP-07, A12)
- [x] Packaging smoke — build wheel/install target and `import cure_pr_context` plus `import cure_github` from installation (TAP-09)

### Feedback-remediation RED and focused proof (complete)
- [x] **R1 historical mixed RED:** Added local A13/A17 safety regressions plus pre-FB-019 remote identity/pruning regressions. This is historical evidence only under FB-021–FB-024.
- [x] **R1 / TAP-03, TAP-04:** Add deterministic local-artifact completeness plus FB-006/FB-007 regressions in `tests/cure_pr_context/test_orient.py` and `test_init.py`; record RED for missing/non-file matching-session failures before orientation, fenced pseudo-headings, finalized-normalization return, and corrupt-input/failure propagation without partial artifacts or later-stage calls.
- [x] **R1 / TAP-06, TAP-07:** Add deterministic FB-003/FB-007/FB-008 regressions in `tests/cure_pr_context/test_templates.py` and focused `tests/test_cure_pr_flow.py`; record RED for stale/missing/empty context-bearing pass artifacts and atomic promotion. For raw/interrupted/failed canonical persistence, exact resume consumption, and byte-for-byte opaque insertion of `keep:$PRIOR_CONTEXT|$OTHER|${OTHER}|$PLAN_JSON_PATH|${PLAN_JSON_PATH}|$AGENT_DESC`, extend the established full fresh/resume multipass owners in `tests/_reviewflow_unittest_grounding_impl.py` (`_run_pr_flow_with_synth_grounding` at line 6229, `_run_pr_flow_for_tool_proof` at line 8489, current fresh opaque-context proof at line 9158, and current resume opaque-context proof at line 14031). Use non-empty `/tmp/plan.json` and `AGENT` values and capture both real `pr_flow` and `resume_flow` synth prompts; do not create a duplicate fake orchestration seam.
- [x] **R1 / TAP-01, TAP-10:** Add deterministic FB-009 valid/invalid decoder-shape regressions in `tests/test_cure_pr_flow.py` plus normalized-fetcher boundary checks; record RED for every currently accepted non-array document/page shape.
- [x] **R2 proof:** Run `python -m pytest tests/cure_pr_context/test_corpus.py tests/cure_pr_context/test_integration.py -v` and confirm TAP-02/TAP-05 GREEN with matching completed-session missing/default/non-file fail-hard behavior, exact containment, identity, and `meta.n_deduped` observability.
- [x] **R3 proof:** Run `python -m pytest tests/cure_pr_context/test_orient.py tests/cure_pr_context/test_init.py -v` and confirm TAP-03/TAP-04 GREEN with fence-aware finalized output and fail-hard/no-partial-artifact observability.
- [x] **R4 proof:** Run `python -m pytest tests/cure_pr_context/test_fetcher.py -v` and `python -m pytest tests/test_cure_pr_flow.py -k gh_api_list -v`; confirm TAP-01/TAP-10 GREEN for all valid and rejected document/page shapes.
- [x] **R5 proof:** Run `python -m pytest tests/cure_pr_context/test_templates.py tests/test_cure_pr_flow.py -v` and the targeted full-flow owners via `python -m pytest tests/test_reviewflow_unittest.py -k 'prior_context and (multipass or resume)' -v`; confirm TAP-06/TAP-07 GREEN for fresh pass attribution, failure recording, atomic acceptance, canonical persistence/resume, and byte-for-byte preservation of the active-key adversarial context in captured real fresh and resumed runtime synth prompts.

## Latest Review Remediation
- [x] **Historical pre-FB-019 behavior (superseded):** Collapsed local/posted representations and pruned the posted event. The FB-019/FB-020 contract forbade this; that superseded R10–R12 plan is replaced by the bounded audit/model split.
- [x] **Historical pre-FB-019 behavior (superseded):** Implemented/proved character 3-gram Jaccard cross-source pruning. Jaccard no longer owns remote corpus behavior; the superseded A18 retained local-only collapse, which FB-022 removes from the MVP.
- [x] Guarantee all five required Markdown `##` orientation headings when LLM output only mentions section names in usage instructions (A5/TAP-03)
- [x] Close the shared synth-template resume leak and prove present/absent persisted orientation behavior (S7/A8/TAP-07)
- [x] Add acceptance-backed fail-hard flow proof for reconcile LLM failure propagation and failed-run/no-successful-final observability (S9/A12/TAP-07)
- [x] Clarify that S2 does not promise authoritative GitHub thread-resolution state
- [x] Remove the non-acceptance coverage threshold from A10 proof; the reproducible deterministic test command remains the gate

### FB-001..FB-009 amended-contract closure (complete)
- [x] **FB-001:** Keep A2/A3/A4/A5/A8/A10/A12 proof claims provisional until every detailed FB-002..FB-009 RED-first regression, implementation task, and focused/full gate below is complete.
- [x] **Local stale-artifact branch:** Prove each parseable matching `status=done` session's explicit/default selected review artifact must be a regular file; missing paths and directory/non-file artifacts raise with session/path observability before orientation and cannot be silently omitted by the permissive scanner.
- [x] **FB-002:** Prove metadata-selected local review paths remain resolved-contained under `sandbox_root`, escape attempts fail before read, and malformed/non-object metadata plus invalid UTF-8 review files propagate.
- [x] **FB-003:** Prove context-bearing singlepass rejects stale/missing/empty pass-one and pass-two output, records failure, and promotes only fresh pass-two output atomically.
- [x] **FB-004 historical proof (superseded for remote behavior):** Proved right-anchored remote footer session-ID parsing; FB-019 made that parser non-authoritative; current replacement R11 removes all local-history composition as well.
- [x] **FB-005 historical proof (superseded):** Proved stable local/remote identity pruning; FB-019 forbade cross-source pruning; FB-021–FB-024 now replace that contract/proof plan.
- [x] **FB-006:** Prove backtick/tilde fenced pseudo-headings do not satisfy required orientation sections and missing real headings are appended outside fences.
- [x] **FB-007:** Prove partial/raw/interrupted/failed scanner output never replaces canonical orientation content, successful persistence is exactly finalized output, and runtime resume consumes that exact content or `""`.
- [x] **FB-008:** Prove fresh/resumed context insertion preserves `keep:$PRIOR_CONTEXT|$OTHER|${OTHER}|$PLAN_JSON_PATH|${PLAN_JSON_PATH}|$AGENT_DESC` byte-for-byte while removing only the template-owned token. `$PLAN_JSON_PATH`, `${PLAN_JSON_PATH}`, and `$AGENT_DESC` are mandatory active post-insertion keys; capture both `_pr_flow_impl` and `_resume_flow_impl` runtime synth prompts with non-empty replacement values.
- [x] **FB-009:** Prove strict array-only GitHub document/page decoding accepts only the contract's array forms and rejects adjacent objects, mixed pages, scalars/objects, and malformed JSON.

## Historical Newest Feedback Remediation (completed under superseded contracts)

### R7 — RED-first reproduction and proof checkpoints
- [x] **FB-012 / TAP-07 RED:** Seed pre-existing pass-one and pass-two artifacts and prove both normal and big context-bearing singlepass real-flow branches currently accept a stale/no-write/missing/empty output or otherwise fail the newly required invocation-attribution assertions for the intended reason; preserve separate empty-context one-call behavior.
- [x] **FB-013 / TAP-08 proof-hygiene checkpoint:** Preserve source `manual:sha256-ebd73804df89:2` and its reported trailing-whitespace/false-clean-gate contradiction as coordination RED evidence, then run `base="$(git merge-base HEAD origin/main)"; git diff --check "$base" --` before remediation and record the actual complete tracked-delta result. Do not create a product-behavior test or use bare `git diff --check` as closure.
- [x] **FB-014 / TAP-10 RED:** Add deterministic blank and whitespace-only GitHub stdout failures alongside literal `[]` success; confirm the zero-document cases fail for the intended decoder reason before implementation.
- [x] **FB-015 / TAP-03 RED:** Add variable-length backtick/tilde fence cases (shorter pseudo-close, equal/longer valid close, unterminated fence) plus omitted/fenced canonical-instruction cases; confirm each newly exposed finalization obligation fails for the intended reason.
- [x] **FB-016 / TAP-06/TAP-07 RED:** Add exact-one-owned-token and former-marker-collision fixtures plus four established real orchestration captures (fresh non-empty, fresh empty, resume persisted, resume absent); confirm helper-only or marker round-trip behavior cannot satisfy the runtime assertions.
- [x] **FB-017 historical RED (superseded for remote membership):** Reproduced terminal-footer precedence under the prior contract. Footer position/content became ordinary remote data; the superseded R10/TAP-13 plan targeted complete retention.
- [x] **FB-018 / TAP-02/TAP-04/TAP-05 RED:** Add equivalent relative/absolute `sandbox_root` valid-artifact cases at the public boundary while retaining real absolute/traversal/symlink escapes; confirm relative-root drift fails for the intended reason before implementation.
- [x] **GitHub list adapter / TAP-11 proof checkpoint:** Create `tests/test_cure_github.py`; reset `_GH_API_SLURP_SUPPORTED` per case and inject scripted `run_cmd` results/errors plus `_github_public_api_list`. Capture exact command sequences for slurp success, unsupported flag then exactly one no-slurp retry, and a later cached no-slurp call; cover direct/post-retry auth routing, disabled fallback, unsupported host, and effective non-auth propagation. Record current behavior truthfully; do not make live CLI/network calls or assert FB-010 terminal both-attempt exception identity/message/provenance.
- [x] **Public fallback completeness / TAP-12 RED:** In `tests/test_cure_github.py`, exercise the real `cure_github._github_public_api_list` with failure-capable fake `urlopen` responses. Require at least two array pages connected by `Link: <...>; rel="next"`, exact requested-URL order, ordered combined items, and separate invalid-JSON/non-array page failures with no partial result. Record RED truthfully if current behavior misses any obligation; do not use live network.

### R8 — Bounded implementation and focused GREEN proof
- [x] **FB-012 implementation/GREEN:** Enforce invocation-attributable fresh non-empty normal/big singlepass outputs and atomic pass-two promotion at the real flow boundary; run the focused TAP-07 cases GREEN without changing empty-context one-call behavior.
- [x] **FB-013 coordination repair:** Fix any whitespace found by the complete tracked-delta gate and correct any coordination claim that cites a bare/partial gate; make no product-code change solely for FB-013 and leave final gate closure to R9.
- [x] **FB-014 implementation/GREEN:** Reject zero decoded GitHub documents from blank/whitespace stdout while preserving literal `[]` and all already accepted array document/page forms; run TAP-10 GREEN.
- [x] **FB-015 implementation/GREEN:** Finalize scanner Markdown with opener-character/length-aware fence handling, close unterminated fences safely, and emit exact canonical instructions/headings structurally; run TAP-03/TAP-04 GREEN.
- [x] **FB-016 implementation/GREEN:** Replace collision-prone protect-and-global-replace insertion with an exact-one-token partition/render/concatenate boundary and prove all four real fresh/resume captures plus marker-like bytes GREEN through TAP-06/TAP-07.
- [x] **FB-017 historical GREEN (superseded for remote membership):** Implemented terminal-footer authority under the prior contract. The superseded R11 plan removed that authority from remote composition; checked status remains historical truth only.
- [x] **FB-018 implementation/GREEN:** Resolve `sandbox_root` once and preserve valid relative/absolute equivalence without weakening containment or matching-artifact failures; run TAP-02/TAP-04/TAP-05 GREEN.
- [x] **GitHub list adapter / TAP-11 GREEN:** Run the TAP-11 routing cases in `python -m pytest tests/test_cure_github.py -v`; make only bounded adapter changes if deterministic command/routing proof exposes a mismatch. Preserve the public `gh_api_list` API and keep terminal retry-failure provenance deferred to initiative FB-010.
- [x] **Public fallback completeness / TAP-12 GREEN:** Run the fake-HTTP `_github_public_api_list` cases in `python -m pytest tests/test_cure_github.py -v`; make only bounded helper changes if two-page traversal, ordered accumulation, invalid JSON, or non-array page validation fails. Keep live GitHub/network excluded and do not absorb FB-010.

### R9 — Final gates, proof reconciliation, and coordination
- [x] **R9 focused proof gate (FB-012/FB-014..FB-018 plus adapter routing/public pagination):** Run all TAP-01..TAP-07 and TAP-10/TAP-12 owning suites together; preserve exact pass summaries and do not treat prior historical GREEN as closure for the new cases.
- [x] **R9 quality and FB-013 complete-delta gate:** Compute `base="$(git merge-base HEAD origin/main)"`, enumerate every changed Python path with `git diff --name-only "$base" -- '*.py'`, and pass that complete list to Ruff (including `cure.py` and every changed test/proof owner). Run mypy on the precise changed typed-production boundary `cure_github.py cure_pr_context/`; tests remain excluded by `[tool.mypy].exclude`, and the legacy `cure.py` monolith remains outside `[tool.mypy].files` rather than importing 225 current legacy errors into this story. Then run `git diff --check "$base" --`; preserve the exact list/results and do not substitute a bare unstaged-only diff check.
- [x] **R9 packaging/regression gates:** Rebuild/install the wheel in a disposable target, import `cure_pr_context` and `cure_github`, and run the full CURe test suite with the approved timeout/fail-fast command; preserve exact summaries.
- [x] **R9 structural/proof reconciliation:** Re-run Scenario → Acceptance → TAP/APM traceability; verify routing-only TAP-11 does not substitute for TAP-12 public pagination completeness; keep FB-013 explicitly classified as proof hygiene and FB-010 terminal provenance explicitly deferred; update provisional A2/A3/A4/A5/A6/A8/A10/A12/A13/A14/A15/A16 rows to `final` only from current evidence.
- [x] **R9 coordination write-back:** Reconcile `story.md`, `design.md`, `tasks.md`, and any delta specs against implemented evidence; record the complete tracked-delta gate truthfully, check these new tasks only after their own evidence exists, and transition implementation coordination only through the implementation-owned workflow.

## FB-021–FB-024 P0 Contract Remediation (complete)

> The obsolete unchecked FB-019/FB-020 R10–R12 plan is superseded in full. The replacement R10–R12 phases below record the completed FB-021–FB-024 implementation; FB-031–FB-033 reopen bounded work only through the unchecked R13 section.

### R10 — RED-first amended-contract proof

#### Executable owner registry

- **TAP-14:** `tests/_reviewflow_unittest_runtime_ui_impl.py::test_parser_accepts_if_reviewed_and_followup_flags` plus `tests/_reviewflow_unittest_grounding_impl.py::{test_pr_flow_prior_review_latest_happens_before_picker,test_pr_flow_no_review_skips_review_only_setup}` extended with adjacent list/prompt-selected and explicit-PR-context sentinels, via `python -m pytest tests/test_reviewflow_unittest.py -k 'parser_accepts_if_reviewed_and_followup_flags or pr_flow_prior_review or pr_flow_no_review_skips_review_only_setup' -v`; plus real-flow eligibility cases via `python -m pytest tests/test_cure_pr_flow.py -k 'pr_context and (eligibility or no_remote)' -v`.
- **TAP-15/TAP-16/TAP-21:** package/raw boundaries via `python -m pytest tests/cure_pr_context tests/test_cure_github.py -v`; exact owners and fixture matrices are in `story.md::Test Architecture Plan`.
- **TAP-17/TAP-18/TAP-22:** fresh delivery, metadata/persistence, and negative catches via `python -m pytest tests/test_cure_pr_flow.py -v`.
- **TAP-19:** regular resume via `tests/test_cure_pr_flow.py` plus `tests/_reviewflow_unittest_grounding_impl.py::{test_resume_flow_from_synth_consumes_persisted_opaque_prior_context,test_resume_flow_from_synth_renders_absent_prior_context_as_empty}` and adjacent context-synthesis/no-delivery cases, executed by the exact commands in the TAP row.
- **TAP-20:** completed-session incremental resume via `tests/_reviewflow_unittest_grounding_impl.py::test_incremental_completed_resume_threads_verbose_guidance_into_resume_synth_prompt` and adjacent PR-context fallback cases, executed with `python -m pytest tests/test_reviewflow_unittest.py -k 'incremental_completed_resume and (prior_context or verbose_guidance or context_synthesis)' -v`.
- **TAP-23:** every command under `story.md::Verification Commands`; no historical GREEN substitutes for the amended proof.

- [x] **TAP-14 / A19 default-off Boolean control RED:** Add parser and real-flow cases for omitted option, `--pr-context`, and `--no-pr-context`. Assert omission/explicit source metadata and prove disabled paths call neither discussion fetch nor orientation.
- [x] **TAP-14 / A20 eligibility RED:** Add custom `--prompt`, custom `--prompt-file`, and unsupported-profile cases with `--pr-context`; prove bypass occurs before fetch/orientation and ordinary prompt flow remains unchanged.
- [x] **TAP-14 / A33/A34 non-review route RED:** Extend the established real-flow owners for completed-session `--if-reviewed list|latest|prompt-selected` and `--no-review`, pair each with explicit `--pr-context`, and install fetch/select/orient/reconcile/synth and artifact/meta sentinels. Prove historical exits return before new sandbox creation without mutating historical sessions, and no-review preserves session/index-only completion while creating no `work/pr_context_*` or `meta.json::pr_context`.
- [x] **TAP-14/TAP-18 / A27 no-remote-data RED:** With explicit enablement and three valid empty endpoint arrays, prove orientation is not called, outcome is `bypassed`, reason is `no_remote_context`, the complete empty audit artifact is preserved when writable, and ordinary review continues.
- [x] **TAP-15/TAP-18 / A32 no-selected-context RED:** Use a nonempty normalized corpus whose first newest-first candidate cannot fit after fixed prompt/framing overhead. Prove the complete nonempty audit result/artifact remains unchanged, selected count is zero, orientation is not called, outcome is `bypassed`, reason is `no_selected_context`, and ordinary review continues.
- [x] **TAP-15 / A21 boundary RED:** In `tests/cure_pr_context/test_corpus.py` plus public `test_init.py`/`test_integration.py`, add cap-1/cap/cap+1 fixtures for the exact 12,000-token fully assembled orientation prompt, fixed instructions/framing, normalized PR stats, and canonical JSON (`ensure_ascii=False`, recursive `sort_keys=True`, separators `(",", ":")`, `allow_nan=False`). Cover 1,000-body/100-event boundaries and D-13 `Z`/offset-equivalent/equal/naive/malformed/empty timestamp classes. Assert exact total orders, stop-at-first-limit, chronological restoration, immutable result/file audit, counts, estimates, and flags with the TAP-15 command.
- [x] **TAP-15 / full-audit RED:** Prove every normalized selected-PR endpoint event remains unchanged and endpoint-ordered in the public audit result and `work/pr_context_discussion.json`, even when omitted/truncated in selected model input.
- [x] **TAP-16 / A22 output/injection RED:** Add fresh orientation-output and fresh injected-context cap-1/cap/cap+1 cases, including fenced Markdown. Assert both independent 2,000 estimates, canonical instructions plus all five headings, valid finalized fence structure, and separate truncation flags.
- [x] **TAP-19/TAP-20 / A31 resume authority RED:** Build one shared D-15 origin-metadata × persisted-brief validator matrix (`used|bypassed|degraded|other|missing/non-object/legacy` × valid/missing/directory/non-UTF-8/empty/structurally-invalid/cap/cap+1). Then exercise representatives through both real owners: `_resume_flow_impl` captured by the established regular-resume tests and `_run_incremental_completed_multipass_resume` captured by the established incremental test/template. Prove exact used-valid reuse only, all other inserts `""` with exact reason, no fetch/orient, and no raw token using TAP-19/TAP-20 commands.
- [x] **TAP-21 / A23 remote-only RED:** Install filesystem/read sentinels around prior local-session owners and prove production context composition accepts no `sandbox_root`, returns no `past_reviews`, and performs no local session/history read. Preserve remote footer/session/head/similarity content as ordinary unchanged audit data.
- [x] **TAP-17/TAP-18 / A24 phase-correct fail-open RED:** Inject failures independently at fetch, selection, orientation, cap/finalization, required discussion write, and required brief write; require visible stage warning, D-14 first-failure `degraded` reason, and ordinary context-free review. Separately fail only final `work/pr_context_meta.json`: require one warning/no retry, authoritative `meta.json::pr_context.persistence` failure, and preservation of the already determined route outcome/reason/context mode/review/usage/latency.
- [x] **TAP-22 / strict failure-boundary RED:** In real `_pr_flow_impl` cases, separately inject checkout, base prompt/profile validation, cancellation, ordinary blind draft, multipass plan/step, final context-free synth, output acceptance/posting, session `meta.json` flush, and unrelated process failures. Prove they retain existing propagation/control semantics and are not swallowed as PR-context degradation.
- [x] **TAP-17/TAP-19/TAP-20/TAP-22 / A25 branch fallback RED:** Prove built-in singlepass reconciliation failure retains the fresh blind draft. For fresh, regular-resume, and completed-session incremental-resume used-context synth routes, script the first context-bearing call to fail and prove exactly one empty-context retry uses the same successful plan/step outputs, records `degraded/context_synthesis_failed`, `context_mode=off`, combined delivery latency, and separate nullable delivery/fallback provider-usage fields; separately prove each route's empty retry failure propagates while authoritative degradation metadata remains and no final mirror is attempted.
- [x] **TAP-18/TAP-19 / A26 no-delivery resume RED:** Seed a distinctive originating `meta.json::pr_context` payload, then exercise `_resume_flow_impl`'s completed/same-head fast no-op and regular reusable-review-artifact (`should_synth == false`) exits. Assert deep-equal nested payload preservation with no key/value mutation, zero reconciliation/synth calls, zero `work/pr_context_meta.json` mirror attempts, and unchanged review artifact while ordinary lifecycle timestamps/status/footer may update.
- [x] **TAP-18 / A26/A32 metadata RED:** With deterministic endpoint cardinalities, monotonic fake clock, provider adapters absent/partial/complete usage, and non-used/used-invalid/used-valid/fallback origin dictionaries containing valid, missing, wrong-typed, and negative leaves, assert the exact D-14/D-18 schema and field ownership. Prove transactional `counts.fetched` versus post-normalization `counts.normalized`, selection-time omitted/truncation assignment, sanitized used-origin acquisition inheritance, non-used acquisition reset, current delivery/result/mirror values, reason precedence, `int(max(0,end-start)*1000)` latency, final mirror equality/failure behavior, and D-17 exclusion of no-delivery resume exits.
- [x] **TAP-18 / pilot comparison RED:** Produce otherwise-comparable context-on/context-off run records keyed only by existing run/PR/head/profile/model coordinates; prove no finding identity/disposition field or matching primitive is introduced.
- [x] **R10 evidence checkpoint:** Preserve exact RED commands/failure reasons. Do not rewrite checked historical tasks or manufacture RED for already-correct strict GitHub/fence/opaque-insertion behavior.

### R11 — Bounded implementation and focused GREEN proof
- [x] **CLI/eligibility implementation:** Add paired `--pr-context` / `--no-pr-context` with default off; capture `default|cli_explicit`; preserve `--if-reviewed list|latest|prompt-selected` and `--no-review` as non-invocations with no context call/artifact/metadata; decide built-in eligibility before binding/calling fetch; bypass custom/unsupported review flows with stable metadata.
- [x] **Remote-only API implementation:** Remove local `find_past_reviews`, `sandbox_root`, `head_sha` local-history coupling, `past_reviews` result/artifact, and all local discovery/recovery behavior from current PR-context composition. Repurpose the focused corpus module for bounded remote selection; add no compatibility fallback that scans local state.
- [x] **Audit/model split implementation:** Preserve full normalized remote `discussion` unchanged and endpoint-ordered in result/audit artifact; create separate selected model-input copies. Footer/session/commit/similarity content remains ordinary data.
- [x] **Selection implementation:** Enforce the fixed estimator and 12,000/1,000/100 limits using canonical compact JSON and the literal prompt framing from `design.md`. Charge fixed instructions/framing/normalized PR stats first, apply D-13's exact valid/equal/offset-equivalent/missing/invalid timestamp total orders, evaluate each tentative model-input array as an exact complete prompt, stop at the first limit, and preserve complete audit evidence. Emit `bypassed/no_selected_context` without orientation when a nonempty corpus admits zero events.
- [x] **Output/injection implementation:** Enforce cap-aware finalized orientation output at 2,000 estimated tokens and independently enforce freshly injected `$PRIOR_CONTEXT` at 2,000 estimated tokens. Reserve canonical instructions/all five headings, preserve valid fence structure, and keep insertion opaque.
- [x] **Resume authority implementation:** Add one D-15 validator that reads originating session `meta.json::pr_context` before `work/pr_context_orientation.md`, returns exact content or `""` plus D-14 reason, and never repairs/truncates. Wire it into both `_resume_flow_impl` and `_run_incremental_completed_multipass_resume`; add exactly one owned `$PRIOR_CONTEXT` token to the incremental resume synth template and preserve existing verbose guidance. Neither route fetches/orients. Preserve the nested originating value without key/value mutation and skip the metadata mirror on D-17 no-delivery exits.
- [x] **Fail-open implementation:** Catch only named PR-context enrichment-stage failures, warn visibly, and record stable degradation. Retain blind draft on singlepass reconciliation failure. On a fresh or either resume context-specific multipass synth failure, flush `degraded/context_synthesis_failed`, retry once with empty context from the same existing plan/step outputs, and retain combined delivery latency and separate nullable delivery/fallback provider-usage fields on success. Propagate an empty-context retry failure without a final mirror and do not catch unrelated/process-control failures.
- [x] **Observability/persistence implementation:** Implement D-14 exact entered-path schema/reason precedence, D-16 sequencing, D-17 no-delivery scope, and D-18 field-level ownership. Assign fetched only after all endpoint arrays pass shape validation, normalized only after normalization, and selection counts only after selection; inherit sanitized acquisition provenance only from used origins while current resume delivery/result/mirror fields replace origin values. Atomically write required discussion/brief artifacts before delivery; flush authoritative session `meta.json::pr_context` at transitions including before/after successful fallback; write `work/pr_context_meta.json` once after final successful route completion (delivery, successful fallback, or bypass) telemetry. Mirror failure warns/no-retry and changes only persistence fields; failed ordinary fallback has no mirror; no-delivery resume preserves the origin value without key/value mutation and makes no mirror attempt; session flush failure propagates. Distinguish estimates from provider usage.
- [x] **TAP-14 focused GREEN:** Run CLI/default/eligibility/historical-selection/no-review/no-remote-data owners and confirm every pre-fetch bypass/non-invocation occurs before fetch/orientation and produces exactly its D-14/D-18-or-no-payload result.
- [x] **TAP-15/TAP-16/TAP-21 focused GREEN:** Run `python -m pytest tests/cure_pr_context tests/test_cure_github.py -v`; confirm D-13 selection/audit, output/template caps, raw shapes, strict adapter, and remote-only no-read behavior.
- [x] **TAP-17/TAP-18/TAP-22 focused GREEN:** Run `python -m pytest tests/test_cure_pr_flow.py -v`; confirm fresh delivery fallback, D-14/D-18 exact metadata, D-16 early/late persistence, D-17 no-delivery preservation, warnings, and unrelated-failure propagation.
- [x] **TAP-19/TAP-20 focused GREEN:** Run both exact resume commands in the owner registry; confirm shared validator matrix, real regular/incremental exact-or-empty captures, one-empty-retry success/failure semantics and metadata on both routes, no-delivery regular-resume preservation, and no resume network.

### R12 — Final gates, proof reconciliation, and opt-in pilot gate
- [x] **Focused proof gate:** Run TAP-14..TAP-22 using every exact command in `story.md::Verification Commands` and the R10 owner registry, plus retained TAP-01..TAP-13 owners compatible with FB-021–FB-024. Preserve exact summaries and identify intentionally removed local-history tests rather than weakening them silently.
- [x] **Quality/type gate:** Compute the merge base, enumerate every changed Python path, run Ruff on that complete list, and run mypy on the changed typed production boundary. Resolve all new findings.
- [x] **Complete-delta hygiene:** Run `base="$(git merge-base HEAD origin/main)"; git diff --check "$base" --`; do not substitute a bare unstaged-only check.
- [x] **Packaging/regression gate:** Rebuild/install the wheel in a disposable target, import `cure_pr_context` and `cure_github`, and run the full CURe suite with the approved timeout/fail-fast command.
- [x] **Structural traceability gate:** Validate S17–S31 each maps exactly once to A19–A28/A31–A34; every behavior in each scenario appears in its linked acceptance and proof variants; A19–A34 each has one complete seven-column APM row; TAP-14..TAP-23 IDs are unique/referenced with the ten required columns; every provisional row has nonblank open detail; FB-021–FB-024 and D-04–D-18 match initiative/proposal/design/tasks.
- [x] **Exclusion scan:** Prove no current normative or production path introduces finding identity/disposition, local history, authoritative resolution, freshness/cache, persistent memory, or richer reconciliation analytics.
- [x] **TAP-23 release gate:** Verify `--pr-context` remains default off and story completion text authorizes only an opt-in pilot. Record evidence dimensions without inventing arbitrary numerical quality thresholds.
- [x] **Coordination write-back:** Reconcile story/design/tasks/progress only from current evidence, append implementation/review records without rewriting historical logs, and leave default-on/general release to a separate operator-approved follow-up.

## FB-031–FB-033 R13 Remediation (current, unchecked)

### R13 — RED-first escaped-fault, proof-owner, and timestamp-order remediation

- [x] **R13 RED — genuine boundaries and owner collection:** Before production edits, add/reproduce (a) TAP-22 real `_pr_flow_impl` cases where unrelated `OSError`/`UnicodeError` from `build_pr_context` or raw orientation-file cleanup/read propagates instead of becoming `artifact_write_failed`; (b) TAP-15 parser-accepted aware minimum/maximum-year timestamp cases whose equivalent UTC wall-clock conversion overflows, asserting valid instant-correct admission/model order; and (c) canonical TAP-14/TAP-18/TAP-19 collection checks that include the actual paired parser owner, genuine singlepass and all D-18 metadata owners, `tests/cure_pr_context/test_runtime.py`, public non-used/invalid validator cases, and current regular-resume route owners. Preserve exact failing commands/reasons and do not manufacture RED for already-correct behavior.
- [x] **R13 GREEN — bounded catch/order repairs:** Narrow only the fresh PR-context catch attribution needed for FB-031 so required artifact-write failures still degrade under D-09/D-16 while unrelated construction/orientation-file faults propagate. Implement D-13's design-owned signed integer instant key without `astimezone(timezone.utc)`/platform timestamp conversion so every parser-accepted aware year-boundary value remains valid, total, and instant-correct. Repair only canonical TAP owner commands/names needed for FB-032; do not change acceptance, scope, metadata semantics, or default-off behavior.
- [x] **R13 final reconciliation — canonical proof and side-effect gates:** Run the corrected TAP-14/TAP-15/TAP-18/TAP-19/TAP-22 commands and every command under `story.md::Verification Commands`, then full regression, changed-path Ruff, scoped mypy, package smoke, complete merge-base `git diff --check`, and structural S17–S31 → A19–A34 → TAP/APM/D-09/D-13/D-14/D-17/D-18 validation. Restore A19/A21/A24/A25/A26/A31 maturity to `final` only when every named owner collects and passes, unrelated failures still propagate, required persistence failures still degrade, and no context/default/resume side effect regresses; reconcile story/design/tasks/progress from current evidence.

## Latest Implementation-Review Remediation (GATE-001, PRC-001, PRC-002)

### R14 — Canonical proof inventory, provenance labels, and D-15 ownership

- [x] **PRC-001 RED/GREEN:** Add a focused delivery-prompt assertion that both singlepass reconciliation and shared/incremental multipass templates identify the brief as selected-PR remote discussion and never as past CURe reviews; preserve the failing pre-edit result, then make the smallest heading-only repair and pass the template suite.
- [x] **PRC-002 sole-validator cleanup:** Remove dead `_read_persisted_pr_context_orientation()` so all live persisted-context decisions continue through `cure_pr_context.runtime.read_persisted_context()`; confirm no reference or duplicate persisted-brief reader remains.
- [x] **GATE-001 complete Python inventory:** Repair the canonical Ruff command to union merge-base tracked Python changes with untracked, non-ignored Python paths, excluding deleted paths; prove the inventory includes `cure_pr_context/runtime.py` and `tests/cure_pr_context/test_runtime.py` and run Ruff on the complete set.
- [x] **R14 final reconciliation:** Run focused prompt/runtime/flow proof, combined amended proof, full regression, complete-path Ruff, scoped mypy, package smoke, structural/proof reconciliation, and merge-base `git diff --check`; update coordination and return to review only if every gate passes.

## Historical Integration & Cleanup (checked under superseded contracts)
- [x] Ruff check + mypy clean in `cure_github.py` and `cure_pr_context/` (TAP-08)
- [x] Run full CURe test suite — verify zero regressions
- [x] `git status` — confirm only in-scope files created/modified

### Historical feedback-remediation final verification (complete under superseded contracts)
- [x] **R6 proof reconciliation:** After R1-R5 are GREEN, update provisional A2/A3/A4/A5/A8/A10/A12/A13 proof maturity/open details truthfully from current evidence; do not reuse pre-feedback final claims.
- [x] **R6 focused gate:** Run all TAP-01..TAP-07 and TAP-10 owning suites together and preserve the exact pass summary.
- [x] **R6 quality gate:** Run `ruff check cure_github.py cure_pr_context/` and `mypy cure_github.py cure_pr_context/`.
- [x] **R6 packaging gate:** Rebuild/install the wheel in a disposable target and import both `cure_pr_context` and `cure_github`.
- [x] **R6 regression gate:** Run `python -m pytest tests/ -x --timeout=120` and preserve the exact pass summary.
- [x] **R6 coordination/diff gate:** Reconcile `story.md`, `design.md`, `tasks.md`, and any delta specs against the implemented evidence; run structural traceability validation, `git diff --check`, and `git status --short`, leaving only truthful in-scope state.
