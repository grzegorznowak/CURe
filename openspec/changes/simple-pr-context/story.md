Plan: 🟢 PLAN APPROVED
Status: 🔄 IN PROGRESS

## Purpose

Deliver an opt-in pilot that gives supported built-in `cure pr` reviews a bounded orientation brief from the selected pull request's remote discussion. Preserve the complete normalized same-PR remote corpus unchanged for audit, but send only a deterministic bounded subset to the orientation model and inject only a bounded brief. If enrichment fails, warn and continue through the ordinary context-free review without swallowing unrelated review/process failures.

This amendment supersedes the prior automatic, unbounded, local-plus-remote, fail-hard contract. Completing this story enables only an opt-in pilot; default-on/general release requires a separate operator-approved follow-up based on evidence.

## Actors

- **Primary:** CURe operator, who explicitly enables or disables PR context.
- **Secondary:** review LLM, which consumes bounded context only in supported reconciliation/synthesis stages.
- **Affected:** PR authors/reviewers, whose same-PR remote discussion may influence the pilot review.
- **Reviewer/operator:** evaluates context-on/context-off evidence before any broader rollout.

## Triggering Need

Blind reviews can duplicate already-discussed concerns or ignore decisions recorded in PR discussion. The earlier plan overreached by coupling the MVP to local session history, feeding every remote event to the model, failing the whole review on enrichment errors, and enabling automatically. The approved P0 amendment bounds cost/context, removes local history from the MVP, adds operator control/fail-open behavior, and defines minimal pilot evidence.

## Expected Prerequisites

- Existing strict GitHub list adapter and three selected-PR endpoint normalizer.
- Existing built-in singlepass draft/reconcile and multipass plan/step/synth execution boundaries.
- No local CURe session/history prerequisite.

## Scope

- Add paired Boolean CLI control `--pr-context` / `--no-pr-context`, default off.
- Decide eligibility before fetch; custom prompts/files and unsupported profiles bypass without discussion/orientation calls.
- Treat `--if-reviewed list|latest|prompt-selected` returns and `--no-review` as non-review routes outside the PR-context invocation boundary, even when `--pr-context` is supplied: perform no discussion/orientation call, create no PR-context artifact, and create no new `pr_context` metadata record.
- Fetch only selected-PR issue comments, reviews, and inline review comments.
- Preserve every normalized remote event unchanged and ordered in the audit result/artifact.
- Select orientation input deterministically within the budget remaining under the fully assembled orientation-generation prompt cap.
- Produce and inject a bounded five-section orientation brief.
- Fail open visibly at PR-context enrichment boundaries.
- Isolate Codex orientation in one fresh invocation-scoped runtime root containing an isolated `CODEX_HOME`, CURe raw/display event logs, and orientation-only progress metadata; keep the ordinary session logs/meta orientation-free, remove the whole runtime root after orientation, and prove its absence before every fresh blind/context-free agent launch. Runtime-root cleanup or shared-log/meta isolation failure launches no agent.
- Record stable pilot metadata and support otherwise-comparable context-on/context-off runs.
- Remove current local session/history discovery, local review inputs/artifacts, filesystem coupling, and local-history corruption semantics.
- Keep existing strict GitHub array/page behavior, fence-aware orientation finalization, opaque insertion, packaging, and code-evidence-wins reconciliation where compatible with this amendment.

## Out of Scope

- Finding identity or disposition model
- Blind-finding suppression primitives or semantic arbitration
- Local CURe session/history discovery, recovery, or migration
- Authoritative resolution/thread state
- Freshness digests, caching, or persistent review memory
- Richer reconciliation analytics
- Default-on/general release
- Arbitrary pilot quality thresholds
- Live GitHub/network tests
- FB-010 terminal two-attempt exception provenance

## Concrete Defaults

| Limit | Value |
|------|-------|
| Fully assembled orientation-generation prompt | 12,000 estimated tokens |
| Per-selected-event body | 1,000 estimated tokens |
| Selected events | 100 maximum |
| Orientation output | 2,000 estimated tokens |
| Injected `$PRIOR_CONTEXT` | 2,000 estimated tokens |
| Paginated discussion-list public fallback request timeout (`_github_public_api_list` only) | 30.0 seconds per request |
| Paginated discussion-list public fallback pagination (`_github_public_api_list` only) | 100 pages maximum |

Estimated tokens are deterministic `ceil(len(text) / 4)` values over Unicode code points. The 12,000 value covers fixed orientation instructions, literal framing, normalized PR stats, and compact deterministic JSON for selected normalized event records; selection receives only the exact remaining budget after that overhead. Provider usage, when available, is separate actual usage and must never be labeled as an estimate.

## Scenarios / Behavior Examples

> **ID continuity:** S1-S16 describe the pre-amendment implementation/review history and are superseded as current normative scenarios where they require local history, unbounded input, automatic enablement, or fail-hard enrichment. Current scenarios begin at S17.

### S17 — Default-off and explicit operator control
- Given: a normal built-in `cure pr` review with no PR-context option.
- When: routing begins.
- Then: no discussion fetch or orientation call occurs; metadata says `bypassed/disabled_default`, enablement source `default`, and the ordinary review runs. `--pr-context` explicitly enables an eligible flow; `--no-pr-context` explicitly bypasses with `disabled_cli`.
- Covers: A19

### S18 — Unsupported/custom flow bypasses before fetch
- Given: `--pr-context` with a custom prompt, custom prompt file, or unsupported prompt/profile flow.
- When: eligibility is evaluated.
- Then: enrichment is bypassed before any discussion/orientation call, a stable reason is recorded, and ordinary operator-owned prompt behavior remains unchanged.
- Covers: A20

### S19 — Full audit corpus, bounded deterministic model input
- Given: endpoint events that approach/exceed the 12,000-estimated-token fully assembled prompt cap or the 100-event cap, including bodies over 1,000 estimated tokens and equal, offset-equivalent, missing, or invalid timestamps.
- When: context is built.
- Then: the complete normalized remote corpus remains byte-for-byte field-equivalent and endpoint-ordered in the public result and `work/pr_context_discussion.json`. Fixed instructions/framing/stats are charged first; candidates follow D-13's total order, stop at the first full-prompt/count limit, cap only selected body copies, and restore D-13 chronological order. Exact counts, estimates, and body/count/prompt truncation flags match the deterministic oracle.
- Covers: A21

### S20 — Output and injection are independently bounded
- Given: fresh orientation output, persisted orientation input, or fresh delivery context is at cap, one code point over cap, or contains headings/fences at different indentation levels, marker types, and closing-run lengths.
- When: fresh finalization or persisted-brief validation occurs.
- Then: finalized orientation output and fresh injection independently fit 2,000 estimated tokens. Fresh and persisted validation share one structural Markdown contract: only constructs with 0–3 leading spaces are structural; 4+ spaces are indented-code/nonstructural; every required heading is a real H2 outside fences; and a closing fence uses the opening backtick/tilde marker with a run at least as long as the opener. The finalized brief retains canonical usage instructions, all five headings, and valid fence structure; exact output/injection estimates and distinct truncation flags are recorded.
- Covers: A22

### S21 — Remote same-PR-only MVP
- Given: matching or corrupt local CURe sessions exist and remote bodies resemble local reviews.
- When: context is built.
- Then: no local session root or historical review file is scanned/read; only events from the selected PR's three endpoints enter the audit/selection boundary; footer/session/commit/body similarity cannot alter remote membership.
- Covers: A23

### S22 — Enrichment persistence respects the delivery boundary
- Given: eligible context is enabled and fetch, selection, orientation, phase-correct audit/brief publication or absence enforcement, or the final best-effort metadata mirror fails.
- When: `cure pr` runs.
- Then: failures before delivery warn, record D-14's first failing enrichment reason as `degraded`, and continue context-free only after D-16 establishes that no nonempty discussion/raw-orientation/canonical-orientation artifact is available to that agent call. A successful multipass `plan.abort` runs its plan process under the same absence boundary, writes the abort review without another agent call, then publishes only the complete audit, finalizes authoritative `bypassed/plan_aborted` metadata and its one best-effort mirror, and returns without publishing a reusable brief. Nonempty audit persistence is delayed until immediately before a permitted context-bearing call or until after all review agent calls on branches that remain context-free. A post-route `work/pr_context_meta.json` failure warns once, records `persistence.meta_artifact=failed` in authoritative `meta.json::pr_context`, retains the already determined used/bypassed/degraded outcome and all delivery telemetry, does not rerun review, and is not retried. A post-provider ordinary review-output validation failure is not an enrichment failure and is never eligible for an empty-context retry; it propagates under existing review semantics after current usage/latency and `degraded/delivery_validation_failed` are made authoritative for an entered resume delivery. Core session `meta.json` flush failures and unrelated review/process failures retain existing propagation.
- Covers: A24

### S23 — Branch-specific delivery fails open
- Given: a blind singlepass draft or context-free multipass plan/steps have completed in a fresh, regular-resume, or completed-session incremental-resume route, including a fresh multipass plan whose output fails parsing and falls back to singlepass.
- When: singlepass context reconciliation fails, or the first fresh/resume synth provider call fails specifically while non-empty prior context is supplied.
- Then: singlepass runs and retains the ordinary blind draft while complete nonempty discussion/finalized orientation remain only in coordinator memory, all discussion/raw-orientation/canonical-orientation artifact paths are absent, and every Codex orientation rollout, raw/display event log, and live-progress record is confined to one invocation-scoped runtime root that has already been removed and absence-proved. The ordinary session's `work/logs/codex.events.jsonl`, `work/logs/codex.log`, and `meta.json::{last_cmd,live_progress}` contain no orientation sentinel; only afterward does `_pr_flow_impl` atomically publish complete discussion plus the finalized brief immediately before reconciliation. On plan-parse fallback, both the preceding plan process and the subsequent singlepass blind draft satisfy that same prompt/filesystem blindness, and publication still occurs only before reconciliation. For fresh multipass, the same in-memory values remain unpublished, every raw orientation output is removed, and all three artifact paths stay absent from before plan starts through every plan/step process. Plan/steps also receive no discussion/orientation bytes or paths in rendered prompts. Only after all plan/steps succeed does `_pr_flow_impl` atomically publish complete discussion plus the finalized brief immediately before context-bearing synth. If either pre-context publication fails, it records `degraded/artifact_write_failed`, removes any partial nonempty publication, proves all three paths absent, and runs synth context-free from the same successful plan/step outputs without a context-bearing attempt; inability to establish absence propagates without launching synth. Every context-bearing multipass route warns, records `degraded/context_synthesis_failed`, and retries synthesis exactly once with empty prior context using the same successful plan/step outputs; fresh flow first removes its published nonempty PR-context artifacts and proves all three paths absent before that retry. A successful retry records `context_mode=off`, combined delivery latency, and separate nullable delivery/fallback provider-usage fields; failure of that context-free synthesis propagates while authoritative session metadata retains the degradation reason and any available attempt telemetry. Ordinary validation failures after a provider returns are not provider-call failures and propagate without an empty-context retry.
- Covers: A25

### S24 — Pilot records support context-on/off comparison
- Given: otherwise comparable runs of the same PR/head/profile/model with `--pr-context` and `--no-pr-context`, including either resume route entering delivery and then encountering an ordinary post-provider validation failure.
- When: the operator evaluates the pilot.
- Then: the exact D-14/D-18 metadata schema, path, and field-ownership oracle distinguishes outcome/reason, enablement/eligibility, counts, estimates, nullable provider usage, truncation, persistence, and stage latency. Once either resume route enters delivery, its sanitized current resume record is authoritative rather than the raw originating record; on ordinary post-provider validation failure it retains current delivery usage/latency, records `degraded/delivery_validation_failed`, propagates the original failure, performs no empty retry and no final mirror, while retaining originating acquisition leaves only where D-18 already requires them. Existing run coordinates plus `context_mode` permit review-output comparison for duplicate/context-invalid comments without finding IDs, dispositions, or matching primitives.
- Covers: A26

### S25 — No-data enabled run
- Given: an eligible `--pr-context` run whose three endpoint arrays normalize to zero events.
- When: context composition begins.
- Then: no orientation call occurs; authoritative metadata says `bypassed/no_remote_context`; ordinary review proceeds context-free; `work/pr_context_discussion.json` is the complete empty audit array and `work/pr_context_meta.json` mirrors the final metadata when writable.
- Covers: A27

### S26 — Pilot completion does not enable general release
- Given: all implementation/proof tasks for this story pass.
- When: the story is completed.
- Then: CLI default remains off. Any default-on/general release change requires a separate operator-approved proposal using collected evidence; no invented numerical threshold silently authorizes rollout.
- Covers: A28

### S27 — Both resume routes trust only an originating used outcome
- Given: either regular `_resume_flow_impl` synthesis or completed-session incremental `_run_incremental_completed_multipass_resume` synthesis with persisted PR-context state.
- When: the route reads originating session `meta.json::pr_context` and `work/pr_context_orientation.md`.
- Then: both routes reuse the exact brief only for `outcome == "used"` plus readable UTF-8, nonempty, structurally valid, at-most-2,000-token content; all other metadata/brief variants inject `""`, record D-14's resume reason, perform no discussion fetch/orientation, and leave no raw `$PRIOR_CONTEXT` token.
- Covers: A31

### S28 — Nonempty remote audit can select no model context
- Given: an enabled eligible run with a nonempty normalized endpoint corpus whose first newest-first candidate cannot fit after fixed prompt/framing overhead.
- When: bounded selection runs.
- Then: the complete nonempty audit corpus is preserved unchanged in coordinator memory, no event is selected, no orientation call occurs, metadata says `bypassed/no_selected_context`, and ordinary review proceeds context-free while every nonempty PR-context artifact is absent; only after all review agent calls does `_pr_flow_impl` atomically persist the complete audit artifact. This is distinct from zero normalized events, which remains `bypassed/no_remote_context`.
- Covers: A32

### S29 — Resume with no new delivery preserves origin evidence
- Given: `_resume_flow_impl` reaches either the completed/same-head fast no-op or a regular reusable-review-artifact branch that skips synthesis.
- When: the resume completes without reconciliation or synthesis and therefore creates no new PR-context delivery decision.
- Then: the originating session `meta.json::pr_context` value is preserved deep-equal with no key/value mutation, no new `work/pr_context_meta.json` mirror attempt occurs, and lifecycle timestamps/status may change without misclassifying the existing review artifact as a new context-on/off run.
- Covers: A26

### S30 — Historical-review selection exits before PR-context work
- Given: completed sessions exist and `cure pr --pr-context` is invoked with `--if-reviewed list`, `--if-reviewed latest`, or interactive `--if-reviewed prompt` where the operator selects an existing review.
- When: `_pr_flow_impl` serves the historical-session action.
- Then: it returns before sandbox/session creation, performs no discussion fetch or orientation call, writes no PR-context artifact or metadata, and does not mutate the selected historical session.
- Covers: A33

### S31 — `--no-review` never enriches
- Given: `cure pr --no-review --pr-context` proceeds through its existing session/index-only route.
- When: `_pr_flow_impl` reaches review routing.
- Then: it performs no discussion fetch, selection, orientation, reconciliation, or synthesis; writes no `work/pr_context_*` artifact and no `meta.json::pr_context`; and preserves the existing `--no-review` completion behavior.
- Covers: A34

### S32 — Bounded paginated discussion-list public GitHub fallback
- Given: existing authentication eligibility permits `gh_api_list(..., allow_public_fallback=True)` to invoke `_github_public_api_list` for a selected `github.com` PR discussion endpoint and the public response chain is empty, multi-page, malformed, cyclic, unsafe, over budget, or fails at any request/decoding stage. The separate `_github_public_api_json` / `gh_api_json` PR-metadata fallback is unchanged and outside D-19.
- When: the strict GitHub discussion-list adapter follows public pagination.
- Then: D-19 applies a 30.0-second timeout to every request, uses an explicit no-redirect opener that rejects every redirect response before follow-up, resolves `next` against the current page, validates the effective target as exactly HTTPS `api.github.com` port 443 with no userinfo or fragment before requesting it, rejects malformed/non-HTTP(S)/cross-origin URLs and direct or multihop canonical repeats before request, and requests at most 100 pages. A terminal page 100 succeeds; a page-100 `next` fails before page 101. Strict array pages are accumulated in page order only when the entire chain completes. Any timeout, redirect, unsafe next, cycle, malformed URL, page-budget, HTTP, network, JSON, or non-array failure raises through the fetch boundary, producing `degraded/fetch_failed`, discarding every partial page, writing no discussion or orientation artifact, and continuing the review context-free. No live-network proof is required.
- Covers: A29

## Acceptance

> **ID continuity:** A1-A18 and their TAP-01..TAP-13 evidence remain historical implementation records. They are not current authority where they require local history, complete remote model input, automatic activation, or fail-hard enrichment. Current amended acceptance begins at A19.

- A19: **Boolean control/default:** `cure pr` exposes paired `--pr-context` / `--no-pr-context`; omission defaults off. Metadata records `enablement_source=default|cli_explicit`. Disabled paths perform no discussion/orientation call.
- A20: **Eligibility before I/O:** custom prompt, custom prompt-file, and unsupported profile flows bypass before fetch/orientation with stable reasons and ordinary context-free behavior.
- A21: **Deterministic bounded orientation prompt:** the complete normalized selected-PR remote corpus remains field-equivalent and endpoint-ordered in the public result and `work/pr_context_discussion.json`; selection mutates only body fields on model-input copies. The exact fully assembled orientation-generation prompt—including fixed instructions, literal framing, normalized PR stats, and compact deterministic selected-event JSON—fits 12,000 estimated tokens. Event bodies fit 1,000 estimated tokens, at most 100 events are admitted using D-13's valid/equal/missing/invalid timestamp order and stop-at-first-limit rule, and selected records use D-13 chronological order. Exact selected/omitted/body-truncated counts, full-prompt estimate, and body/count/prompt flags are reproducible at cap-1/cap/cap+1.
- A22: **Output/injection caps and shared Markdown structure:** finalized orientation output and freshly injected `$PRIOR_CONTEXT` each independently fit 2,000 estimated tokens at cap-1/cap/cap+1. Fresh finalization and persisted validation use the same structural grammar: only 0–3 leading spaces can introduce a structural heading or fence; 4+ leading spaces are indented-code/nonstructural; every required heading is an H2 outside fences; and a closing fence matches the opening backtick/tilde marker with a run at least as long as the opener. A non-empty finalized brief retains canonical usage instructions, all five required headings, and valid fence structure. Exact orientation-output/injected estimates and separate orientation-output/injected truncation flags are recorded.
- A23: **Remote-only composition:** no production PR-context path scans local CURe sessions/history or returns local past reviews. Endpoint membership alone defines the complete remote audit corpus; no footer/session/head/similarity parsing controls membership.
- A24: **Phase-correct persistence fail-open:** fetch, selection, orientation, phase-correct discussion/brief publication, and pre-agent absence-enforcement failures warn and record `degraded` with D-14's first-failure reason. Codex orientation runs under one fresh invocation-scoped runtime root whose isolated `CODEX_HOME` contains only required regular-file auth/config inputs and no inherited sessions, and whose separate CURe event/display logs plus dedicated `SessionProgress` receive every orientation-local JSON event, display message, command record, and live-progress update. The ordinary session progress and shared Codex logs receive no orientation bytes. After provider output/usage ingestion the whole runtime root is removed and its nonexistence is rechecked before every blind/context-free launch. Runtime-root cleanup/absence or shared-log/meta isolation failure propagates without launching an agent and is not converted into a context-free degradation. A successful multipass `plan.abort` runs its plan agent while all three nonempty paths are absent, then writes the abort review, publishes only the complete audit after that final agent call, records authoritative `bypassed/plan_aborted` with current acquisition/persistence telemetry, attempts the final mirror once, and returns without a reusable orientation artifact. A fresh context-free agent call occurs only after all nonempty discussion/raw-orientation/canonical-orientation artifacts are absent; inability to establish absence propagates without launching it. Nonempty no-selected/post-build-degraded audits are written only after all review agent calls; a post-agent audit failure never reruns review and either becomes `artifact_write_failed` or retains an earlier first degradation reason with failed persistence telemetry. Final `work/pr_context_meta.json` mirror failure warns once, is not retried, records `persistence.meta_artifact=failed` in authoritative session `meta.json::pr_context`, and retains the already determined route outcome, context mode, review artifact, usage, and latency without rerunning delivery. Post-provider ordinary output validation is not empty-context-fallback eligible; on an entered resume delivery it records current usage/latency and `degraded/delivery_validation_failed`, then propagates the original failure with no final mirror. Core `meta.json` flush plus unrelated checkout, prompt/profile, cancellation, ordinary review, output/posting, and process-control failures preserve existing propagation.
- A25: **Delivery fail-open and fresh blindness:** built-in singlepass reconciliation failure retains the ordinary blind draft. Fresh blindness covers actual prompt/process/filesystem capability, including provider-managed transcripts and CURe-owned event/display/live-progress persistence: orientation prompt/response bytes may exist locally only inside the invocation-scoped orientation runtime root while orientation runs, must not enter the inherited/default Codex sessions root or ordinary session logs/meta, and that entire runtime root must be absent before every blind/context-free review-agent launch. A multipass plan-parse fallback keeps all three nonempty PR-context paths absent and exposes no discussion/orientation bytes or paths through both the plan process and the fallback singlepass blind draft; only then may it publish discussion plus brief immediately before reconciliation. Complete nonempty discussion and finalized orientation remain only in coordinator memory while the blind draft or multipass plan/steps run; raw orientation output is removed, and `work/pr_context_discussion.json`, `work/pr_context_orientation.raw.md`, and `work/pr_context_orientation.md` are absent before and throughout each such process; rendered prompts expose no discussion/orientation bytes or paths. Immediately before the first permitted context-bearing reconciliation/synth, `_pr_flow_impl` publishes complete discussion and canonical orientation using atomic replacement for each file. A pre-context publication failure records `degraded/artifact_write_failed`, removes partial nonempty artifacts, and executes context-free from the same successful outputs only after proving all three paths absent; absence failure propagates without launching an agent. In fresh, regular-resume, and completed-session incremental-resume routes, only a provider-call synthesis failure attributable to non-empty prior context records `degraded/context_synthesis_failed` and adds exactly one empty-context synth-stage invocation from the same successful plan/step outputs, with no second PR-context fallback; fresh flow removes published nonempty PR-context artifacts and proves absence before that invocation. Successful retry records `context_mode=off`, combined delivery latency, and separate nullable delivery/fallback provider-usage fields; failure of the context-free retry propagates while authoritative session metadata retains the degradation reason and available attempt telemetry. Ordinary validation after provider return propagates without empty retry.
- A26: **Stable pilot observability:** every fresh or resume route that enters a PR-context delivery decision records the exact D-14/D-18 metadata schema and deterministic path/reason/field-ownership oracle, including outcome `used|bypassed|degraded`, reason, enabled/eligible/source, counts, estimates, nullable provider usage, truncation, persistence, stage latency, and `context_mode`. Once either resume route enters delivery, the sanitized current resume dictionary is authoritative. A post-provider ordinary validation failure retains current delivery usage/latency, records `degraded/delivery_validation_failed`, propagates the original failure, performs no empty retry or final mirror, and retains originating acquisition leaves only where D-18 already specifies inheritance. Completed/same-head fast no-op and reusable-review-artifact branches that perform no synthesis/reconciliation are not new PR-context invocations: they preserve the originating `meta.json::pr_context` value deep-equal with no key/value mutation and make no new metadata-mirror attempt. Existing PR/head/profile/model/run coordinates plus `context_mode` support context-on/off review-output comparison; estimates are never provider usage, and no finding identity/disposition/matching field is introduced.
- A27: **No-remote-data bypass:** an enabled eligible run whose three endpoint arrays normalize to zero events skips orientation, records D-14's `bypassed/no_remote_context` values, writes the complete empty `work/pr_context_discussion.json`, proceeds context-free, and mirrors final metadata to `work/pr_context_meta.json` when writable under D-16.
- A28: **Release gate:** final story state leaves the CLI default off and documents that default-on/general release requires a separate operator-approved evidence review. No numerical quality threshold is invented by this story.
- A29: **Regression preservation and bounded paginated discussion-list public fallback:** strict GitHub list decoding/pagination, complete remote audit ordering, fence-aware orientation finalization, opaque insertion, packaging, and context-free built-in review behavior remain covered where not superseded. D-19 applies only when `gh_api_list(..., allow_public_fallback=True)` reaches `_github_public_api_list` for the three discussion endpoints on `github.com` after existing authenticated list-route eligibility; `_github_public_api_json` / `gh_api_json` PR-metadata fallback behavior is unchanged and outside D-19. Every in-scope list page request has a 30.0-second timeout; an explicit no-redirect opener rejects every redirect response before follow-up; each `next` resolves against the current page and is rejected before request unless its effective origin is exactly HTTPS `api.github.com` port 443 with no userinfo or fragment. Malformed/non-HTTP(S)/cross-origin targets and direct or multihop repeats of canonical requested URLs are rejected before request. At most 100 pages are requested: terminal page 100 succeeds, but `next` on page 100 fails before page 101. Strict JSON arrays retain page order only after the full chain succeeds; timeout, redirect, unsafe-next, cycle, malformed URL, page-budget, HTTP, network, JSON, or non-array failure raises through fetch, discards partial pages, becomes warning plus `degraded/fetch_failed`, and yields no discussion or orientation artifact before context-free review. Deterministic fakes suffice; no live-network proof is required.
- A30: **Explicit exclusions:** no finding identity/disposition primitive, local history, authoritative resolution, freshness/cache, persistent memory, or richer reconciliation analytics is introduced.
- A31: **Resume persisted-context authority:** regular `_resume_flow_impl` and completed-session incremental `_run_incremental_completed_multipass_resume` reuse `work/pr_context_orientation.md` if and only if originating session `meta.json::pr_context.outcome == "used"` and the exact brief is readable UTF-8, nonempty, structurally valid under A22's shared 0–3-space/H2/fence-marker grammar, and within 2,000 estimated tokens. Missing/bypassed/degraded/legacy/other metadata or missing/malformed/non-UTF-8/invalid/over-cap brief injects `""`, records the exact resume bypass reason, performs no discussion fetch/orientation, and leaves no raw template token. Once either route enters delivery, its sanitized current resume record becomes authoritative; ordinary post-provider validation failure follows A24/A26 and never restores the origin record.
- A32: **No-selected-context bypass:** a nonempty normalized corpus with zero admitted events preserves its complete audit result in coordinator memory, skips orientation, records D-14's exact `bypassed/no_selected_context` counts/context-mode values, proceeds context-free with every nonempty PR-context artifact absent, atomically writes `work/pr_context_discussion.json` only after all review agent calls, and mirrors final metadata under D-16; `no_remote_context` remains reserved for zero normalized events.
- A33: **Historical-selection non-invocation:** with explicit `--pr-context`, each completed-session `--if-reviewed list|latest|prompt-selected` return occurs before sandbox/session creation, makes no discussion/orientation call, writes no PR-context artifact or metadata, and leaves historical sessions unchanged.
- A34: **No-review non-invocation:** with explicit `--pr-context`, the `--no-review` route preserves its existing session/index-only completion while making no discussion/selection/orientation/reconciliation/synthesis call and writing neither `work/pr_context_*` nor `meta.json::pr_context`.

## Verification

### Verification Commands

- Parser and runtime routing: `python -m pytest tests/test_cure_pr_flow.py::test_parser_pr_context_is_paired_and_defaults_off -v`; `python -m pytest tests/test_reviewflow_unittest.py -k 'pr_flow_prior_review or pr_flow_no_review_skips_review_only_setup' -v`; and `python -m pytest tests/test_cure_pr_flow.py -v`
- Remote normalization, selection, orientation, artifacts, and adapter regression: `python -m pytest tests/cure_pr_context tests/test_cure_github.py -v`
- Exact persisted-context validation and regular/incremental resume orchestration owners: `python -m pytest tests/cure_pr_context/test_runtime.py tests/test_cure_pr_flow.py -v`; and `python -m pytest tests/test_reviewflow_unittest.py -k '(resume_flow_from_synth and (pr_context or prior_context)) or (incremental_completed_resume and pr_context)' -v`
- Combined amended proof: `python -m pytest tests/cure_pr_context tests/test_cure_github.py tests/test_cure_pr_flow.py tests/test_reviewflow_unittest.py -q`
- Full regression: `python -m pytest tests/ -x --timeout=120`
- Changed-path Ruff: `base="$(git merge-base HEAD origin/main)"; mapfile -d '' -t py_paths < <({ git diff --name-only --diff-filter=ACMR -z "$base" -- '*.py'; git ls-files --others --exclude-standard -z -- '*.py'; } | sort -zu); ((${#py_paths[@]} == 0)) || ruff check "${py_paths[@]}"`
- Typed production boundary: `mypy cure_github.py cure_output.py cure_pr_context/`
- Package smoke: `tmp="$(mktemp -d)"; python -m build --wheel --outdir "$tmp/dist" && python -m pip install --no-deps --target "$tmp/site" "$tmp"/dist/*.whl && PYTHONPATH="$tmp/site" python -c 'import cure_github, cure_pr_context'`
- Complete-delta hygiene: `base="$(git merge-base HEAD origin/main)"; git diff --check "$base" --`
- Structural reviewer action: inspect S17-S32 → A19-A34 → TAP/APM rows plus D-09/D-13/D-14/D-17/D-18/D-19/D-20 and confirm every matrix variant—including shared Markdown indentation/fences, fresh blind/context-free prompt+capability blindness across every nonempty PR-context artifact, the isolated orientation runtime root, and ordinary session Codex logs/live-progress metadata (including plan-parse fallback and successful plan abort), plan-abort audit/metadata early-return completion, both-resume post-provider validation, `_github_public_api_list` timeout/no-redirect/origin/cycle/page-budget/transactional failures, unrelated build/orientation faults, and parser-accepted aware year-boundary timestamps—has exactly one owning executable row before changing proof maturity.

### Feedback Source and Disposition

- **FB-019:** endpoint-owned remote membership; retained for full audit membership.
- **FB-020:** complete remote corpus retained for audit; superseded only in its former implication that all events enter model input.
- **FB-021:** bounded fully assembled orientation prompt/body/count/output/injection contract; current.
- **FB-022:** remote same-PR-only MVP; current and supersedes local corpus obligations.
- **FB-023:** paired opt-in/default-off control, used-outcome-gated resume, and visible fail-open behavior; current and supersedes automatic/fail-hard obligations.
- **FB-024:** stable pilot observability and separate general-release gate; current.
- **FB-031:** escaped fail-open catch-width recurrence; require a genuine `_pr_flow_impl` fault seam proving unrelated `OSError`/`UnicodeError` from context construction or orientation-file handling propagates rather than becoming `artifact_write_failed`.
- **FB-032:** canonical proof-owner correction; TAP-14, TAP-18, and TAP-19 commands and owner lists must collect the actual paired parser, singlepass delivery, D-18 metadata, persisted-context validator, and current regular-resume route owners before affected proof can return to final.
- **FB-033:** parser-accepted timezone-aware year-boundary timestamps remain valid even when direct UTC conversion overflows; D-13 ordering and TAP-15 must prove a total, instant-correct, overflow-safe order without reclassifying them invalid.
- **FB-034:** fresh multipass plan/steps must be blind to persisted orientation in both prompt content and process/filesystem capabilities.
- **FB-036:** fresh finalization and persisted validation share one structural Markdown grammar for 0–3-space structural constructs, 4+-space indented code, real out-of-fence H2 headings, and same-marker adequate-length fence closes.
- **FB-037:** D-19 defines behavior-centered, bounded, same-origin public pagination only for the discussion-list `_github_public_api_list` fallback, with per-page-request timeout, pre-request safety/cycle/page-budget checks, transactional arrays, and fetch-boundary degradation; the separate public JSON metadata helper is unchanged, and no live-network proof is required.
- **FB-038:** a sanitized current resume record becomes authoritative on delivery entry; ordinary post-provider validation failure preserves current attempt telemetry, records `delivery_validation_failed`, propagates, and neither retries empty nor mirrors final state.
- **GATE-001:** latest implementation review found the canonical Ruff inventory omitted untracked Python owners; the gate now unions merge-base tracked changes with untracked, non-ignored Python paths before Ruff.
- **PRC-001:** latest implementation review found remote-only orientation mislabeled as including past CURe reviews; all singlepass/shared multipass delivery headings must identify selected-PR remote discussion only.
- **PRC-002:** latest implementation review found a dead persisted-orientation reader bypassing D-15; remove it so `cure_pr_context.runtime.read_persisted_context()` remains the sole validation authority.
- **FB-010:** terminal retry provenance remains deferred.

### Fail-open Checks

| Render / route | Enabled activation proof | Disabled/fallback proof | Raw-token / swallowed-failure guard |
|---|---|---|---|
| Fresh built-in singlepass (`_pr_flow_impl` → `_reconcile_prior_context`) | blind draft launches with all three nonempty artifact paths absent; exact audit/brief publish only before reconciliation, where brief appears once | default/explicit-off/custom/unsupported bypass; publication/reconciliation failure retains ordinary draft without another agent call | no unresolved `$PRIOR_CONTEXT`; all-path snapshot/read bombs; blind-draft/acceptance and cleanup failures still propagate |
| Fresh built-in multipass (`_pr_flow_impl` → `_render_synth_prompt_with_prior_context` → `mrereview_gh_local_big_synth.md`) | nonempty brief is opaquely inserted exactly once only at synth; plan/steps have no prompt bytes/path and run while discussion/raw/canonical artifacts do not exist | after successful steps, exact audit/brief publish before synth; pre-context write or context-provider failure reuses successful outputs for empty synth only after all three paths are absent | no raw `$PRIOR_CONTEXT`; all-path snapshot/read bombs; cleanup/fallback synth failure propagates; ordinary post-provider validation propagates without empty retry |
| Regular resume (`_resume_flow_impl`, shared synth template) | D-15 used-valid persisted brief is inserted byte-for-byte; delivery entry makes the sanitized current resume record authoritative | every non-used/invalid variant injects `""`; a used-valid provider-call context synth failure gets exactly one empty retry from unchanged plan/steps | no raw token/network; successful fallback is `degraded/context_synthesis_failed` with `context_mode=off`; fallback failure propagates; ordinary validation records `delivery_validation_failed` with current telemetry, propagates, and does not retry/mirror |
| Completed-session incremental resume (`_run_incremental_completed_multipass_resume`, `mrereview_gh_local_big_resume_synth.md`) | D-15 used-valid brief is inserted byte-for-byte in the incremental synth template; delivery entry makes the sanitized current resume record authoritative | every non-used/invalid variant injects `""`; a used-valid provider-call context synth failure gets exactly one empty retry from unchanged plan/steps | template gains exactly one owned token; no raw token/network; fallback and ordinary-validation telemetry/propagation rules match the regular route |
| Resume no-delivery branches | no activation: completed/same-head fast no-op and regular reusable-review-artifact branch execute no synth/reconcile | originating `meta.json::pr_context` remains deep-equal without key/value mutation and no new metadata mirror is attempted | D-17 prevents stale origin evidence from being relabeled as a new context-on/off invocation |
| Historical-selection exits | no activation: `--if-reviewed list|latest|prompt-selected` returns before sandbox/session creation even with `--pr-context` | historical output/list behavior remains unchanged; no PR-context call, artifact, metadata, or historical-session mutation | these are not review invocations and therefore do not enter D-14 |
| `--no-review` | no activation even with `--pr-context` | existing session/index-only completion remains unchanged; no fetch/select/orient/reconcile/synth or PR-context artifact/metadata | route is outside D-14 because no review delivery can occur |
| Metadata mirror | final route metadata is written once after successful delivery, successful fallback, or bypass telemetry is known | mirror failure is warning-only and not retried; authoritative session metadata retains route and persistence failure | no attempt on D-17 no-delivery, A33/A34 non-invocations, or failed ordinary fallback; session `meta.json` flush failure remains process-control propagation |

### Input Boundary Shape Risk

| Boundary | Raw Input Source | Strict Assumption | Variant / Case | Evidence | Mitigation / Exclusion |
|---|---|---|---|---|---|
| GitHub list adapter | `gh api --paginate` / `_github_public_api_list` bytes, redirect responses, and `next` URLs | each document/page is an array of objects; the in-scope paginated list chain rejects redirects and obeys D-19 before each request | empty array, blank, malformed JSON, scalar/object, mixed pages, multi-page arrays; 3xx with same-/cross-origin/relative `Location`; relative next, userinfo/fragment, malformed/non-HTTP(S)/cross-origin, direct/multihop cycle, timeout/HTTP/network failure, terminal/overflowing page 100 | TAP-21 at `tests/test_cure_github.py` and `tests/cure_pr_context/test_fetcher.py` | explicit no-redirect opener; strict transactional adapter; canonical requested-URL set; 30.0s/request; 100 pages; deterministic fake HTTP only, no live network |
| Three endpoint items | issue comments, reviews, inline comments | normalized object fields without changing endpoint membership | missing user/body/id, review submitted time, inline line/path, footer/session-like text | TAP-21 | preserve complete endpoint-owned audit membership and field normalization |
| `created_at` ordering | normalized external timestamp strings | every nonempty timezone-aware value accepted by `datetime.fromisoformat(value.replace("Z", "+00:00"))` is a valid sort instant | `Z`, equal instants expressed with different offsets, equal timestamp, parser-accepted aware minimum/maximum-year values whose direct UTC conversion would underflow/overflow, naive, malformed, empty/missing | TAP-15 | D-13 overflow-safe total order; parser-accepted aware values are never reclassified invalid and the raw value remains unchanged in audit/model copy |
| PR stats / prompt assembly | computed mapping and selected normalized records | JSON-safe finite values and exact canonical serialization | absent stats, Unicode, sorted nested keys, `NaN`/non-serializable values, cap-1/cap/cap+1 | TAP-15 | canonical JSON; assembly failure is `degraded/selection_failed` |
| Orientation Markdown | fresh LLM text / persisted UTF-8 brief | five real H2 headings outside fences, canonical instructions, shared valid-fence grammar, <=2,000 estimate | cap boundary; 0–3 vs 4+ leading spaces; fenced/indented-code pseudo-headings; backtick/tilde opener with wrong-marker and shorter/equal/longer close; unterminated fence | TAP-16/TAP-19/TAP-20 | one shared structural scanner; cap-aware finalizer reserves structure and closes with matching marker and adequate run |
| Persisted origin metadata | session `meta.json::pr_context` | mapping with exact `outcome == "used"` for reuse | missing file/key/outcome, legacy shape, bypassed/degraded/other, malformed JSON/non-object | TAP-19/TAP-20 | D-15 rejects to empty context without network |
| Persisted brief | `work/pr_context_orientation.md` bytes | readable UTF-8, nonempty, finalized structure, <=2,000 estimate | missing, directory, invalid UTF-8, empty, malformed headings/fence, cap/cap+1 | TAP-19/TAP-20 | exact reuse only; no repair/truncation |
| Artifact/provider/CURe-log filesystem | `work/` paths, Codex `CODEX_HOME` rollout state, CURe raw/display Codex logs, and `SessionProgress` live metadata under a permissive agent runtime | fixed contained artifacts, atomic complete writes, and no nonempty artifact or orientation prompt/response/event/live-progress bytes reachable during fresh blind/context-free agent calls | blind draft; plan/steps; no-selected and post-build degradation; pre-context partial-write/cleanup failure; context-synth empty retry; orientation success/failure, shared-log/meta routing, and runtime-root cleanup failure | TAP-16/TAP-17/TAP-18/TAP-22 | D-16/D-20 delayed publication and isolated orientation runtime: fake Codex emits the sentinel through normal JSON stdout and provider rollout only beneath the isolated root; ordinary session logs/meta and inherited/default sessions remain sentinel-free; root deletion is verified at every launch; cleanup/isolation failure propagates without an agent |
| Provider telemetry | optional adapter usage mapping and post-provider review validation | nullable integers never substituted by estimates; returned-call usage/latency survives ordinary validation failure | observer absent/`None`, partial mapping, available delivery/fallback usage, ordinary validation exception after provider return on both resume routes | TAP-18/TAP-19/TAP-20/TAP-22 | sanitized current resume record is authoritative at delivery entry; `delivery_validation_failed`, original propagation, no empty retry/final mirror |

### Surface / Branch Proof Matrix

| Surface | Supported Variant | Internal Execution Branch | Proof Class | Owning Proof Seam | Why This Seam Is Sufficient | Out of Scope Notes |
|---|---|---|---|---|---|---|
| Fresh built-in singlepass | omitted / explicit-off | `_pr_flow_impl` eligibility before fetch | behavior | TAP-14, `tests/test_cure_pr_flow.py` | real flow sentinels prove no I/O and one ordinary draft | custom prompt content itself unchanged |
| Fresh built-in singlepass | explicit-on normal/big | artifact-absent blind draft → publish discussion/brief → reconcile | behavior | TAP-16/TAP-17 | path snapshots/read bombs prove every nonempty artifact absent during draft; real review artifact proves post-draft context activation and draft retention fallback | no context capability in initial draft |
| Fresh built-in multipass | explicit-on ordinary plan/steps | discussion/brief in coordinator memory → raw cleanup → all-artifact-absent plan/steps → atomic discussion+canonical publication → shared synth with context / all-artifact-absent provider-call empty retry | behavior | TAP-17 plus TAP-16 template/persistence-order proof | real flow snapshots/read-bombs all three paths during plan/steps and empty retry, captures prompts/synth, proves exact post-step publication and cleanup-gated write-failure behavior, and distinguishes provider-call failure from post-provider validation | no discussion/orientation access in plan/steps or fresh context-free fallback by prompt/filesystem |
| Fresh multipass plan-parse fallback | explicit-on malformed plan output | all-artifact-absent plan → parse failure → all-artifact-absent singlepass blind draft → atomic discussion+canonical publication immediately before reconciliation | behavior | TAP-16/TAP-17 | one real route fixture captures both agent launches and prompts, read-bombs all three paths at each launch, and proves no publication after parse failure until immediately before permitted reconciliation | fallback must not expose context merely because multipass was disabled |
| Fresh multipass successful plan abort | explicit-on valid `abort=true` plan | all-artifact-absent plan → abort review with no further agent → post-agent complete-audit publication → authoritative `bypassed/plan_aborted` flush and one final mirror → return | behavior | TAP-16/TAP-17/TAP-18 | real early-return fixture proves plan prompt/path blindness, no reconcile/synth, audit-only post-agent publication, no reusable brief, exact final metadata, and mirror-before-return behavior | abort review semantics remain existing multipass behavior |
| Fresh custom prompt / file | explicit-on | eligibility bypass | routing + behavior | TAP-14 | real flow fetch/orient sentinels plus unchanged prompt capture | custom reconciliation unsupported |
| Fresh unsupported profile | explicit-on | eligibility bypass | routing + behavior | TAP-14 | exact profile fixture and no-I/O sentinels | named unsupported set is program-owned |
| Historical completed-session selection | `--if-reviewed list|latest|prompt-selected` plus explicit-on | pre-session return in `_pr_flow_impl` | behavior | TAP-14 | real-flow output plus sandbox/history snapshots and fetch/orient bombs prove no invocation or mutation | `--if-reviewed new` continues to normal eligibility |
| Fresh no-review | `--no-review` plus explicit-on | session/index-only branch in `_pr_flow_impl` | behavior | TAP-14 | established real no-review flow plus fetch/select/orient/reconcile/synth bombs and artifact/meta absence prove no enrichment | indexing remains existing behavior |
| Regular resume | used-valid success / provider-call context-synth failure / post-provider validation failure / every rejected metadata-or-brief shape | `_resume_flow_impl` shared synth | behavior | TAP-19/TAP-22 | real executor captures prove exact-or-empty insertion, shared Markdown grammar, exactly one eligible empty fallback, no retry for ordinary validation, current sanitized authority/telemetry, propagation, and no network/final mirror on failure | no refetch/reorientation |
| Regular resume no-delivery | completed/same-head fast no-op / reusable review artifact | `_resume_flow_impl` before incremental dispatch or with `should_synth == false` | behavior | TAP-18/TAP-19 | deep-equal before/after `meta.json::pr_context` and mirror-attempt sentinels prove D-17 preservation | lifecycle status/timestamps remain owned by resume |
| Completed-session incremental resume | used-valid success / provider-call context-synth failure / post-provider validation failure / every rejected metadata-or-brief shape | `_run_incremental_completed_multipass_resume` incremental synth | behavior | TAP-20/TAP-22 | distinct real incremental executor captures prove exact-or-empty insertion, one eligible empty fallback, no validation fallback, sanitized current authority/telemetry, propagation, and retained guidance | no refetch/reorientation |
| Remote audit | empty / nonempty / zero-selected / body-truncated | `fetch_pr_discussion` → pure `build_pr_context` result → `_pr_flow_impl` phase-correct artifact | behavior | TAP-15/TAP-16/TAP-21 | public result plus timed path snapshots and final file read prove complete endpoint order without blind-stage exposure | live network excluded |
| Paginated discussion-list public fallback (`_github_public_api_list` only) | eligible `github.com`; safe terminal/multipage; redirect/unsafe/cyclic/malformed/timeout/budget/HTTP/network/JSON/non-array | authenticated list-adapter failure → explicit no-redirect opener → bounded public page chain → discussion fetch boundary | behavior | TAP-21/TAP-22 | real-opener fake responses prove each redirect code stops after one request; transport captures exact 30.0s timeout, current-page resolution, pre-request origin/canonical-cycle/100-page guards, ordered all-or-nothing arrays, and `degraded/fetch_failed` with no discussion or orientation artifact from partial pages | unauthenticated fallback for other hosts and live network excluded |
| Local history | matching/corrupt sessions present | production composition sentinel | behavior | TAP-21 | read/scan sentinels at removed owners prove no filesystem coupling | migration excluded |
| Metadata/persistence | used/bypassed/degraded, early/late write failure, and no-delivery resume preservation | session meta plus work mirror | behavior | TAP-18/TAP-19 | exact dict/file equality, malformed-origin/fake-clock/usage fixtures, and deep-equal/no-mirror sentinels prove D-14/D-17/D-18 oracle | finding-level analytics excluded |

### Design Sources

| Source | Status | Durable anchor |
|---|---|---|
| Initiative P0 decisions | normative | `openspec/initiatives/simple-pr-context/initiative.md::FB-021` through `FB-024` and `## Decisions & Constraints` |
| Acknowledged PR #28 feedback | normative | `https://github.com/grzegorznowak/CURe/pull/28#issuecomment-5033288018` items absorbed as FB-034, FB-036, FB-037, and FB-038; source receipts in notebook `openspec-feedback-simple-pr-context-simple-pr-context` |
| Technical design | normative | `openspec/changes/simple-pr-context/design.md::Data flow`, `::Deterministic selection`, `::Orientation finalization and injection`, `::Stable metadata contract`, `::Field-level fresh/resume metadata oracle`, `::Persistence and delivery phase semantics`, `::Fail-open boundary`, and `story.md::D-19` |
| Historical implementation/PR material | orientation only | `story.md::Implementation Log` and Plan Review Log's historical branch/PR anchors; superseded where they conflict with FB-021-FB-024 or acknowledged FB-034/FB-036/FB-037/FB-038 |

### Design Element Trace

| Source Anchor | Visible Element / State | Obligation | Bounds / Required Behavior | Scenario | Acceptance ID | Proof Row / Reviewer Action |
|---|---|---|---|---|---|---|
| FB-023 / design eligibility | paired CLI/default off | required | omission and explicit disable perform no enrichment I/O | S17/S18 | A19/A20 | TAP-14 parser + real-flow commands |
| FB-023 / review-only enrichment boundary | historical-selection and `--no-review` non-invocations | required | explicit-on cannot create enrichment work/artifacts/metadata when no new review is delivered | S30/S31 | A33/A34 | TAP-14 real pre-session/no-review flow |
| FB-021 / D-13 | audit/model split and deterministic selection | required | 12k/1k/100, complete audit, exact total orders | S19 | A21 | TAP-15 cap/order/audit fixtures |
| FB-021/FB-036 / orientation finalization | output/injection caps and shared fresh/persisted Markdown grammar | required | independent 2k caps; 0–3 structural, 4+ code; real out-of-fence H2s; matching adequate fence close | S20 | A22 | TAP-16/TAP-19/TAP-20 structural fixtures |
| FB-022 | remote-only MVP | required | three endpoints; no local scan/read | S21 | A23 | TAP-21 raw-boundary/sentinel proof |
| FB-023/FB-038 / D-16 | phase-correct fail-open | required | every fresh context-free agent call sees no nonempty PR-context artifact; late nonempty audit publication/mirror never reruns review; post-provider validation propagates with current resume telemetry and no mirror | S22 | A24 | TAP-16/TAP-17/TAP-18 capability/persistence plus TAP-19/TAP-20/TAP-22 validation propagation |
| FB-023/FB-034 / PLAN-009/PLAN-010 | branch delivery fallback | required | complete discussion/orientation remain in coordinator memory; all three paths and the isolated orientation runtime root are absent through blind draft/plan/steps/fresh context-free calls; ordinary session Codex logs/meta never receive orientation bytes; phase-correct atomic publication; artifact/runtime cleanup required before fallback; one empty retry only for eligible provider-call failure | S23 | A25 | TAP-16/TAP-17 normal-JSON-stdout event/display/live-progress plus Codex-home confinement/deletion boundary and all-path snapshots/read bombs; TAP-19/TAP-20 captures and TAP-22 failure/cleanup propagation |
| FB-024/FB-038 / D-14/D-17/D-18 | pilot observability | required | exact entered-path ownership; sanitized current resume authority on delivery; validation failure retains current telemetry/no mirror; no-delivery preserves origin | S24/S29 | A26 | TAP-18/TAP-19/TAP-20 exact metadata/file/no-mirror assertions |
| FB-023 | zero-data distinctions | required | empty remote vs nonempty zero-selected reasons/artifacts | S25/S28 | A27/A32 | TAP-15/TAP-18 |
| FB-024 | release gate | required | default remains off; separate approval | S26 | A28 | TAP-23 source/runtime gate |
| FB-023/FB-036/FB-038 / D-15 | both resume routes | required | shared structural validation, used-valid exact reuse only, all others empty/no-network, sanitized current authority after delivery entry | S27 | A31 | TAP-19/TAP-20 real prompt/state captures |
| FB-037 / D-19 | bounded paginated discussion-list public fallback | required | `_github_public_api_list` on github.com/auth-eligible list route only; exact no-redirect/origin/timeout/cycle/100-page/transactional failure contract; `_github_public_api_json` unchanged and excluded | S32 | A29 | TAP-21 real no-redirect opener with fake responses plus TAP-22 fetch degradation |

### Risk Lens Inventory

| Lens | Activation | Proof / explicit exclusion |
|---|---|---|
| External service / network / subprocess | GitHub CLI plus D-19 bounded `_github_public_api_list` page chain; public JSON metadata fallback unchanged/outside D-19 | TAP-21 deterministic real-opener transport fakes prove auth/host eligibility, exact per-request timeout, redirect rejection before follow-up, origin/URL/cycle/page guards, transactional ordering/failure; live network excluded |
| Filesystem / persistence / generated artifacts | audit, brief, metadata mirror, persisted resume input, Codex rollout transcripts, CURe raw/display logs and live-progress metadata, fresh blind/context-free capability boundary | TAP-16/TAP-17/TAP-18/TAP-19/TAP-20/TAP-22; one isolated orientation runtime root contains `CODEX_HOME`, event/display logs, and orientation-only progress then is deleted, while ordinary session logs/meta stay sentinel-free; discussion and raw/canonical orientation-path nonexistence continues through blind draft/plan/steps/no-selected/post-build degraded/empty-retry calls, with phase-correct publication and cleanup gating |
| Retries / process lifecycle | one context-free synth retry only for eligible context-bearing provider-call failure; no retry after ordinary validation or metadata-mirror failure | TAP-17/TAP-18/TAP-19/TAP-20/TAP-22 exact invocation counts, current-authority metadata/telemetry, no-final-mirror, and original propagation |
| Prompt/template naming | `$PRIOR_CONTEXT` in shared and incremental synth templates | TAP-16/TAP-19/TAP-20 exact-one token and captured render proof |
| Raw shape / serialization | endpoint JSON, timestamp parsing, canonical prompt JSON, persisted metadata/UTF-8 | Input Boundary Shape Risk rows and TAP-15/TAP-21 |
| Time / telemetry | monotonic stage latency and nullable provider usage | TAP-18 fake clock/adapter variants; no wall-clock/live provider |
| Concurrency / async / permissions / migrations / UI | no new behavior in this story | explicitly excluded; existing process permissions/UI behavior unchanged |

### Test Architecture Plan

| Row ID | Layer / Scope | Behavior / Acceptance Slice | Owning Suite / File(s) | Boundary Exercised | Assertions / Observability | Fixture / Test Data Strategy | CI Lane / Command | Fallback Plan | Split / Merge Rationale |
|---|---|---|---|---|---|---|---|---|---|
| TAP-14 | parser + fresh routing | A19/A20/A27/A33/A34 | `tests/test_cure_pr_flow.py::test_parser_pr_context_is_paired_and_defaults_off`; `tests/_reviewflow_unittest_grounding_impl.py::{test_pr_flow_prior_review_latest_happens_before_picker,test_pr_flow_no_review_skips_review_only_setup}` extended with adjacent list/prompt-selected and PR-context sentinels; `tests/test_cure_pr_flow.py` planned `test_pr_context_*eligibility*` cases | `build_parser` and real `_pr_flow_impl` pre-session returns, no-review route, and boundary before `fetch_pr_discussion` | paired parse values/source; omitted/off/custom/file/unsupported no fetch/orient; enabled empty endpoints bypass; list/latest/prompt-selected and no-review explicit-on paths make no context call/artifact/meta or historical mutation | argparse vectors; seeded completed sessions and TTY choices; sandbox/history snapshots; monkeypatched fetch/orient/reconcile/synth bombs; no network | `python -m pytest tests/test_cure_pr_flow.py::test_parser_pr_context_is_paired_and_defaults_off -v`; `python -m pytest tests/test_reviewflow_unittest.py -k 'pr_flow_prior_review or pr_flow_no_review_skips_review_only_setup' -v`; `python -m pytest tests/test_cure_pr_flow.py -k 'pr_context and (eligibility or no_remote)' -v` | if the established broad-flow owner cannot express a sentinel, add a focused real-flow case in `tests/test_cure_pr_flow.py`; never replace behavior proof with parser/helper-only proof | one row owns operator routing before review creation; each independent exit gets its own fixture/assertions |
| TAP-15 | package/public composition | A21/A32 | `tests/cure_pr_context/test_corpus.py`, `test_init.py`, `test_integration.py` | `select_orientation_events` through pure `build_pr_context` result; final artifact bytes cross-checked with TAP-16 flow owner | D-13 cap/order oracle including parser-accepted aware year-boundary values whose direct UTC conversion overflows; exact counts/flags; complete unchanged audit for empty/nonempty/zero-selected/body-truncated cases | deterministic Unicode canonical JSON; valid/equal/offset-equivalent/parser-accepted aware minimum/maximum-year UTC-overflow/missing/invalid timestamps; cap-1/cap/cap+1; tmp work dir | `python -m pytest tests/cure_pr_context/test_corpus.py tests/cure_pr_context/test_init.py tests/cure_pr_context/test_integration.py -v` | if public composition obscures a boundary, retain public proof and add selector unit fixtures rather than replacing it | selector/result stay merged for A21; TAP-16 separately owns phase-correct file publication without duplicating selection oracles |
| TAP-16 | orientation + template/persistence/runtime-isolation assembly | A22/A24/A25/A29/A31/A32 | `tests/cure_pr_context/test_orient.py`, `tests/cure_pr_context/test_templates.py`, planned `tests/test_cure_pr_flow.py::test_fresh_review_calls_cannot_access_nonempty_pr_context_artifacts` | shared fresh/persisted Markdown scanner; exact-one opaque insertion; pure build result; real fresh `_pr_flow_impl` coordinator-memory → isolated orientation runtime (`CODEX_HOME` + CURe event/display logs + dedicated progress) → blind/context-free stage → phase-correct publication ordering | independent 2k estimates/flags; structure rules; prompts contain no discussion/orientation bytes/path; fake Codex emits the exact sentinel through normal JSON stdout and provider rollout only below the runtime root; ordinary session event/display logs, `meta.json::{last_cmd,live_progress}`, and inherited/default Codex roots remain sentinel-free; the runtime root plus discussion/raw/canonical paths are absent at singlepass draft, every plan/step, parse-fallback draft, abort plan, no-selected/degraded review, and empty synth retry; exact audit/brief bytes appear only at permitted boundaries | cap and Markdown matrices; fake Codex CLI/runner driving real `CodexJsonEventSink`, event/display resolution, live-progress callbacks, and rollout under supplied `CODEX_HOME`; seeded shared/inherited/default paths; every fresh branch scans/snapshots/read-bombs the runtime root and three artifact paths; runtime/artifact cleanup and display-routing failures | `python -m pytest tests/cure_pr_context/test_orient.py tests/cure_pr_context/test_templates.py -v`; `python -m pytest tests/test_cure_pr_flow.py::test_fresh_review_calls_cannot_access_nonempty_pr_context_artifacts -v` | the named real-flow provider/capability owner is authoritative; do not substitute source inspection or another path assertion | unit owners prove structure/caps while one parameterized real-flow owner proves provider/log/progress isolation, every fresh capability branch, and publication phase |
| TAP-17 | fresh delivery behavior | A24/A25/A32 | planned `tests/test_cure_pr_flow.py::{test_fresh_review_calls_cannot_access_nonempty_pr_context_artifacts,test_public_fallback_pagination_failure_degrades_context_free_without_partial_artifacts}` plus established singlepass/reconcile owners | real `_pr_flow_impl` runtime-and-artifact-absent blind draft/plan/steps/plan-parse-fallback/plan-abort/no-selected/degraded calls, phase-correct discussion+brief or audit-only publication, cleanup-gated context-free fallback, multipass provider call, public-fetch degradation, and ordinary output validation | stage warnings/reasons; isolated orientation runtime root and all three paths absent at every blind/context-free launch; ordinary session logs/meta contain no orientation sentinel; runtime cleanup or log-routing failure propagates and launches no review agent; plan-parse fallback publishes only after its blind draft and before reconciliation; successful plan abort publishes only audit after its plan call, completes exact `bypassed/plan_aborted` authority/mirror before return, and publishes no brief; exact bytes otherwise publish only at allowed boundaries; pre-context write failure/first provider failure removes partial/published artifacts before unchanged-output empty synth; cleanup failure launches no agent; post-agent audit failure reruns nothing; D-19 failure records `degraded/fetch_failed` with no partial artifacts; draft retained; validation propagates without retry | injected provider-call versus validation exception; runtime/session-log/meta scans and all-path snapshots/read bombs; malformed-plan fallback and valid abort fixtures with stage-reach/mirror sentinels; per-file write, display-route, and cleanup failure injection; no-selected and selection/orientation degradation fixtures; partial-page fake transport; captured prompts/artifacts | `python -m pytest tests/test_cure_pr_flow.py::test_fresh_review_calls_cannot_access_nonempty_pr_context_artifacts tests/test_cure_pr_flow.py::test_public_fallback_pagination_failure_degrades_context_free_without_partial_artifacts -v`; `python -m pytest tests/test_cure_pr_flow.py -k 'pr_context and (fail_open or reconcile or synthesis)' -v` | use established full-flow grounding fixture only where a named focused owner cannot capture synth invocation; retain the named public-failure route owner | one parameterized capability owner covers singlepass/ordinary-multipass/plan-parse-fallback/plan-abort/no-selected/degraded branches; independent cases own delivery and public-fetch fail-open |
| TAP-18 | metadata + persistence | A24/A26/A27/A32 | `tests/cure_pr_context/test_init.py`; `tests/test_cure_pr_flow.py::{test_tap18_pr_context_meta_d14_d18_complete_route_dictionary,test_resume_metadata_inherits_only_sanitized_used_acquisition,test_tap18_pr_context_singlepass_fresh_success_genuine_route_owner,test_tap17_tap18_pr_context_synthesis_meta_fresh_multipass_canonical_routes,test_tap18_pr_context_meta_no_selected_artifact_genuine_route_owners,test_tap18_tap19_pr_context_meta_resume_no_delivery_same_head_genuine_route,test_tap18_tap19_tap20_resume_pr_context_empty_delivery_records_validator_and_provider_telemetry,test_tap18_empty_prior_context_synth_is_not_pr_context_delivery_telemetry,test_tap18_canonical_metadata_flush_precedes_equal_final_mirror}` plus mirror-failure route owners; the planned singlepass wrapper delegates to `tests/_reviewflow_unittest_grounding_impl.py::test_tap18_pr_context_pr_flow_singlepass_fresh_success_has_exact_authority_usage_and_route_latency` | `progress.meta['pr_context']`, session `meta.json`, `work/pr_context_meta.json`, required audit/brief writes, completed/same-head and reusable-artifact resume exits | exact D-14/D-18 schema/path/field-ownership/reason oracle including fetched-versus-normalized; fake-clock ms; nullable/available usage; sanitized current resume authority on delivery entry; `delivery_validation_failed` retains current usage/latency and inherited acquisition leaves only; no empty retry/final mirror; early write degradation; final mirror failure retention/no retry; D-17 deep-equal origin preservation and zero mirror attempts | deterministic endpoint cardinalities and used/non-used/malformed origin dictionaries; monotonic clock; adapters absent/partial/full usage; write-failure injection by fixed artifact; seeded sentinel origin payload and mirror-write bomb | `python -m pytest tests/cure_pr_context/test_init.py tests/test_cure_pr_flow.py -v` | if package code cannot observe final delivery, keep build metadata assertions there and final/no-delivery oracle in flow tests | persistence is split from delivery because its late phase differs; one exact-dictionary parameterization owns every D-18 reset/inherit/current field class |
| TAP-19 | regular resume orchestration | A22/A24/A25/A26/A31 | `tests/cure_pr_context/test_runtime.py`; `tests/test_cure_pr_flow.py::{test_resume_requires_used_origin,test_resume_pr_context_reuses_exact_used_valid_brief,test_resume_pr_context_reuses_exact_crlf_brief,test_resume_rejects_invalid_used_brief,test_tap18_tap19_pr_context_meta_resume_no_delivery_same_head_genuine_route}`; `tests/_reviewflow_unittest_grounding_impl.py::{test_resume_flow_from_synth_pr_context_synthesis_reuses_exact_crlf_and_fallback_captures_calls,test_tap19_resume_flow_from_synth_prior_context_non_used_and_used_invalid_render_empty,test_tap18_tap19_resume_flow_from_synth_prior_context_same_head_d17_completed_latest_head_is_exact_no_delivery_state}` | `_resume_flow_impl`, shared synth executor/template, `_mark_resume_noop_completed`, and `should_synth` reusable-artifact decision | D-15 cross-product exact brief or `""` under shared Markdown grammar; used-valid provider-call failure gets exactly one empty retry with unchanged plan/steps; ordinary post-provider validation gets no retry, preserves current usage/latency in authoritative sanitized resume state as `delivery_validation_failed`, propagates original failure, and skips final mirror; successful fallback D-14 metadata; fallback failure propagates; D-17 no-delivery branches preserve origin and skip mirror; no fetch/orient/raw token | origin metadata × brief matrix; scripted first/second synth outcomes with captured prompts and seeded plan/steps; sentinel origin payload; same-head and reusable-artifact fixtures | `python -m pytest tests/cure_pr_context/test_runtime.py tests/test_cure_pr_flow.py -v`; `python -m pytest tests/test_reviewflow_unittest.py -k 'resume_flow_from_synth and (pr_context or prior_context)' -v` | extend established grounding fixture if focused flow cannot prove executor prompt; do not use helper-only rendering as closure | distinct row because regular resume uniquely owns both no-delivery exits and the shared-template delivery route |
| TAP-20 | incremental completed resume orchestration | A22/A24/A25/A26/A31 | `tests/_reviewflow_unittest_grounding_impl.py::test_incremental_completed_resume_threads_verbose_guidance_into_resume_synth_prompt` extended/adjacent PR-context fallback cases | `_run_incremental_completed_multipass_resume` + incremental synth executor/template | D-15 representative exact-or-empty insertion under shared Markdown grammar; provider-call failure gets one empty retry; ordinary post-provider validation gets no retry, current sanitized authority/usage/latency plus `delivery_validation_failed`, original propagation, and no final mirror; successful fallback metadata; fallback failure propagates; no network/raw token; guidance retained | established completed-session fixture; used-valid/non-used/used-invalid representatives; scripted first/second synth outcomes; captured prompts and seeded plan/steps | `python -m pytest tests/test_reviewflow_unittest.py -k 'incremental_completed_resume and (prior_context or verbose_guidance or context_synthesis)' -v` | if full origin/brief cross-product is too costly, prove it in TAP-19/shared validator and retain route representatives plus complete fallback outcomes here | separate row is mandatory because this branch has its own template/callsite and must prove its own fallback executor behavior |
| TAP-21 | external/raw boundary + remote-only/public-fallback regression | A23/A29 | `tests/cure_pr_context/test_fetcher.py`, `test_corpus.py`, `test_init.py`, `tests/test_cure_github.py` | raw gh bytes/pages/redirects/next URLs → explicit no-redirect opener + bounded D-19 chain → normalized events → public composition; filesystem sentinel at removed local owner | auth/host eligibility; 30.0s every request; same-origin/cross-origin/relative redirect responses rejected before follow-up with one captured request; current-page resolution; exact effective origin/no userinfo/fragment; malformed/scheme/cross-origin/canonical direct+multihop repeat rejection before request, including host-case, omitted/explicit-port, dot-segment, and percent-encoding equivalent spellings; terminal page 100 success and page-100 next rejection; strict transactional arrays/page order; every endpoint event retained; no local read | fake response chains through the real opener/redirect handler for 301/302/303/307/308 and every safe/failure variant, request-count capture, partial-page discard, equivalent-spelling cycles; local tree/read/scan bombs; no live network | `python -m pytest tests/cure_pr_context/test_fetcher.py tests/cure_pr_context/test_corpus.py tests/cure_pr_context/test_init.py tests/test_cure_github.py -v` | retain direct adapter tests if package fake cannot represent pagination; retain public composition for no-read proof | merged because these are the complete raw-to-public remote-only boundary and retained adapter regression |
| TAP-22 | negative failure boundary | A24/A25/A26/A29/A31 | `tests/test_cure_pr_flow.py` planned `test_pr_context_does_not_swallow_*` parameterization, `test_pr_context_does_not_swallow_build_or_orientation_file_faults_genuine_route`, and `test_public_fallback_pagination_failure_degrades_context_free_without_partial_artifacts` | real `_pr_flow_impl` exceptions outside named enrichment-stage translations, absence/cleanup gate failures, public-chain including redirect failures translated only at fetch, and post-provider ordinary validation on fresh and both resume routes | checkout/config/cancel/draft/plan/step/fallback synth/accept/post/session flush and unrelated faults preserve original signal; partial-publication cleanup failure propagates and launches no context-free agent; the named D-19 owner proves redirect/chain failures become `fetch_failed` and leave no partial artifacts; validation makes no empty retry and entered resume retains current telemetry/authority without final mirror | one fault per stage with stage-reach sentinels; monkeypatched context builder and all three artifact operations; deterministic redirect/partial-page fake transport; no network | `python -m pytest tests/test_cure_pr_flow.py -k 'pr_context and does_not_swallow' -v`; `python -m pytest tests/test_cure_pr_flow.py::test_public_fallback_pagination_failure_degrades_context_free_without_partial_artifacts -v` | split any branch whose existing harness cannot reach the real stage; retain the named genuine fetch-degradation owner and never replace it with source-text assertion | isolated because false-positive swallowing and intentional fetch-boundary degradation require distinct observable signals |
| TAP-23 | final release/regression gates | A28/A29/A30 | all focused owners, `pyproject.toml`, source scan, package/full suite | installed package and complete repository delta | default off; excluded primitives absent from current production contract; all rows final; package/full/ruff/mypy/diff pass | no live network; disposable install target; merge-base changed paths | run every command under `### Verification Commands` | stop and leave proof provisional on any unavailable lane; do not substitute historical GREEN | final gates are merged because they share release disposition, not behavior implementation |

### Acceptance Proof Matrix

| Acceptance ID | Proof Maturity | Proof Method | Reviewer Action | Expected Evidence | Relevant Surfaces | Open Detail |
|---|---|---|---|---|---|---|
| A19 | final | automated TAP-14 | run TAP-14 parser and flow commands | omitted/off/on parse values and source; disabled no-I/O ordinary review | `cure.py::build_parser`, `_pr_flow_impl` | Canonical TAP-14 collected the actual paired parser owner (1), historical/no-review owners (4), and eligibility/no-remote owners (8); all passed. |
| A20 | final | automated TAP-14 | run TAP-14 eligibility filter | custom prompt/file/unsupported each records exact bypass before sentinels | `cure.py::_pr_flow_impl`, prompt/profile routing | custom prompt/file and unsupported-profile pre-I/O cases pass |
| A21 | final | automated TAP-15 | run package composition command and inspect cap boundary IDs | exact D-13 order, <=12k, 1k/100 limits, immutable result/file audit, counts/flags | `cure_pr_context/{corpus,__init__}.py`, discussion artifact | TAP-15 passed 31 tests, including parser-accepted aware minimum/maximum-year offset values ordered by the signed integer microsecond key without UTC conversion. |
| A22 | final | automated TAP-16/TAP-19/TAP-20 | run shared fresh/persisted structural grammar and cap commands | independent <=2k output/injection; 0–3 structural vs 4+ code; real out-of-fence H2s; same-marker adequate close | `orient.py`, persisted validator, both synth templates, fresh insertion | Shared Markdown/cap matrices and both resume owners pass in the canonical package, regular-resume, and incremental-resume commands. |
| A23 | final | automated TAP-21 | run raw/remote-only command and inspect public signature | no local read/export/result; all endpoint-owned events retained | `fetcher.py`, `corpus.py`, `__init__.py` | remote-only public API and no-local-read sentinels pass |
| A24 | final | automated TAP-16/TAP-17/TAP-18/TAP-19/TAP-20/TAP-22 | run fail-open, orientation-runtime/artifact-absence persistence, successful plan-abort completion, and post-provider validation commands | every fresh context-free call sees no nonempty artifact or locally reachable provider/CURe-log/live-progress orientation transcript; plan abort publishes audit post-agent and completes authoritative bypass metadata/mirror before early return; phase-correct late audit/mirror semantics; ordinary validation original propagation/no empty retry; current resume telemetry/no final mirror | `_pr_flow_impl`, `run_codex_exec`, `ReviewflowOutput.run_logged_cmd`, both resume routes, session/work metadata/artifacts | The integrated genuine-flow owner confines normal JSON stdout/rollout/event/display/live-progress state, proves root deletion and no-agent routing/close/cleanup failures; plan-abort and both-resume authority owners pass. |
| A25 | final | automated TAP-16/TAP-17/TAP-19/TAP-20/TAP-22 | run fresh orientation-runtime/all-artifact-absence publication ordering plus provider-call/validation discrimination across delivery routes | blind draft/plan/steps/plan-parse-fallback draft/fresh empty fallback have no prompt, artifact, provider-transcript, CURe event/display-log, or orientation live-progress filesystem context; parse fallback publishes only before reconciliation; exact audit/brief bytes appear only at permitted boundaries; partial write/provider failure cleans artifacts before unchanged-output empty synth; only provider-call context failure gets one empty retry; validation propagates without retry | singlepass reconcile; fresh ordinary and parse-fallback plan routes; fresh synth; both resume synth executors | Integrated orientation isolation plus genuine singlepass/multipass/parse-fallback/abort/no-selected/write-fallback/empty-retry owners pass; close failure now propagates before any later review launch. |
| A26 | final | automated TAP-18/TAP-19/TAP-20/TAP-22 | run exact current-authority metadata, validation-failure, and no-delivery commands | delivery entry replaces raw origin authority with sanitized current record; validation retains current usage/latency, inherited acquisition leaves only, no empty retry/mirror; D-17 no-delivery remains deep-equal | session `meta.json`, `work/pr_context_meta.json`, both resume routes and no-delivery branches | Exact D-14/D-18 dictionaries, both-resume post-provider validation captures, and D-17 no-delivery owners collect and pass. |
| A27 | final | automated TAP-14/TAP-18 | run no-remote filter | empty audit array, no orient, bypass reason, final mirror when writable | fresh flow and work artifacts | enabled empty-endpoint artifact/bypass owner passes |
| A28 | final | automated/file read TAP-23 | run parser/default and inspect final story/release text | default false and only opt-in pilot authorized | parser, initiative/proposal/story | runtime default-off and opt-in-only source gate pass |
| A29 | final | automated TAP-16/TAP-21/TAP-22/TAP-23 | run deterministic D-19 transport plus retained regression commands | exact eligibility, 30.0s/request, redirect rejection before follow-up, safe current-page resolution/origin, pre-request cycle/URL/page guards, 100-page terminal boundary, transactional arrays, fetch degradation/discard, retained regressions | adapter/public fallback/package/templates/full flow | All D-19 opener/redirect/URL/cycle/page-budget/transactional cases and the genuine fetch-degradation route owner pass without live network. |
| A30 | final | source/file read TAP-23 | search current production/result/meta schemas for excluded primitives and inspect scope | no finding identity/disposition/local history/cache/persistent memory/richer analytics | production delta and OpenSpec scope | current PR-context production/result/metadata exclusion scan passes |
| A31 | final | automated TAP-19/TAP-20/TAP-22 | run both exact resume commands with shared structure and current-authority validation failures | exact used-valid brief only under A22 grammar; rejected variants empty; delivery entry makes sanitized current record authoritative; validation follows A24/A26 | both resume functions, shared validator, synth executors/templates | Shared validator matrices and real regular/incremental exact-byte, fallback, and post-provider validation owners pass with no network/final mirror on failure. |
| A32 | final | automated TAP-15/TAP-16/TAP-18 | run zero-selected composition/metadata/capability cases | nonempty unchanged audit, zero selected, no orient, exact reason/counts, artifact absent during review and written afterward | selector, public composition, flow metadata | Canonical zero-selected composition, metadata, and genuine capability owners prove post-agent audit publication and pass. |
| A33 | final | automated TAP-14 | run real historical-selection routing cases for list/latest/prompt-selected with explicit-on | no sandbox/context call/artifact/meta, unchanged historical session, and established output behavior | `_pr_flow_impl` pre-session exits | list/latest/prompt-selected non-invocation sentinels pass |
| A34 | final | automated TAP-14 | run established real no-review flow with explicit-on and context-stage bombs | session/index-only completion with no context call/artifact/meta | `_pr_flow_impl` no-review branches | explicit-on no-review artifact/metadata absence owner passes |

## Critical Files

| Path | Planned role |
|------|--------------|
| `cure.py::{_pr_flow_impl,_run_pr_context_orientation,SessionProgress}` / `build_parser` | paired CLI option, historical/no-review non-invocation routing, pre-fetch eligibility, isolated orientation-local progress/runtime ownership, fresh singlepass/multipass delivery, D-14/D-18 metadata, and D-16/D-20 persistence sequencing |
| `cure.py::_execute_multipass_synth_stage` | shared real synth executor boundary whose fresh/regular/incremental callers own A25 context-bearing failure interception, exact one-empty retry, and fatal retry propagation |
| `cure_llm.py::{run_llm_exec,run_codex_exec,_resolve_codex_events_log_path,_resolve_codex_display_log_path,_ensure_codex_live_progress,_record_codex_live_event,prepare_review_agent_runtime,_iter_codex_session_logs,find_codex_resume_info}` | effective `CODEX_HOME` launch/discovery, progress-selected raw/display log routing, durable rollout/live-progress ownership, isolated orientation runtime seeding/cleanup, and proof that later permissive review runtimes receive neither the deleted root nor orientation-contaminated shared session paths |
| `cure.py::_resume_flow_impl` / `_mark_resume_noop_completed` | regular resume D-15 persisted-origin/brief gate, shared synth A25 fallback, and D-17 completed/same-head plus reusable-artifact no-delivery preservation |
| `cure.py::_run_incremental_completed_multipass_resume` | distinct completed-session incremental resume gate, incremental synth insertion, and A25 fallback |
| `cure_output.py::{ReviewflowOutput.run_logged_cmd,CodexJsonEventSink}` | explicit resolved Codex display-log routing plus raw/display serialization; ordinary runs keep existing logs while orientation writes only beneath its isolated runtime root |
| `cure_pr_context/__init__.py::build_pr_context` | pure remote-only composition/result boundary with no artifact writes; returns complete discussion/finalized orientation to `_pr_flow_impl` for D-16 timing |
| `cure_pr_context/fetcher.py` | complete selected-PR endpoint normalization |
| `cure_pr_context/corpus.py` | D-13 deterministic bounded remote selection; replaces local-history behavior |
| `cure_pr_context/orient.py` | bounded five-section finalization and output cap |
| `cure_pr_context/runtime.py` | fresh eligibility classification, build-metadata application, D-15 persisted-context validation, D-18 resume metadata ownership/sanitization, and atomic brief/metadata persistence |
| `cure_github.py::_github_public_api_list` / `gh_api_list` | retained strict list adapter plus D-19 bounded, same-origin, transactional public discussion pagination; `_github_public_api_json` / `gh_api_json` is explicitly unchanged and outside D-19 |
| `prompts/mrereview_gh_local_big_synth.md` | fresh/regular shared synth exact-one opaque context token |
| `prompts/mrereview_gh_local_big_resume_synth.md` | incremental completed-resume exact-one opaque context token |
| `tests/cure_pr_context/` and `tests/test_cure_github.py` | TAP-15/TAP-16/TAP-18/TAP-21 package/raw/artifact proof |
| `tests/test_cure_pr_flow.py` | TAP-14/TAP-17/TAP-18/TAP-19/TAP-22 real fresh/regular-resume proof |
| `tests/test_cure_pr_flow.py::test_parser_pr_context_is_paired_and_defaults_off` | actual paired `--pr-context` / `--no-pr-context` parser proof owner for TAP-14 |
| `tests/cure_pr_context/test_runtime.py`; `tests/test_cure_pr_flow.py::{test_resume_requires_used_origin,test_resume_pr_context_reuses_exact_used_valid_brief,test_resume_pr_context_reuses_exact_crlf_brief,test_resume_rejects_invalid_used_brief,test_tap18_tap19_pr_context_meta_resume_no_delivery_same_head_genuine_route}`; `tests/_reviewflow_unittest_grounding_impl.py::{test_resume_flow_from_synth_pr_context_synthesis_reuses_exact_crlf_and_fallback_captures_calls,test_tap19_resume_flow_from_synth_prior_context_non_used_and_used_invalid_render_empty,test_tap18_tap19_resume_flow_from_synth_prior_context_same_head_d17_completed_latest_head_is_exact_no_delivery_state,test_tap20_incremental_completed_resume_pr_context_synthesis_reuses_exact_crlf_and_fallback_captures_calls}` | canonical persisted-context validator and current real regular/incremental synth capture owners for TAP-19/TAP-20 |
| `openspec/changes/simple-pr-context/` | amended contract, tasks, evidence, and opt-in release gate |

## Implementation Notes

### New RED-first phases

- **R10 — amended-contract RED:** add TAP-14..TAP-20 failing boundary tests before production edits. Preserve already-green historical behavior without manufacturing failures.
- **R11 — bounded implementation/GREEN:** implement remote-only bounded composition, CLI eligibility/control, fail-open delivery, and stable metadata; run each owning suite independently.
- **R12 — final gates/reconciliation:** run focused/full/quality/type/package/diff/structural checks; reconcile only from current evidence; leave default off and request fresh plan/implementation review through the normal operator workflow.
- **R13 — FB-031–FB-033 remediation RED/GREEN:** first add the genuine `_pr_flow_impl` unrelated-fault seam, the missing canonical TAP-14/TAP-18/TAP-19 owner collection, and D-13 aware year-boundary overflow-ordering cases; then make only the bounded catch/order repairs needed to turn those proofs GREEN. Preserve all existing acceptance semantics and rerun the complete canonical gates before restoring proof maturity.
- **R14 — GATE-001/PRC-001/PRC-002 review remediation:** prove selected-PR remote-only delivery labels RED-first, remove the dead D-15-bypassing reader, make the canonical Ruff inventory include untracked non-ignored Python owners, and rerun all focused and release gates before returning to review.
- **R15 — FB-034/FB-036/FB-037/FB-038 plus PLAN-010 amended RED/GREEN:** first add failing seams proving every fresh blind/context-free agent call is blind by prompt and by nonexistence of discussion plus raw/canonical orientation artifacts and the isolated Codex orientation runtime root; use a fake real Codex launch that emits a unique sentinel through normal JSON stdout and writes it to the effective `CODEX_HOME/sessions` rollout, prove provider rollout plus CURe raw/display logs and orientation live progress are confined to one root, prove ordinary session logs/meta and inherited/default provider state remain sentinel-free, prove whole-root deletion before every permissive review launch, and prove cleanup/log-routing failure launches no agent; phase-correct publication only immediately before permitted context delivery or after all agent calls, cleanup-gated fallback, the shared fresh/persisted Markdown indentation-heading-fence grammar, every D-19 deterministic `_github_public_api_list` chain success/failure including real-opener redirect rejection, the multipass plan-parse fallback's two blind launches/publication boundary, successful plan-abort audit-only/authoritative-metadata early return, and both resume routes' post-provider ordinary-validation authority/telemetry/no-retry/no-mirror behavior. Then make only bounded implementation changes, rerun all existing owners for A22/A24/A25/A26/A29/A31/A32, and keep those proof rows provisional until canonical focused and release gates pass.

### Locked Decisions

- **D-04 — Audit/model split:** full normalized endpoint corpus remains unchanged in audit output; only deterministic selected copies enter orientation.
- **D-05 — Exact defaults:** 12,000 for the canonically assembled complete orientation-generation prompt, 1,000 per-body, 100 events, 2,000 output, and 2,000 injection estimated-token caps.
- **D-06 — Selection order:** newest-first admission until first limit, then chronological model-input order; no skip-to-pack behavior.
- **D-07 — Remote-only MVP:** remove all local session/history discovery and `past_reviews` API/artifact behavior without compatibility fallback.
- **D-08 — Control:** paired CLI Boolean, default off; unsupported/custom flows bypass before I/O.
- **D-09 — Fail-open boundary:** only named enrichment stages degrade. Every fresh blind/context-free agent call—including a multipass plan-parse fallback's plan and subsequent singlepass draft, plus the plan call on a successful `plan.abort` route—receives no rendered discussion/orientation bytes/path, and filesystem blindness is guaranteed by nonexistence: complete nonempty discussion/finalized orientation remain only in coordinator memory, raw orientation output is removed, `work/pr_context_discussion.json`, `work/pr_context_orientation.raw.md`, and `work/pr_context_orientation.md` are absent, and the Codex orientation runtime root has been removed and absence-proved before and throughout the singlepass blind draft, multipass plan/steps, no-selected/post-build degraded review, and fresh empty-context synth fallback. Codex orientation uses one fresh invocation-scoped runtime root containing an isolated `CODEX_HOME`, CURe event/display logs, and dedicated progress metadata; it stages only required regular-file auth/config inputs, inherits no `sessions/`, and uses that same local progress/log/provider boundary for any internal provider retry. The exact orientation prompt/response/event bytes must never be written to the inherited/default Codex root or the ordinary session's Codex logs and `meta.json::{last_cmd,live_progress}`. Runtime cleanup, log-routing, or absence failure propagates as process-control failure and launches no agent. Singlepass keeps blind draft; every fresh, regular-resume, and incremental-resume multipass route whose context-bearing provider call fails adds exactly one empty-context synth-stage invocation from the same successful plan/steps, with no second PR-context fallback. Existing grounding/manual retry policy inside either provider invocation is separately owned and unchanged. A successful empty-context invocation is `degraded/context_synthesis_failed` with `context_mode=off`, combined delivery latency, and separate nullable delivery/fallback provider-usage fields. Before any fresh context-free fallback, partial/published nonempty artifacts are removed and absence is rechecked; inability to establish absence propagates without launching the agent. Ordinary validation after provider return is not fallback eligible and propagates; entered resume routes first retain current usage/latency as `degraded/delivery_validation_failed` with no final mirror.
- **D-10 — Pilot metadata/release:** stable used/bypassed/degraded run records; estimates distinct from provider usage; story completion enables only opt-in pilot.
- **D-11 — Resume authority:** persisted context is reused only for originating `pr_context.outcome == "used"` plus an exact valid/in-cap brief; all other and legacy states resume context-free with no network enrichment.
- **D-12 — Distinct zero-selection bypass:** nonempty audit corpus plus zero admitted events is `bypassed/no_selected_context`; zero normalized events alone is `bypassed/no_remote_context`.
- **D-13 — Timestamp/order oracle:** a valid sort timestamp is a nonempty timezone-aware RFC3339/ISO-8601 string accepted by `datetime.fromisoformat(value.replace("Z", "+00:00"))`; normalize only the sort instant to UTC and never rewrite the event field. Admission orders valid instants descending, then endpoint ordinal ascending (`issue_comment=0`, `review=1`, `review_comment=2`), then zero-based source index ascending. Missing, empty, naive, or parse-invalid timestamps follow every valid instant and use endpoint ordinal/source index ascending. Model input orders valid instants ascending with the same ascending tie-breakers, followed by invalid/missing values in endpoint/source order. Equal instants expressed with different offsets are ties. Parser-accepted timezone-aware values remain valid even when direct `astimezone(timezone.utc)` conversion would underflow or overflow at a year boundary; ordering must compare those instants overflow-safely, remain total and instant-correct, and must not reclassify them with missing/naive/parse-invalid values.
- **D-14 — Metadata path/reason oracle:** `design.md::Stable metadata contract` is exact. Disabled precedence is omission `bypassed/disabled_default` then explicit `--no-pr-context` `bypassed/disabled_cli`; with explicit enablement, custom prompt/file precedes unsupported profile, then fetch/normalize → zero-remote empty-audit write or select → orient/finalize → required pre-context discussion+brief publication → delivery → post-agent nonempty-audit write where applicable, and the first failing entered enrichment stage wins. Zero remote is classified after its empty audit write. Zero selected is classified after selection while the audit remains in memory; ordinary review runs with all nonempty paths absent, then the post-agent audit write completes the bypass. A successful multipass plan abort is classified only after the all-path-absent plan call and abort-review write as `bypassed/plan_aborted`; `_pr_flow_impl` then publishes only the complete audit, flushes authoritative current metadata, attempts the one final mirror, and returns. A post-agent audit failure becomes `degraded/artifact_write_failed` only when no prior degradation exists; otherwise failed persistence telemetry does not replace the first degradation reason. Resume delivery classifies all non-`used`/missing/legacy origins as `resume_without_used_context` and only a `used` origin with bad brief as `resume_invalid_context`; D-17/A33/A34 non-delivery/non-review exits do not enter this table. Successful delivery is `used/context_delivered`; delivery fallback reasons are `reconciliation_failed` or `context_synthesis_failed`. On either resume route after delivery entry, ordinary post-provider validation records `degraded/delivery_validation_failed` with current usage/latency, propagates the original exception, makes no empty retry, and makes no final mirror. The final metadata-mirror failure never replaces the determined outcome/reason; it sets `persistence.meta_artifact=failed` and `persistence.warning=meta_artifact_write_failed`.
- **D-15 — Exact resume owners and persisted producer:** `build_pr_context()` has the exact signature in `design.md::Public result contract` with no `work_dir` and performs no artifact writes. `_run_pr_context_orientation` owns the Codex-only isolated-runtime lifecycle around `run_llm_exec`: derive the effective configured Codex home before overriding `CODEX_HOME`; create a fresh runtime root under coordinator-owned session state with contained `codex-home/`, `logs/codex.events.jsonl`, `logs/codex.log`, and orientation-only `meta.json`; copy only required regular-file `auth.json`/`config.toml` inputs into `codex-home/` (never `sessions/`); call `run_llm_exec(..., progress=orientation_progress)` so event/display/live-progress state cannot mutate the ordinary session; ingest output/usage; make both primary and trusted-directory-return paths pass `codex-home/` explicitly as `codex_root` to `find_codex_resume_info`; then remove the entire runtime root in all success/failure paths. D-20 owns exact log routing and the shared fresh launch gate owns repeated absence/non-contamination verification. Fresh `_pr_flow_impl` holds complete discussion/finalized orientation in memory and publishes both via per-file atomic replacement immediately before permitted context delivery: after the singlepass blind draft/before reconciliation, or after every successful multipass plan/step process/immediately before synth. A branch with no context-bearing call publishes only its complete audit after all review agent calls. On a successful multipass `plan.abort`, the plan process is the final agent call; `_pr_flow_impl` then writes the abort review, publishes only the audit, completes authoritative `bypassed/plan_aborted` metadata and the final mirror, and returns. On multipass plan-parse fallback, the plan and subsequent singlepass blind draft both run before discussion/brief publication immediately ahead of reconciliation. Raw orientation output is removed after coordinator ingestion and before any blind/context-free delivery route. The final nested authority is recorded at session `meta.json::pr_context`; both `_resume_flow_impl` and `_run_incremental_completed_multipass_resume` read those exact persisted paths. The shared and incremental synth templates each own exactly one `$PRIOR_CONTEXT` token. A shared validator returns the exact persisted brief or `""` plus D-14 reason; neither route fetches or orients.
- **D-16 — Persistence phase semantics:** empty `work/pr_context_discussion.json` is non-context-bearing and may be atomically written after zero-event fetch/normalization. The invocation-scoped orientation runtime root is context-bearing regardless of artifact publication state: its provider rollout, CURe event/display logs, and dedicated live-progress metadata are removed together after orientation output/usage ingestion, and root nonexistence is rechecked with the three artifact paths plus ordinary session-log/meta non-contamination before every fresh blind/context-free launch. A multipass plan-parse fallback keeps every nonempty path absent through both plan and fallback blind draft before ordinary singlepass publication. A successful multipass `plan.abort` publishes only the complete audit after the plan agent and abort-review write, completes authoritative bypass metadata/mirror, and never publishes the canonical brief. Nonempty discussion plus canonical orientation use atomic replacement per file and are absent through fresh blind/context-free agent calls. They are published together at the route boundary immediately before permitted reconciliation/synth; on no-selected or post-build degraded routes, only complete discussion is published after all review agent calls. A pre-context write failure records `degraded/artifact_write_failed`; any partial/nonempty publication is removed and absence rechecked before context-free fallback. Cleanup/absence failure propagates without launching an agent. A post-agent audit failure never reruns review: absent an earlier degradation it becomes `artifact_write_failed`, otherwise the first degradation reason remains and discussion persistence records failed. Authoritative run state is session `meta.json::pr_context`, flushed at each route transition—including before an empty-context retry and after its final telemetry; a core session flush failure propagates as existing process-control failure. `work/pr_context_meta.json` is a final best-effort mirror written once only after a successful route completion (delivery, successful fallback, or bypass) has final usage and latency. Mirror failure warns, is not retried, preserves the current review artifact and route outcome/context mode/telemetry, records persistence failure in authoritative metadata, and never triggers a second review. If the empty-context retry fails, the review failure propagates, authoritative metadata retains `degraded/context_synthesis_failed` plus available attempt telemetry, and the final mirror is not attempted.
- **D-17 — No-delivery resume metadata scope:** a PR-context invocation begins only when fresh flow evaluates enrichment or a resume route reaches reconciliation/synthesis with a D-15 context decision. `_resume_flow_impl`'s completed/same-head fast no-op and regular reusable-review-artifact branch perform no new review delivery and are outside the D-14 invocation-path table. They must preserve the originating nested `meta.json::pr_context` value deep-equal with no key/value mutation and must not attempt `work/pr_context_meta.json`; ordinary resume lifecycle status/timestamp/footer updates remain allowed.
- **D-18 — Field-level metadata ownership:** `design.md::Field-level fresh/resume metadata oracle` is normative. Fresh `counts.fetched` is the total object count only after all three endpoint arrays return and pass list/object shape validation; `counts.normalized` is the resulting normalized-event count and is independently assigned only after normalization succeeds. Resume dictionaries inherit only canonical acquisition provenance from an origin whose `outcome == "used"`, reset all acquisition fields for every other origin, and always replace delivery/result/mirror fields with current-attempt observations exactly as the design table specifies. Once either resume route enters delivery, this sanitized current dictionary—not the raw origin—is authoritative, including when ordinary post-provider validation preserves current usage/latency and `delivery_validation_failed` before propagating without retry/mirror. Missing, wrong-typed, negative, or non-enum inherited leaves canonicalize field-by-field to the schema default without making an otherwise used-valid brief ineligible.
- **D-19 — Bounded paginated discussion-list public GitHub fallback:** this decision applies only to `cure_github._github_public_api_list`, reached through `gh_api_list(..., allow_public_fallback=True)` for the selected PR's three discussion endpoints after the existing authenticated list-route eligibility decision on `github.com`. The separate `_github_public_api_json` / `gh_api_json` PR-metadata fallback, including its existing request and failure semantics, is unchanged and outside D-19. Every in-scope list page request uses timeout `30.0` seconds. The transport uses an explicit opener with a redirect handler that rejects every 301/302/303/307/308 response before follow-up; redirects are fetch failures, never pagination pages, and issue exactly one captured request. Resolve each `next` against the current page and, before request, require effective origin exactly scheme `https`, host `api.github.com`, port `443`, with no userinfo or fragment; reject malformed, non-HTTP(S), cross-origin, or canonical requested-URL repeats (direct or multihop). For cycle identity, normalize scheme and host to lowercase, normalize omitted and explicit HTTPS port to effective port `443`, normalize an empty path to `/`, remove dot segments, uppercase percent-escape hex, decode percent-encoded unreserved characters in path and query, and otherwise preserve path and query byte order; compare that canonical identity before every request. Request at most 100 pages: a terminal page 100 succeeds, while any `next` from page 100 fails before page 101. Decode every page as a strict JSON array and expose ordered accumulation only after the entire chain succeeds. Timeout, redirect, unsafe next, cycle, malformed URL, page-budget, HTTP, network, JSON, or non-array failure raises through fetch, discards partial pages, warns and records `degraded/fetch_failed`, writes no discussion or orientation artifact, and continues context-free. Deterministic fake transport, including equivalent-spelling direct and multihop cycles, is authoritative proof; live network is excluded.
- **D-20 — Orientation-local CURe log/progress wiring:** `_run_pr_context_orientation` creates `orientation_runtime_root`, `orientation_codex_home = orientation_runtime_root / "codex-home"`, and `orientation_progress = SessionProgress(orientation_runtime_root / "meta.json", quiet=True)`. It initializes `orientation_progress.meta["logs"]` with `codex_events = orientation_runtime_root / "logs" / "codex.events.jsonl"` and `codex = orientation_runtime_root / "logs" / "codex.log"`, then invokes `run_llm_exec(..., progress=orientation_progress)`; the ordinary session `progress` is not passed. `_resolve_codex_events_log_path` and `_resolve_codex_display_log_path` continue to resolve from the supplied progress. `run_codex_exec` passes the resolved display path on both primary and trusted-directory attempts to the new optional `ReviewflowOutput.run_logged_cmd(..., codex_display_log_path: Path | None = None)` parameter; when supplied, that method opens/owns the designated display file for `CodexJsonEventSink`, and when omitted ordinary logging remains unchanged. `CodexJsonEventSink` remains the serializer for raw JSON and completed-message display text. Output/usage are consumed before whole-root deletion. The RED proof must use normal JSON stdout, not direct file writes or source-text assertions, and must prove the unique orientation sentinel is absent from every later agent-reachable session/work log or metadata file and configured/default Codex root. Any routing, close, cleanup, or absence failure launches no blind/context-free agent.

## Discovery Notes

- `_pr_flow_impl` returns from completed-session `--if-reviewed list|latest|prompt-selected` before sandbox creation and gates review setup/context composition behind `not args.no_review`; these live branches own A33/A34 non-invocation behavior.
- Existing strict GitHub list adapter and endpoint normalizer remain reusable.
- Existing full remote corpus behavior becomes audit evidence, not an unbounded orientation-input requirement.
- Existing local `find_past_reviews`/`sandbox_root`/`past_reviews` behavior is intentionally removed from the MVP rather than hidden behind an empty compatibility shim.
- Existing singlepass already has a blind draft boundary suitable for fallback retention.
- Existing fresh, regular-resume, and incremental-resume multipass plan/step outputs are context-free, so a context-specific synth failure can retry synthesis with empty context without rerunning plan/steps.
- `_resume_flow_impl` has two verified no-delivery exits: the completed/same-head fast no-op through `_mark_resume_noop_completed`, and a reusable-review-artifact branch where `should_synth` remains false. Neither creates a new review artifact or PR-context delivery decision, so D-17 preserves origin evidence instead of inventing a new context classification.
- Existing progress metadata and `work/` artifacts provide the narrow pilot observability surface; no finding-level persistence is needed.
- Historical S1-S16/A1-A18/TAP-01..TAP-13 and R1-R9 remain evidence of prior work only where compatible with D-04..D-10.
- `_pr_flow_impl` now limits `(OSError, UnicodeError)` artifact-write degradation to `_finalize_and_persist_fresh_pr_context`; unrelated construction and raw orientation cleanup/read faults propagate unchanged under D-09 while required orientation writes retain D-16 degradation.
- D-13 now compares parser-accepted aware timestamps by a signed integer microsecond instant key, so minimum/maximum-year offset values remain valid without out-of-range UTC datetime construction.
- Corrected TAP-14/TAP-18/TAP-19 commands collect the paired parser, genuine fresh singlepass/D-18, runtime validator, and current regular-resume owners.
- R15 must preserve every existing proof while adding every-fresh-branch prompt blindness plus discussion/raw/canonical artifact nonexistence—including multipass plan-parse fallback and successful plan abort—one-root confinement of Codex provider state, CURe raw/display logs, and orientation-only progress with ordinary session logs/meta sentinel-free, phase-correct publication and cleanup gating, plan-abort authoritative metadata completion, shared fresh/persisted Markdown structure, D-19 no-redirect bounded transactional `_github_public_api_list` pagination, and sanitized current resume authority on ordinary post-provider validation.
- Remote-only orientation delivery headings now identify selected-PR remote discussion without claiming past CURe reviews, and the removed dead reader leaves `read_persisted_context()` as the sole D-15 validation authority.
- The canonical changed-path Ruff inventory unions tracked merge-base Python changes with untracked non-ignored Python paths, so new package/test owners cannot escape quality proof.

## Implementation Log

- 2026-07-20T12:26:22Z Completed GATE-001/PRC-001/PRC-002 R14 remediation and transitioned implementation to review.
  - RED/GREEN: the focused delivery-prompt provenance test failed against the old singlepass label, then all 6 template tests passed after heading-only repairs; dead `_read_persisted_pr_context_orientation()` was removed and sole-validator scans pass.
  - Canonical proof: the repaired Ruff inventory collected 18 Python paths, including untracked `cure_pr_context/runtime.py` and `tests/cure_pr_context/test_runtime.py`; Ruff passed. Focused template/runtime passed 14, flow passed 67, resume passed 4 plus 2 subtests, combined amended proof passed 579 plus 30 subtests, and full regression passed 784.
  - Final gates: mypy passed 6 source files; merge-base diff hygiene, structural trace, production provenance/sole-validator scan, and disposable `build==1.5.0` wheel/install/import smoke passed. The default interpreter retains the known `build.__main__` environment limitation.

- 2026-07-20T11:31:48Z Completed FB-031–FB-033 R13 remediation and transitioned implementation to review.
  - RED/GREEN: genuine `_pr_flow_impl` build/raw-file `OSError`/`UnicodeError` propagation and D-13 year-boundary overflow failures were reproduced before source edits; scoped catch handling and signed integer instant ordering made them GREEN while required orientation-write degradation remained GREEN.
  - Canonical proof: TAP-14 parser 1, historical/no-review 4, eligibility/no-remote 8; TAP-15 31; TAP-18/TAP-19 runtime/flow 75; resume route 4 plus 2 subtests; TAP-22 19. Combined amended proof passed 578 plus 30 subtests and full regression passed 783.
  - Final gates: Ruff passed all 16 merge-base changed Python paths; scoped mypy passed 6 source files; merge-base diff hygiene passed at `775c8617c9fb6b63c51cd400a974d22e109cf6fb`; disposable `build==1.5.0` wheel/install/import smoke passed after the default interpreter's known repository `build/` namespace prevented the exact frontend from starting.


- 2026-07-18T08:26:57Z Closed the final A29/R12 package and regression gate and transitioned implementation to review.
  - Root cause: the repository's ignored generated `build/` directory is visible as a namespace package when the default interpreter has no PyPA `build` distribution installed; it does not contain or replace a valid `build.__main__`. A disposable virtual environment with PyPA `build==1.5.0` supplied the repository-approved frontend without changing repository dependencies.
  - Proof: the exact `python -m build --wheel` / target install / import smoke passed; stronger imports from `/` resolved `cure_github` and `cure_pr_context` from the disposable installed target. The full suite passed `685`, changed-path Ruff passed, scoped mypy passed, and merge-base diff hygiene passed.
  - Reconciliation: A19-A34 and R10-R12 are final/checked. All tasks and implementation proof pass; status moved `🔄 IN PROGRESS` → `🟣 IN REVIEW`.

- 2026-07-18T07:28:00Z Completed all executable FB-021..FB-024 implementation/proof while retaining the unavailable package gate.
  - Added distinct fresh, regular-resume, and completed-session incremental-resume context-synth fallback success/fatal executor captures and exact partial discussion-write/selection/orientation metadata dictionaries.
  - Proof: 480 amended combined tests plus 11 subtests, 685 full-suite tests, changed-path Ruff, scoped mypy, and merge-base diff hygiene passed.
  - Reconciliation: A19-A28/A30-A34 are final; A29 and the R12 packaging/regression task remain provisional/open because this environment's `python -m build` fails before build startup with no `build.__main__`. Status remains `🔄 IN PROGRESS`.

- 2026-07-16T14:36:49Z Completed approved FB-012..FB-018 and R7-R9 remediation and transitioned to review.
  - RED/GREEN: 14 intended focused failures reproduced FB-014..FB-018 gaps; preserved FB-012 normal/big stale-artifact enforcement and TAP-11/TAP-12 behavior checkpoints were already GREEN. After bounded repairs, 86 package/adapter/focused-flow tests and 4 established real fresh/resume synth captures passed.
  - Adapter proof: TAP-11 captured exact slurp/retry/cached-no-slurp commands plus direct/post-retry auth routing and ineligible propagation. TAP-12 exercised the real public helper with fake two-page Link traversal, ordered accumulation, invalid JSON, and non-array failures. No live CLI/network was used; FB-010 terminal both-attempt exception provenance remains deferred.
  - Final proof: full suite `711 passed`; Ruff passed all 16 merge-base-delta Python paths; mypy passed `cure_github.py cure_pr_context/`; installed wheel imports resolved from the disposable target; structural S1-S14/A1-A16/TAP-01..TAP-12 traceability passed; merge base `775c8617c9fb6b63c51cd400a974d22e109cf6fb` passed `git diff --check "$base" --`.

- 2026-06-20T08:20:00Z Story claimed and implemented in worktree `/home/vscode/add-worktrees/CURe-simple-pr-context-impl`.
  - Added `cure_pr_context` package (`fetcher`, `corpus`, `orient`, public `build_pr_context`) and setuptools package metadata.
  - Added `cure_github.py::gh_api_list`, `_pr_flow_impl` context build phase after `compute_pr_stats`, effective `head_sha=review_head_sha or head_sha`, and prior-context propagation only for the multipass synth/reconcile paths; plan and step entries intentionally exclude prior context.
  - Superseded by the two-pass update: `$PRIOR_CONTEXT` belongs only in the multipass synth template; normal/big singlepass templates exclude it and use the reconcile call when context exists.
  - Verification passed: `python -m pytest tests/cure_pr_context tests/test_cure_pr_flow.py -q` (15 passed), `python -m pytest tests/test_reviewflow_unittest.py -q` (433 passed, 13 subtests), `python -m pytest tests/ -q` (635 passed, 13 subtests), `ruff check cure_pr_context tests/cure_pr_context tests/test_cure_pr_flow.py tests/_reviewflow_unittest_grounding_impl.py`, `mypy cure_pr_context`, and wheel/install import smoke.

- 2026-06-20T17:45:00Z Story resumed after implementation review request-changes.
  - Fixed A4 remote footer trust: `parse_footer_metadata()` now requires a valid non-empty `sha` token inside official footer markers before an event can become a past review; added marker-only footer regression coverage.
  - Fixed fail-hard local session handling: corpus scan validates local `meta.json` parseability/object shape before delegating to `scan_completed_sessions_for_pr`, so corrupt session metadata aborts PR context build.
  - Fixed TAP-07 proof maturity: added a runtime `_pr_flow_impl` monkeypatch test proving `compute_pr_stats` -> `build_pr_context` order, effective `review_head_sha` propagation, and prior-context branch behavior; A7 proof row is final.
  - Fixed meta shape: `build_pr_context().meta` now includes `n_comments`, `n_reviews`, and `n_review_comments` alongside aggregate counts; unit/integration tests assert the split.
  - Verification passed: `python -m pytest tests/cure_pr_context tests/test_cure_pr_flow.py -q` (18 passed), `ruff check cure_pr_context tests/cure_pr_context tests/test_cure_pr_flow.py`, and `mypy cure_pr_context`.

- 2026-07-17 P0 contract supersession/replanning record.
  - Historical implementation and proof entries above remain unchanged evidence of work performed under the then-current contract; they do not establish completion of FB-021..FB-024.
  - Automatic enablement, unbounded complete-corpus model input, local session/history discovery, and fail-hard enrichment are superseded. No production code or tests were changed by this planning amendment.
  - Current implementation must restart RED-first at R10 and remain `🔄 IN PROGRESS`; S17–S31 cover A19–A28/A31–A34, A29/A30 are direct closure acceptances, and all A19–A34 rows are provisional.

## Plan Review Log

- 2026-07-21T14:17:00Z Historical plan-review log compressed after PLAN-010 absorption.
  - Preserved history: PLAN-003–PLAN-009 were addressed by receipts through 2026-07-21T13:59:30Z; earlier independent approval was superseded by later feedback. PLAN-003 preserved exactly three unchecked R15 tasks and FB-035/FB-039 exclusion; PLAN-004–PLAN-009 locked permissive-runtime artifact nonexistence, phase-correct publication, reject-all-redirect paginated-list proof, plan-parse-fallback/plan-abort ownership, and isolated provider-session state.
  - Preserved decisions/evidence: FB-021–FB-024 and acknowledged FB-034/FB-036/FB-037/FB-038; S17–S32/A19–A34; D-09/D-13–D-19; TAP-14–TAP-23; seven provisional A22/A24/A25/A26/A29/A31/A32 rows; FB-010 deferred.
  - Material source anchors: `cure.py::{_pr_flow_impl,_run_pr_context_orientation,prepare_review_agent_runtime}`; `cure_llm.py::{run_llm_exec,run_codex_exec,_resolve_codex_events_log_path,_resolve_codex_display_log_path,_record_codex_live_event,prepare_review_agent_runtime}`; `cure_output.py::{ReviewflowOutput.run_logged_cmd,CodexJsonEventSink}`; `cure_github.py::{_github_public_api_json,_github_public_api_list,gh_api_json,gh_api_list}`; named TAP-14–TAP-23 owners.
  - Addressed review entries/receipts: 2026-07-21T13:20:34Z → 2026-07-21T13:32:08Z; 2026-07-21T13:39:33Z → 2026-07-21T13:45:57Z; 2026-07-21T13:53:12Z → 2026-07-21T13:59:30Z.

- 2026-07-21T14:17:00Z Plan feedback addressed by `/openspec-story-plan-resume`
  - Original plan review entry: 2026-07-21T14:10:19Z
  - Sections edited: `proposal.md::Decisions & Constraints`; `design.md::{Package structure,Provider-state and CURe-log isolation,Data flow,Persistence and delivery phase semantics,Proof ownership}`; `story.md::{Scope,S23,A24,A25,Verification,Input Boundary Shape Risk,Design Element Trace,Risk Lens Inventory,TAP-16,TAP-17,Acceptance Proof Matrix,Critical Files,Implementation Notes,D-09,D-15,D-16,D-20,Discovery Notes,Plan Review Log}`; `tasks.md::R15`
  - Plan lane transition: 🟠 PLAN CHANGES REQUESTED -> 🟡 PLAN DRAFT
  - Changes: PLAN-010 now confines Codex provider rollout, CURe raw/display JSON-event logs, and orientation command/live-progress metadata to one invocation-scoped orientation runtime root. `_run_pr_context_orientation` passes dedicated `SessionProgress` to `run_llm_exec`; existing log resolvers select that local metadata, and both `run_codex_exec` attempts pass the resolved display path to `ReviewflowOutput.run_logged_cmd` so the real `CodexJsonEventSink` cannot write orientation output to the ordinary session log. Output/usage are ingested before whole-root deletion and absence proof. TAP-16/TAP-17 use a unique sentinel through normal Codex JSON stdout plus provider rollout and prove every later agent-reachable session/work log or metadata path and configured/default Codex root remains sentinel-free; routing/cleanup failure launches no agent.
  - Preserved: PLAN-005/PLAN-006 all-artifact publication and reject-all-redirect boundaries; PLAN-007/PLAN-008 route/helper scope; PLAN-009 provider-session isolation; exactly three unchecked R15 tasks; seven provisional A22/A24/A25/A26/A29/A31/A32 rows; all acknowledged FB-034/FB-036/FB-037/FB-038 edits; default-off pilot; FB-035/FB-039 exclusion.
  - Source verification: `cure.py:9405-9499` creates one session `SessionProgress` and shared `work/logs`; `cure.py:9761-9784` passes that progress into orientation today; `cure_llm.py:139-168,391-456,516-569` resolves and writes shared raw/display logs on both attempts; `cure_llm.py:294-350` also writes agent-message text into `progress.meta.live_progress`; `cure_output.py:337-440,574-609` writes raw JSON and display text; `cure_llm.py:1198-1232` gives later review runtimes session/work access. D-20's dedicated progress plus explicit display routing closes each verified local channel.

- 2026-07-21T14:17:00Z Debt Friction recorded by `/openspec-story-plan-resume`
  - Debt Friction: shared Codex logging hid orientation-derived state outside the original provider-root/artifact boundary.
  - Current Story Action: prove fresh blind/context-free review agents cannot access orientation bytes.
  - Friction Evidence: `cure_llm.py::{_resolve_codex_events_log_path,_resolve_codex_display_log_path,_record_codex_live_event,run_codex_exec}` and `cure_output.py::{ReviewflowOutput.run_logged_cmd,CodexJsonEventSink}` persist orientation output through the session's shared logs and `meta.json::live_progress` while later runtimes receive session/work access.
  - Delivery Impact: A24/A25 and TAP-16/TAP-17 would falsely pass a three-artifact/provider-session-root inventory while a permissive later agent could read the orientation sentinel.
  - Decision: fix-now.
  - Scope Justification: orientation-local progress/display routing and whole-root cleanup are directly required to make the story's existing blindness acceptance correct and executable; they add no new product behavior.
  - Guardrail: D-20 fixes exact wiring, TAP-16 drives normal JSON stdout through the real sink, TAP-17 blocks every blind/context-free launch on root absence and ordinary-session sentinel absence, and R15 remains exactly three RED-first tasks.

- 2026-07-21T14:28:28Z Plan review run by fresh maintainer session
  - Verdict: request_changes
  - Plan lane transition: 🟡 PLAN DRAFT -> 🟠 PLAN CHANGES REQUESTED
  - Status transition: unchanged: 🔄 IN PROGRESS -> 🔄 IN PROGRESS
  - Sections reviewed: Purpose, Actors, Triggering Need, Expected Prerequisites, Scope, Out of Scope, Scenarios / Behavior Examples, Acceptance, Verification, Critical Files, Implementation Notes, Locked Decisions, Discovery Notes
  - Original intent checked: `openspec/initiatives/simple-pr-context/initiative.md`; PR #28 body and `https://github.com/grzegorznowak/CURe/pull/28#issuecomment-5033288018`; no ticket key was supplied
  - Traceability: forward complete; backward gaps
  - Design trace: complete
  - Code surfaces searched: `cure.py::{_pr_flow_impl,_run_pr_context_orientation,SessionProgress,_resume_flow_impl,_run_incremental_completed_multipass_resume}`; `cure_llm.py::{run_codex_exec,_resolve_codex_events_log_path,_resolve_codex_display_log_path,_record_codex_live_event,prepare_review_agent_runtime,find_codex_resume_info}`; `cure_output.py::{ReviewflowOutput.run_logged_cmd,CodexJsonEventSink}`; `cure_github.py::{_github_public_api_json,_github_public_api_list,gh_api_json,gh_api_list}`; `cure_pr_context/`; prompts and named tests; `pyproject.toml`
  - Risk lenses reviewed: permissive subprocess/provider state, filesystem/resource cleanup, raw/display/live-progress persistence, external pagination/redirect/timeout lifecycle, generated Markdown structure, prompt fail-open behavior, resume telemetry authority; concurrency/async/UI/migrations excluded
  - Evidence quality: confirmed PLAN-010 source channels and contract repair plus the typed-gate omission; inferred none; unknown remote provider retention outside the documented local boundary; provisional A22/A24/A25/A26/A29/A31/A32 remain bounded by R15
  - Finding closure: PLAN-010 closes the previously identified provider/CURe-log contract gap in Scope, S23, A24/A25, TAP-16/TAP-17, D-09/D-15/D-16/D-20, Critical Files, design, and R15; regression/side-effect ownership is present, but the newly planned typed owner is omitted from the canonical mypy gate
  - Key findings:
    - [High] `Verification Commands` and `tasks.md::R15 final reconciliation` run `mypy cure_github.py cure_pr_context/` even though PLAN-010 explicitly changes the typed production owner `cure_output.py::ReviewflowOutput.run_logged_cmd`; `pyproject.toml:38-49` includes `cure_output.py` in the project's mypy files and direct `mypy cure_output.py` currently succeeds. The plan therefore has no canonical type-check action for its new display-log parameter and file-handle routing. Amend the typed command and R15 final gate to include `cure_output.py` (while retaining the documented `cure_llm.py`/legacy `cure.py` exclusions).
    - [Info] PLAN-010's dedicated progress, explicit display-log routing, whole-root cleanup, normal-JSON-stdout sentinel, and no-agent-on-routing/close/cleanup-failure obligations verify against current shared-log/live-progress source behavior. Exactly three R15 tasks and seven provisional rows remain; FB-035/FB-039 remain explicitly excluded in `tasks.md::R15`.
  - Hypothesis triage: none
  - Debt Friction: fix-now — shared Codex logging hid orientation-derived state; D-20/TAP-16/TAP-17/R15 provide the bounded guardrail
  - Next action: amend `story.md::Verification Commands` and `tasks.md::R15 final reconciliation` so the canonical typed-production gate includes `cure_output.py`, then run a fresh plan review

- 2026-07-21T14:31:06Z Plan feedback addressed by `/openspec-story-plan-resume`
  - Original plan review entry: 2026-07-21T14:28:28Z
  - Sections edited: `story.md::Verification Commands`; `story.md::Plan Review Log`; `tasks.md::R15 final reconciliation`
  - Plan lane transition: 🟠 PLAN CHANGES REQUESTED -> 🟡 PLAN DRAFT
  - Changes: added typed production owner `cure_output.py` to both canonical mypy gates while preserving the deliberate `cure_llm.py` and legacy `cure.py` exclusions. No acceptance, proof-row maturity, R15 task count, product code, tests, workflows, or PR metadata changed.
  - Preserved: exactly three unchecked R15 tasks; seven provisional A22/A24/A25/A26/A29/A31/A32 rows; FB-035/FB-039 exclusion; all PLAN-010 runtime-isolation and RED-first boundaries.
  - Source verification: before this repair, `story.md::Verification Commands` line 200 and `tasks.md::R15 final reconciliation` line 186 both omitted `cure_output.py`; `pyproject.toml:38-49` configures `cure_output.py` as a mypy-owned production file while excluding `cure_llm.py` and tests. The corrected commands now type-check `cure_github.py`, `cure_output.py`, and `cure_pr_context/`.

- 2026-07-21T14:36:27Z Plan review run by fresh maintainer session
  - Verdict: approve
  - Plan lane transition: 🟡 PLAN DRAFT -> 🟢 PLAN APPROVED
  - Status transition: unchanged: 🔄 IN PROGRESS -> 🔄 IN PROGRESS
  - Sections reviewed: Purpose, Actors, Triggering Need, Expected Prerequisites, Scope, Out of Scope, Scenarios / Behavior Examples, Acceptance, Verification, Critical Files, Implementation Notes, Locked Decisions, Discovery Notes
  - Original intent checked: `openspec/initiatives/simple-pr-context/initiative.md`; PR #28 body; `https://github.com/grzegorznowak/CURe/pull/28#issuecomment-5033288018`; no ticket key was supplied
  - Traceability: forward complete; backward complete
  - Design trace: complete
  - Code surfaces searched: `cure.py::{_pr_flow_impl,_run_pr_context_orientation,SessionProgress,_resume_flow_impl,_run_incremental_completed_multipass_resume,_execute_multipass_synth_stage}`; `cure_llm.py::{run_llm_exec,run_codex_exec,_resolve_codex_events_log_path,_resolve_codex_display_log_path,_record_codex_live_event,prepare_review_agent_runtime,find_codex_resume_info}`; `cure_output.py::{ReviewflowOutput.run_logged_cmd,CodexJsonEventSink}`; `cure_github.py::{_github_public_api_json,_github_public_api_list,gh_api_json,gh_api_list}`; `cure_pr_context/`; prompts, named tests, `pyproject.toml`, and merge-base changed-path inventory
  - Risk lenses reviewed: permissive subprocess/provider state, filesystem/resource cleanup, raw/display/live-progress persistence, external pagination/redirect/timeout lifecycle, generated Markdown structure, prompt/template fail-open behavior, resume telemetry authority; concurrency/async/UI/migrations excluded
  - Evidence quality: confirmed current source gaps and every R15 owner/route plus the repaired typed gate; inferred none; unknown remote provider retention outside the explicitly bounded local process/filesystem contract; provisional A22/A24/A25/A26/A29/A31/A32 are safely bounded by executable RED-first R15 ownership
  - Finding closure: PLAN-011 is closed by `story.md::Verification Commands` and `tasks.md::R15 final reconciliation`, both now including `cure_output.py`; `.venv/bin/mypy --no-incremental cure_github.py cure_output.py cure_pr_context/` passed 7 source files, structural inventories and `git diff --check` passed, and no acceptance/proof/exclusion side effect changed
  - Key findings:
    - [Info] No unresolved planning-contract blocker remains. S17-S32 each map once to A19-A29/A31-A34, A30 has direct closure proof, TAP-14-TAP-23 are unique and concrete, all A19-A34 have one APM row, and exactly the seven implementation-dependent rows A22/A24/A25/A26/A29/A31/A32 remain provisional with nonblank R15 closure details.
    - [Info] R15 retains exactly three unchecked RED / bounded-GREEN / final-reconciliation tasks and maps the live shared orientation channels (`cure.py:9761-9784`, `cure_llm.py:139-168,294-350,391-569`, `cure_output.py:337-440,574-609`) to real normal-JSON-stdout, provider-rollout, log/progress isolation, cleanup, and no-agent-on-failure proof rather than a mocked helper seam.
    - [Info] PLAN-011 verifies at `story.md::Verification Commands`, `tasks.md::R15 final reconciliation`, and `pyproject.toml:38-49`; the notebook PLAN-010/PLAN-011 orientation matched current source and coordination anchors without mismatch.
  - Hypothesis triage: none
  - Debt Friction: fix-now — shared Codex logging hides orientation-derived state outside the former artifact/provider-root boundary; D-20/TAP-16/TAP-17/R15 provide direct scoped repair and regression ownership
  - Next action: resume implementation through the approved R15 RED-first plan
