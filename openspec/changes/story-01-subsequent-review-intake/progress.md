# Progress: story-01-subsequent-review-intake

> Runtime/progress artifact migrated from legacy `agent_coordination` during the 2026-06-12 OpenSpec-format rewrite.
> Canonical story contract: `story.md`. Legacy source remains under `/workspaces/cure_workspace/agent_coordination/epics/cure-subsequent-pr-review/`.

## Current Claim
- Claimed at: 2026-06-04T10:00:00Z
- Claimed by: pi child agent (epic-story-claim)
- Scope: Implement Story 01 subsequent-review intake package, fixtures, tests, and branch-safe integration only.
- Primary write surfaces: `projects/CURe/cure_subsequent_review/`, `projects/CURe/tests/test_subsequent_review.py`, `projects/CURe/tests/fixtures/subsequent_review/`, `projects/CURe/cure.py`, `projects/CURe/pyproject.toml`, coordination story/master files.

### Legacy Scaffold Notes
> Legacy story retrofitted for `/epic-story-plan` contract review after operator-directed TAP contract updates.

## Progress Timeline
- 2026-06-04T10:00:00Z Claimed approved story and moved implementation status to in-progress.
- 2026-06-04T10:16:00Z Added red-first `tests/test_subsequent_review.py`; initial focused run failed with missing `cure_subsequent_review` package.
- 2026-06-04T10:18:00Z Implemented `cure_subsequent_review` contracts/control plane/GitHub history/prior corpus/prior findings/finding reconciliation modules, deterministic `tests/fixtures/subsequent_review/simulation_raw.json`, packaging/mypy scope, list-capable GitHub helper, and branch-safe `_pr_flow_impl` integration behind top-level `--subsequent-review`.
- 2026-06-04T10:20:00Z Verified focused and regression checks: `PYTHONPATH=tests python -m unittest tests.test_subsequent_review` (7 tests) passed; `PYTHONPATH=tests python -m unittest tests.test_reviewflow_unittest` (442 tests) passed; `ruff check .` passed; `mypy` passed; venv package install/import smoke passed. Direct system `pip install --force-reinstall .` was blocked by PEP 668 externally-managed environment, so the install/import proof used a temporary virtualenv.
- 2026-06-04T10:21:00Z Moved implementation status to in-review. No blockers. Pre-existing untracked `projects/CURe/docs/examples/subsequent-pr-run-flow.svg` preserved.
- 2026-06-04T10:22:00Z Resumed after implementation review request_changes; added red-first coverage for simulation bullet prior-review markdown and public-fallback list incompleteness, then fixed parser/fallback behavior.
- 2026-06-04T10:23:00Z Verified resumed changes: `PYTHONPATH=tests python -m unittest tests.test_subsequent_review` (9 tests) passed after initial red failures; `PYTHONPATH=tests python -m unittest tests.test_reviewflow_unittest` (442 tests) passed with pre-existing ResourceWarnings; `ruff check .` passed; `mypy` passed. Moved implementation status back to in-review.
- 2026-06-04T15:33:00Z Resumed after FB-004/FB-005/FB-006 re-approved plan. Added red-first tests for duplicate local IDs/ambiguous+transitive supersedes provenance, `gh api --paginate --slurp` fallback/cause preservation, and real generated CURe `review.md` in-scope issue extraction. Implemented origin-keyed reconciliation payloads, discussion pagination endpoint/fetch/cause details with public fallback for recoverable list failures, and generated-review issue parsing/degradation. Verified `PYTHONPATH=tests python -m unittest tests.test_subsequent_review` (17 tests) passed after initial red failures; `PYTHONPATH=tests python -m unittest tests.test_reviewflow_unittest` (442 tests) passed with pre-existing ResourceWarnings; `ruff check .` passed; `mypy` passed. Moved implementation status to in-review.
- 2026-06-04T15:41:40Z Resumed after implementation review finding FB-005/A21. Added red-first regression coverage proving `gh api --paginate --slurp` command strings do not mask API status, rate-limit, or transport stderr causes in both `gh_api_list` public-fallback payloads and discussion pagination markers; preserved true `unknown flag: --slurp` classification. Fixed classifiers to use stderr/stdout (and command-free generic exception details) instead of `ReviewflowSubprocessError.__str__`. Verified focused regression red/green, `PYTHONPATH=tests python -m unittest tests.test_subsequent_review` (18 tests), `PYTHONPATH=tests python -m unittest tests.test_reviewflow_unittest` (442 tests; pre-existing ResourceWarnings), `ruff check .`, and `mypy`. Moved implementation status back to in-review.
- 2026-06-05T16:02:16Z Resumed after PR #22 local review f750 finding FB-008/A13. Added red-first coverage for a remote-only trusted CURe pull review body that enables auto mode and must enter the prior corpus/finding extractor, plus negative coverage for human/missing-author pull review bodies and review comments. Implemented trusted `review` event corpus entries as `source_type="pr_review"` with review ID/URL/author/submitted timestamp/state provenance while preserving `_looks_cure_authored` and keeping `review_comment` bodies out of the prior corpus. Verified focused red/green, `PYTHONPATH=tests python -m unittest tests.test_subsequent_review` (24 tests), `PYTHONPATH=tests python -m unittest tests.test_reviewflow_unittest` (442 tests; pre-existing ResourceWarnings/log noise), and `ruff check . && mypy`. Moved implementation status back to in-review.
- 2026-06-05T16:27:17Z Resumed after PR #22 local review 7efb finding FB-009. Added red-first status-semantics coverage for remote-only trusted pull review and issue-comment corpora: corpus and prior-finding ledgers must be `success` and must not propagate stale `no_prior_reviews` when trusted remote entries exist without local sessions. Implemented delayed `no_prior_reviews` emission in `prior_corpus.py` so it is recorded only when no local sessions and no trusted remote corpus entries exist. Finding 2 from the same review was routed as FB-010 new-story candidate because it spans Story 02 decision and Story 01 intake evidence reuse/persistence.
- 2026-06-06T04:48:30Z Resumed after PR #22 GitHub feedback FB-011. Added red-first coverage for completed-session metadata whose `paths.review_md` points outside the session boundary and for missing review artifacts that must reach prior-corpus degradation. Constrained completed-session review artifact resolution in `cure_sessions.py` to the session directory, added unavailable-session records for subsequent-review intake, and updated `prior_corpus.py` to record `prior_review_artifact_unavailable` with session/path/reason provenance instead of ingesting unrelated markdown or silently dropping missing artifacts before corpus status. Verified focused red/green, `PYTHONPATH=tests python -m unittest tests.test_subsequent_review` (27 tests), `PYTHONPATH=tests python -m unittest tests.test_reviewflow_unittest` (442 tests; pre-existing ResourceWarnings/log noise), `ruff check . && mypy`, and `git diff --check`. Moved implementation status back to in-review.
- 2026-06-06T05:35:31Z Resumed after sandbox 5864 Story 01 feedback FB-013/FB-014/FB-016. Added red-first regressions for generated-review malformed sibling cards plus clean `- None.` sections, symlinked completed-session directories escaping the sandbox root, and same-entry duplicate finding IDs in reconciliation. Implemented per-card generated-review parse-degraded provenance, rejected completed-session entries whose resolved directory leaves `sandbox_root.resolve()` before reading metadata/review content, and indexed duplicate same-entry origin keys while degrading reconciliation with `duplicate_origin_keys`. Verified focused red/green, `PYTHONPATH=tests python -m unittest tests.test_subsequent_review` (30 tests), `PYTHONPATH=tests python -m unittest tests.test_reviewflow_unittest` (442 tests; pre-existing ResourceWarnings/log noise), `ruff check . && mypy`, and `git diff --check`. Moved implementation status back to in-review.
- 2026-06-06T07:12:40Z Reopened after sandbox 59e0 Story 01 feedback FB-017/FB-018/FB-019/FB-020. Request changes require red-first hardening for degraded PR discussion propagation through downstream artifacts/manifest, heading-style section inheritance, ambiguous/missing supersedes status semantics, and additive top-level `schema_version` on persisted module artifacts. Moved implementation status to in-progress.
- 2026-06-06T07:18:50Z Resumed after sandbox 59e0 Story 01 feedback FB-017/FB-018/FB-019/FB-020. Added red-first regressions for downstream degraded discussion propagation, heading-style section inheritance, ambiguous/missing supersedes degraded reconciler status, and top-level module artifact schema versions. Implemented additive `schema_version` in Story 01 module artifacts, propagated degraded discussion reasons through corpus/findings/reconciler and manifest, preserved enclosing section for heading-style findings, and degraded reconciliation for ambiguous or missing `Supersedes` targets while preserving serialized markers. Verified focused red/green, `PYTHONPATH=tests python -m unittest tests.test_subsequent_review` (33 tests), `PYTHONPATH=tests python -m unittest tests.test_reviewflow_unittest` (442 tests; pre-existing ResourceWarnings/log noise), `ruff check . && mypy`, and `git diff --check`. Moved implementation status back to in-review.
- 2026-06-06T08:20:39Z Reopened after sandbox 095b Story 01 feedback FB-021/FB-022/FB-023/FB-024/FB-025. Request changes require red-first hardening for historical `paths.review_md` resolve failures, symlinked `meta.json` containment, generated-review severity-without-source degradation, authored parse-degraded provenance/evidence retention, and remote pull-review reviewed-head provenance. Moved implementation status to in-progress.
- 2026-06-06T08:31:32Z Resumed after sandbox 095b Story 01 feedback FB-021/FB-022/FB-023/FB-024/FB-025. Added red-first regressions for `paths.review_md` symlink-loop/resolve failures, symlinked `meta.json` escape rejection, generated-review severity-without-source degradation, authored parse-degraded provenance/evidence retention, and trusted pull-review reviewed-head propagation. Implemented `review_md_unresolvable` degradation, `meta.json` resolved-path containment checks, parse-degraded provenance payloads, `missing_generated_sources`, and `DiscussionEvent.reviewed_head` propagation through corpus/finding provenance. Verified focused red/green, `PYTHONPATH=tests python -m unittest tests.test_subsequent_review` (35 tests), `PYTHONPATH=tests python -m unittest tests.test_reviewflow_unittest` (442 tests; pre-existing ResourceWarnings/log noise), `ruff check . && mypy`, and `git diff --check`. Focused implementation review approved closure. Moved implementation status back to done.
- 2026-06-07T05:34:05Z Operator explicitly reopened Story 01 after TAP quality-lens contract edits; Plan lane moved to changes requested and implementation status moved to in-review so fresh plan and implementation reviews can run against the amended verification contract. No product code/tests changed in this reopen.
- 2026-06-07T06:43:04Z Resumed after implementation review requested six TAP quality-lens fixes. Added red-first coverage for plural `SubsequentReviewModules` public import, A20-A22 deterministic fixture/golden samples under `tests/fixtures/subsequent_review/`, malformed GitHub list payload degradation plus complete fetch provenance, generated-review incidental `Sources: port:443, ratio:16` rejection, preflight summary disabled later-module statuses, latest/prompt historical exits, and non-TTY prompt fallback-to-new. Implemented the plural alias/export, added regression fixture files/goldens, hardened `_normalize_source_payload()`, tightened extensionless generated source refs to uppercase root files such as `LICENSE:1`, expanded summary statuses across all modules, and preserved branch-safe TAP-07 behavior. Verified focused red failures then green: split pytest suite (47 passed, 6 subtests), public wrapper (49 passed, 13 subtests), reviewflow regression (442 passed, 14 subtests), `ruff check . && mypy`, `git diff --check`, and temporary-venv package install/import smoke for `SubsequentReviewModules`. Moved implementation status back to in-review.

## Session Handoff
- Status: in-review after explicit 2026-06-07 operator reopen for TAP quality-lens contract changes. Previous done closure remains: sandbox 095b feedback hardening review approved FB-021/FB-022/FB-023/FB-024/FB-025; prior sandbox 59e0 and 5864 Story 01 feedback closures remain approved.
- What changed:
  - FB-021 hardening catches historical `paths.review_md` resolve/is-file failures such as symlink loops and carries them as unavailable prior-review artifacts with reason `review_md_unresolvable` instead of aborting intake.
  - FB-022 hardening degrades generated-review cards that have severity but no valid source references as `missing_generated_sources`, preserving artifact provenance.
  - FB-023 hardening rejects completed-session `meta.json` symlinks whose resolved path leaves the resolved session directory before reading metadata.
  - FB-024 hardening adds provenance/evidence to authored parse-degraded statuses such as missing severity.
  - FB-025 hardening adds `DiscussionEvent.reviewed_head` and propagates trusted pull-review commit/head SHA into corpus entries and finding provenance.
  - FB-013 hardening records `parse_degraded` artifact provenance for malformed generated-review sibling cards even when other generated cards parse, while clean `### In Scope Issues` / `- None.` sections remain success-empty.
  - FB-014 hardening rejects completed-session directory entries whose resolved path escapes `sandbox_root.resolve()` before reading `meta.json` or `review.md`, preserving the FB-011 artifact-boundary intent.
  - FB-016 hardening preserves duplicate same-entry display IDs by assigning indexed origin keys in the reconciliation ledger and marking `duplicate_origin_keys` degradation instead of overwriting one origin.
  - FB-011 hardening constrains completed-session `paths.review_md` resolution to the session artifact boundary; missing/out-of-bound review artifacts are carried into subsequent-review intake as unavailable sessions and recorded in `prior_review_corpus.json` as `prior_review_artifact_unavailable` with session/path/reason provenance.
  - Added Story 01 package `projects/CURe/cure_subsequent_review/` with contracts for all 13 modules and implemented intake modules only.
  - Added GitHub discussion intake with explicit degraded statuses for API unavailable, pagination incomplete, and thread-state unavailable; unauthenticated public list fallback now returns `discussion_incomplete` metadata rather than a bare complete list.
  - Added prior review corpus, prior finding extraction with heading, simulation bullet (`- [A-01][Medium] ...`), and generated CURe `review.md` in-scope issue parsing plus parse-degraded handling; reconciliation now preserves origin-keyed local display IDs, ambiguity markers, supersedes edges, and provenance.
  - Added `work/subsequent/` artifact writing through the control plane and branch-safe `cure pr --subsequent-review` integration after work dir creation.
  - Added deterministic Story 01 fixtures with raw prior-review markdown, raw discussion comments, raw source facts, and executable degraded inputs; focused tests include historical `--if-reviewed list` no-artifact behavior, `new` branch intake invocation, simulation bullet extraction, partial parse recovery, public-fallback incompleteness, and `--slurp` failure cause taxonomy for API status/rate-limit/transport.
  - Updated `gh_api_list` and discussion marker classifiers to derive causes from stderr/stdout (or command-free generic exception details), so the expected command flag `--slurp` no longer forces non-CLI failures into `cli_unsupported_flag`; true `unknown flag: --slurp` remains classified as `cli_unsupported_flag`.
  - Updated packaging and plain `mypy` scope for installed package visibility.
- Files touched: `projects/CURe/cure_sessions.py`, `projects/CURe/cure_subsequent_review/prior_findings.py`, `projects/CURe/cure_subsequent_review/finding_identity.py`, `projects/CURe/tests/test_subsequent_review.py`, plus earlier Story 01 surfaces `projects/CURe/cure.py`, `projects/CURe/cure_subsequent_review/prior_corpus.py`, `projects/CURe/pyproject.toml`, `projects/CURe/cure_subsequent_review/*`, and `projects/CURe/tests/fixtures/subsequent_review/simulation_raw.json`.
- Tests run: FB-021/FB-022/FB-023/FB-024/FB-025 focused review tests ✅ after red-first failures; `PYTHONPATH=tests python -m unittest tests.test_subsequent_review` ✅ (35 tests); `PYTHONPATH=tests python -m unittest tests.test_reviewflow_unittest` ✅ (442 tests; pre-existing ResourceWarnings/log noise); `ruff check . && mypy` ✅; `git diff --check` ✅; temporary-venv package install/import smoke ✅ from prior implementation pass.
- Remaining work: product commit/push for sandbox 095b closure; dependent/later-story work remains scoped to Stories 02/03, FB-007, and FB-010/FB-012/FB-015 as already routed.
- Blockers / risks: direct non-venv `python -m pip install --force-reinstall .` cannot run in this container because the Python environment is externally managed (PEP 668); venv equivalent passed.

> Story scaffolded directly by `/epic-story-plan` after interactive planning.

## PR State
- Not applicable / not recorded in the legacy story.

## Unresolved Debt Friction
- None current; story status is `✅ DONE`.


## Live-audit remap progress

- 2026-06-14T10:46:40Z **OpenSpec remap repair**: FB-033 and FB-037 from the superseded Story 05 live-audit bucket are now documented as Story 01 addenda. FB-033 belongs to session/prior-corpus path containment; FB-037 belongs to prior-finding extraction of supported concise generated reviews with degraded provenance for missing optional metadata. Story status remains `✅ DONE` because no product/test code changed in this provenance repair.
